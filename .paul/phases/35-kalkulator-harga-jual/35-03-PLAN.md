---
phase: 35-kalkulator-harga-jual
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - src/pages/Calculator.tsx
autonomous: true
---

<objective>
## Goal
Sesuaikan layout form dan output halaman /calculator: Harga Pokok + Qty sejajar (Qty 25%), Margin + Admin MP sejajar (50/50), label dipersingkat, dan tambah nominal margin di output.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@src/pages/Calculator.tsx
</context>

<acceptance_criteria>

## AC-1: Harga Pokok + Qty sejajar, Qty 25%
```gherkin
Given form Calculator terbuka
When user melihat section input harga dan qty
Then kedua field muncul dalam satu baris horizontal:
  Harga Pokok mengambil ~75% lebar, Qty mengambil ~25% lebar
```

## AC-2: Margin + Admin MP sejajar, label dipersingkat
```gherkin
Given form Calculator terbuka
When user melihat section input margin dan admin mp
Then kedua field muncul dalam satu baris horizontal dengan lebar seimbang (50/50)
  Dan label input admin marketplace berubah menjadi "Admin MP (%)"
```

## AC-3: Nominal margin tampil di output
```gherkin
Given user mengisi Harga Pokok = 10000, Margin = 20%
When kalkulasi berjalan
Then di output section muncul informasi: "Margin: Rp 2.500 / unit" (hargaJual - hargaPokok)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Sesuaikan layout form dan tambah nominal margin di output</name>
  <files>src/pages/Calculator.tsx</files>
  <action>
    **1. Harga Pokok + Qty — inline row:**
    Bungkus kedua field dalam `<div className="grid grid-cols-4 gap-3">`:
    - Harga Pokok: `<div className="col-span-3">` (75%)
    - Qty: `<div className="col-span-1">` (25%)

    **2. Margin + Admin MP — inline row:**
    Bungkus kedua field dalam `<div className="grid grid-cols-2 gap-3">`:
    - Margin (%) → tetap
    - Admin Marketplace (%) → ubah label menjadi "Admin MP (%)"

    **3. Nominal margin di output:**
    Di dalam card output, setelah tabel harga (grid rows), tambahkan satu baris info setelah baris "Harga Jual":
    ```tsx
    <div className="grid grid-cols-3 px-3 py-2 border-t border-slate-800/60 bg-slate-800/30">
      <span className="text-[11px] text-slate-500 col-span-3">
        Margin: Rp {fmt(hargaJual - hargaPokok)} / unit
        {safeQty > 1 && <span className="ml-2 text-slate-600">· Total Rp {fmt((hargaJual - hargaPokok) * safeQty)}</span>}
      </span>
    </div>
    ```
    Letakkan tepat setelah baris "Harga Jual" (sebelum baris "Harga Marketplace").
  </action>
  <verify>npm run lint — tidak ada TypeScript error</verify>
  <done>AC-1, AC-2, AC-3 satisfied</done>
</task>

</tasks>

<output>
Setelah selesai, buat `.paul/phases/35-kalkulator-harga-jual/35-03-SUMMARY.md`
</output>
