---
phase: 15-produk-modal-clean
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Products.tsx
autonomous: true
---

<objective>
## Goal
Perbaiki dua modal di halaman Produk: (1) form Tambah/Edit Produk tanpa scrollbar vertikal dengan spacing kompak, (2) Detail Info Produk diubah ke centered overlay dengan animasi fade+scale dan tanpa scrollbar vertikal.

## Purpose
Konsistensi UX — pola centered modal sudah diterapkan di Mitra (Phase 14-mitra-modal-fix) dan Akun (Phase 14-modal-akun-center). Produk harus mengikuti pola yang sama.

## Output
`src/pages/Products.tsx` dimodifikasi pada dua komponen: `ProductFormModal` dan `ProductDetailPanel`.
</objective>

<context>
@.paul/PROJECT.md
@src/pages/Products.tsx
</context>

<acceptance_criteria>

## AC-1: Form Tambah/Edit Produk tanpa scrollbar
```gherkin
Given user membuka form Tambah Produk atau Edit Produk
When modal muncul di layar normal (≥375px height)
Then seluruh konten form terlihat tanpa scrollbar vertikal
```

## AC-2: Detail Info Produk centered + tanpa scrollbar
```gherkin
Given user mengklik kartu produk di halaman Products
When panel Detail Info Produk muncul
Then modal tampil di tengah layar (horizontal + vertical center) dengan animasi fade+scale, bukan slide dari kanan, dan area konten tidak menampilkan scrollbar vertikal
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Compact ProductFormModal — hapus scrollbar, kurangi spacing</name>
  <files>src/pages/Products.tsx</files>
  <action>
    Di komponen ProductFormModal (sekitar baris 359–389):

    Ubah wrapper modal div (baris 361):
    - Dari: `className="bg-white rounded-3xl shadow-xl w-full max-w-md p-6 lg:p-8 animate-in slide-in-from-bottom-8 max-h-[90vh] overflow-y-auto"`
    - Ke: `className="bg-white rounded-3xl shadow-xl w-full max-w-md p-5 animate-in slide-in-from-bottom-8"`
    (hapus `max-h-[90vh]`, hapus `overflow-y-auto`, hapus `lg:p-8`, ganti `p-6` → `p-5`)

    Ubah title margin (baris 362):
    - Dari: `mb-6`
    - Ke: `mb-4`

    Ubah form spacing (baris 363):
    - Dari: `space-y-4`
    - Ke: `space-y-3`

    Ubah textarea rows (baris 377):
    - Dari: `rows={3}`
    - Ke: `rows={2}`

    Ubah button container padding (baris 382):
    - Dari: `pt-6`
    - Ke: `pt-3`

    Avoid: menghapus `animate-in slide-in-from-bottom-8` (animasi dipertahankan)
  </action>
  <verify>Buka halaman Products → klik "Tambah Produk" → modal muncul tanpa scrollbar vertikal, semua field terlihat</verify>
  <done>AC-1 satisfied: form Tambah/Edit Produk tampil tanpa scrollbar</done>
</task>

<task type="auto">
  <name>Task 2: ProductDetailPanel — center + fade+scale + no-scrollbar</name>
  <files>src/pages/Products.tsx</files>
  <action>
    Di komponen ProductDetailPanel (sekitar baris 239–255):

    1. Overlay div (baris 240):
       - Dari: `className="fixed inset-0 z-50 flex justify-end bg-slate-950/40 backdrop-blur-sm"`
       - Ke: `className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-950/40 backdrop-blur-sm"`

    2. Motion div props (baris 241–246):
       - Hapus: `initial={{ x: '100vw' }}`, `animate={{ x: 0 }}`, `exit={{ x: '100vw' }}`
       - Tambah: `initial={{ opacity: 0, scale: 0.95 }}`, `animate={{ opacity: 1, scale: 1 }}`, `exit={{ opacity: 0, scale: 0.95 }}`
       - Ubah transition: `transition={{ type: 'tween', duration: 0.18 }}`
       - Ubah className: dari `"bg-white w-full max-w-md h-full shadow-2xl flex flex-col"` → `"bg-white w-full max-w-md max-h-[90vh] rounded-2xl shadow-2xl flex flex-col overflow-hidden"`

    3. Header (baris 248):
       - Dari: `px-6 py-5`
       - Ke: `px-5 py-4`

    4. Scrollable content div (baris 255):
       - Dari: `className="flex-1 overflow-y-auto p-6"`
       - Ke: `className="flex-1 overflow-y-auto no-scrollbar p-5"`

    5. Compact spacing di view mode (baris 257+):
       - `space-y-8` → `space-y-5`
       - Image container `mb-5` → `mb-3`
       - Title `mb-1.5` → `mb-1`
       - Price `mb-4` → `mb-2`
       - Status badges `mb-6` → `mb-3`

    Avoid: mengubah edit form yang ada di dalamnya (biarkan edit form spacing seperti semula)
  </action>
  <verify>Buka halaman Products → klik kartu produk → modal muncul di tengah layar dengan animasi fade+scale, tanpa scrollbar vertikal di area konten</verify>
  <done>AC-2 satisfied: Detail Info Produk centered overlay, no scrollbar</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/index.css` — class `.no-scrollbar` sudah ada, tidak perlu diubah
- Edit form fields di dalam ProductDetailPanel (hanya view mode yang dikompak)
- Komponen `FileUpload` dan logika submit

## SCOPE LIMITS
- Hanya `src/pages/Products.tsx` yang dimodifikasi
- Tidak ada perubahan pada logika bisnis, state, atau API calls
- Tidak mengubah halaman lain

</boundaries>

<verification>
- [ ] Buka http://localhost:3000/products — halaman load tanpa error
- [ ] Klik "Tambah Produk" — form muncul tanpa scrollbar, semua field visible
- [ ] Klik kartu produk — modal muncul di center dengan animasi fade+scale
- [ ] Area konten Detail Info Produk: tidak ada scrollbar vertikal terlihat
- [ ] Edit mode di Detail Info Produk: masih berfungsi normal
- [ ] `npm run lint` atau TypeScript check tanpa error baru
</verification>

<success_criteria>
- Kedua task selesai tanpa error TypeScript
- AC-1 dan AC-2 terpenuhi
- Tidak ada regresi pada fitur lain di halaman Products
</success_criteria>

<output>
After completion, create `.paul/phases/15-produk-modal-clean/15-01-SUMMARY.md`
</output>
