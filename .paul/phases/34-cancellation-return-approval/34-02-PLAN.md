---
phase: 34-cancellation-return-approval
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/pages/CancellationsReturns.tsx
autonomous: false
---

<objective>
## Goal
Redesign halaman Pembatalan & Retur: submit form hanya membuat ActionRequest pending (tidak langsung eksekusi), dan tambah panel approval untuk admin/staff.

## Purpose
Satu-satunya entry point untuk cancel/return adalah halaman ini. Setelah Plan 34-01 mengamankan backend, Plan 34-02 mengubah UI agar alurnya konsisten: mitra ajukan → tampil di panel pending → admin/staff approve/reject.

## Output
`src/pages/CancellationsReturns.tsx` — redesign lengkap dengan dua section: pending panel (approval) + riwayat, dan form submit yang hanya buat pending ActionRequest.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/34-cancellation-return-approval/CONTEXT.md
@.paul/phases/34-cancellation-return-approval/34-01-SUMMARY.md

## Source Files
@src/pages/CancellationsReturns.tsx
@src/lib/api.ts
@src/types.ts
</context>

<acceptance_criteria>

## AC-1: Submit form membuat ActionRequest pending, tidak mengubah order
```gherkin
Given mitra login dan ada pesanan eligible untuk cancel/return
When mitra submit form pembatalan atau retur
Then ActionRequest baru dibuat dengan status 'pending' di DB
  AND order status TIDAK berubah
  AND pesan sukses muncul: "Pengajuan berhasil dikirim. Menunggu persetujuan admin."
  AND form ditutup dan request muncul di section "Menunggu Persetujuan"
```

## AC-2: Admin/staff melihat semua pending requests dengan tombol approve/reject
```gherkin
Given ada ActionRequest dengan status 'pending' di DB
When admin atau staff membuka halaman Pembatalan & Retur
Then section "Menunggu Persetujuan" tampil dengan daftar semua pending requests
  AND setiap item menampilkan: nomor pesanan, nama mitra, tipe (Batal/Retur), alasan
  AND setiap item punya tombol "Setujui" (hijau) dan "Tolak" (merah)
```

## AC-3: Approve request mengeksekusi perubahan dan hilang dari pending panel
```gherkin
Given admin/staff melihat pending request di halaman
When admin/staff klik "Setujui" dan konfirmasi
Then PUT /api/requests/:id { status: 'approved' } dikirim ke backend
  AND item hilang dari section "Menunggu Persetujuan"
  AND order yang terkait muncul di section riwayat (cancelled/returned)
  AND toast sukses muncul
```

## AC-4: Reject request hilang dari pending panel, order tidak berubah
```gherkin
Given admin/staff melihat pending request
When admin/staff klik "Tolak" dan konfirmasi
Then PUT /api/requests/:id { status: 'rejected' } dikirim
  AND item hilang dari section "Menunggu Persetujuan"
  AND order status TIDAK berubah di halaman Pesanan
  AND toast info "Pengajuan ditolak" muncul
```

## AC-5: Mitra hanya melihat pending requestnya sendiri
```gherkin
Given mitra login dan punya pending request
When mitra membuka halaman Pembatalan & Retur
Then section "Menunggu Persetujuan" tampil hanya request miliknya
  AND TIDAK ada tombol approve/reject (read-only untuk mitra)
  AND badge status "Menunggu" tampil di setiap pending item
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix handleSubmit — buat pending ActionRequest saja</name>
  <files>src/pages/CancellationsReturns.tsx</files>
  <action>
    Ubah fungsi `handleSubmit` di `CancellationsReturns.tsx`:

    **Hapus dari handleSubmit:**
    - Block `if (orderToProcess.isBilled) { await api.ledgers.removeByOrder(...) }` — jangan hapus ledger saat submit
    - `await api.orders.update(orderToProcess.id, { status: newStatus, isBilled: false, ... })` — jangan update order
    - Update local orders state setelah submit

    **Ganti dengan:**
    - Buat ActionRequest dengan `status: 'pending'` (bukan `'resolved'`):
      ```ts
      const newRequest: Omit<ActionRequest, 'id'> = {
        type: isCancel ? 'cancellation' : 'return',
        orderId: orderToProcess.id,
        mitraId: orderToProcess.mitraId,
        reason: reason || (isCancel ? 'Pembatalan via form' : 'Retur via form'),
        status: 'pending',
        createdAt: Date.now(),
        updatedAt: Date.now(),
      };
      const createdRequest = await api.requests.create(newRequest);
      ```
    - Update local `requests` state: tambah createdRequest ke daftar (tidak perlu update orders)
    - Toast: `'Pengajuan berhasil dikirim. Menunggu persetujuan admin.'`

    **Update confirm modal message:**
    - Ganti pesan konfirmasi dari "bersifat permanen dan saldo tagihan akan otomatis disesuaikan"
    - Menjadi: "Pengajuan akan dikirim ke admin untuk ditinjau. Status pesanan tidak akan berubah sampai disetujui."

    **Tetap pertahankan:**
    - Audit log create (tetap buat log pengajuan)
    - Form reset setelah submit
    - Role check (`allowedRoles`)
  </action>
  <verify>
    `npm run lint` — 0 errors.
    Cek: handleSubmit tidak ada lagi call ke `api.orders.update` atau `api.ledgers.removeByOrder`.
    Cek: ActionRequest dibuat dengan `status: 'pending'`.
  </verify>
  <done>AC-1 satisfied: submit hanya membuat pending ActionRequest</done>
</task>

<task type="auto">
  <name>Task 2: Tambah pending panel dan approve/reject logic</name>
  <files>src/pages/CancellationsReturns.tsx</files>
  <action>
    **Data preparation** (di atas return statement):
    ```ts
    const pendingRequests = requests.filter(r => r.status === 'pending');
    const canApprove = user.role === 'admin' || user.role === 'staff';
    ```

    **Handler handleApprove dan handleReject:**
    ```ts
    const handleApprove = async (request: ActionRequest) => {
      const isConfirmed = await confirm({
        title: 'Setujui Pengajuan',
        message: `Setujui ${request.type === 'cancellation' ? 'pembatalan' : 'retur'} pesanan? Order akan berubah status dan tagihan dihapus.`,
        confirmText: 'Ya, Setujui',
        type: 'info'
      });
      if (!isConfirmed) return;
      try {
        await api.requests.update(request.id, { status: 'approved', updatedAt: Date.now() });
        // Update local state: request approved + order status changed
        const newOrderStatus = request.type === 'cancellation' ? 'cancelled' : 'returned';
        setRequests(prev => prev.map(r => r.id === request.id ? { ...r, status: 'approved' } : r));
        setOrders(prev => prev.map(o => o.id === request.orderId ? { ...o, status: newOrderStatus as OrderStatus, isBilled: false } : o));
        toast.success('Pengajuan disetujui. Pesanan telah dibatalkan/diretur.');
      } catch (err: any) { toast.error(err.message || 'Gagal menyetujui'); }
    };

    const handleReject = async (request: ActionRequest) => {
      const isConfirmed = await confirm({
        title: 'Tolak Pengajuan',
        message: 'Tolak pengajuan ini? Order tidak akan berubah.',
        confirmText: 'Ya, Tolak',
        type: 'danger'
      });
      if (!isConfirmed) return;
      try {
        await api.requests.update(request.id, { status: 'rejected', updatedAt: Date.now() });
        setRequests(prev => prev.map(r => r.id === request.id ? { ...r, status: 'rejected' } : r));
        toast('Pengajuan ditolak.', { icon: 'ℹ️' });
      } catch (err: any) { toast.error(err.message || 'Gagal menolak'); }
    };
    ```

    **Section "Menunggu Persetujuan"** — tambahkan di JSX sebelum section riwayat:

    Tampilkan section ini jika `pendingRequests.length > 0` ATAU `canApprove`:
    ```tsx
    {(pendingRequests.length > 0) && (
      <div className="space-y-3">
        <div className="flex items-center gap-2">
          <Clock className="w-4 h-4 text-amber-500" />
          <h2 className="text-[11px] font-bold text-slate-500 uppercase tracking-widest">
            Menunggu Persetujuan ({pendingRequests.length})
          </h2>
        </div>
        <div className="space-y-3">
          {pendingRequests.map(r => {
            const relatedOrder = orders.find(o => o.id === r.orderId);
            const relatedMitra = mitras.find(m => m.id === r.mitraId);
            return (
              <div key={r.id} className="bg-white border border-amber-200/60 rounded-xl p-4 flex flex-col sm:flex-row sm:items-center justify-between gap-3 border-l-4 border-l-amber-400">
                <div className="space-y-1">
                  <div className="flex items-center gap-2">
                    <span className="text-xs font-bold text-slate-800">#{relatedOrder?.orderNumber || r.orderId.slice(0, 8)}</span>
                    <span className={`text-[10px] font-bold px-2 py-0.5 rounded-full ${r.type === 'cancellation' ? 'bg-red-100 text-red-700' : 'bg-purple-100 text-purple-700'}`}>
                      {r.type === 'cancellation' ? 'Batal' : 'Retur'}
                    </span>
                  </div>
                  {canApprove && <p className="text-[11px] text-slate-500">{relatedMitra?.name || 'Mitra'}</p>}
                  <p className="text-[11px] text-slate-600 italic">"{r.reason}"</p>
                  <p className="text-[10px] text-slate-400">{formatDate(r.createdAt)}</p>
                </div>
                {canApprove ? (
                  <div className="flex gap-2 shrink-0">
                    <button onClick={() => handleApprove(r)} className="px-3 py-2 bg-emerald-500 text-white text-[11px] font-bold rounded-lg hover:bg-emerald-400 transition cursor-pointer">
                      Setujui
                    </button>
                    <button onClick={() => handleReject(r)} className="px-3 py-2 bg-red-500 text-white text-[11px] font-bold rounded-lg hover:bg-red-400 transition cursor-pointer">
                      Tolak
                    </button>
                  </div>
                ) : (
                  <span className="text-[10px] font-bold text-amber-600 bg-amber-50 border border-amber-200 px-2 py-1 rounded-lg">
                    Menunggu
                  </span>
                )}
              </div>
            );
          })}
        </div>
      </div>
    )}
    ```

    Import `Clock` dari lucide-react jika belum ada.

    **Update displayedOrders** — tidak perlu perubahan besar, bagian riwayat (cancelled/returned orders) tetap seperti sekarang.
  </action>
  <verify>
    `npm run lint` — 0 errors.
    Cek: `handleApprove` dan `handleReject` terdefinisi.
    Cek: section pending tampil dengan conditional `pendingRequests.length > 0`.
    Cek: tombol Setujui/Tolak hanya muncul untuk `canApprove`.
  </verify>
  <done>AC-2, AC-3, AC-4, AC-5 satisfied: approval panel berfungsi dengan role-aware UI</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Halaman Pembatalan & Retur setelah redesign:
    - Form submit sekarang membuat ActionRequest pending (order tidak langsung berubah)
    - Section "Menunggu Persetujuan" tampil pending requests
    - Admin/staff punya tombol Setujui/Tolak
    - Mitra hanya melihat status "Menunggu" read-only
  </what-built>
  <how-to-verify>
    1. Login sebagai **mitra** → buka halaman Pembatalan & Retur
    2. Submit pembatalan untuk pesanan yang eligible
    3. Cek: order status TIDAK berubah di halaman Pesanan
    4. Cek: item baru muncul di section "Menunggu Persetujuan" dengan badge "Menunggu"
    5. Login sebagai **admin** → buka Pembatalan & Retur
    6. Cek: pending request mitra tadi muncul dengan tombol Setujui/Tolak
    7. Klik Setujui → konfirmasi
    8. Cek: item hilang dari pending panel
    9. Cek: di halaman Pesanan, order sudah berubah status ke cancelled/returned
  </how-to-verify>
  <resume-signal>Ketik "approved" jika semua berjalan, atau deskripsi masalah untuk di-fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/api/routes/requests.ts` — sudah diubah di Plan 34-01
- `src/api/routes/orders.ts` — sudah diubah di Plan 34-01
- `src/pages/orders/OrderDetail.tsx` — tidak ada cancel/return buttons di sana; tidak perlu diubah
- `src/lib/api.ts` — `api.requests.update` sudah cukup, tidak perlu helper baru
- `handleDelete` function di CancellationsReturns.tsx — delete order permanen tetap ada, tidak diubah

## SCOPE LIMITS
- Tidak tambah fitur withdraw/tarik kembali pengajuan pending
- Tidak ubah schema DB
- Tidak ubah alur pengajuan prioritas di halaman lain
- Tidak redesign section riwayat (cancelled/returned orders history) — tetap seperti sekarang

</boundaries>

<verification>
- [ ] `npm run lint` — 0 errors
- [ ] handleSubmit TIDAK memanggil `api.orders.update` atau `api.ledgers.removeByOrder`
- [ ] ActionRequest dibuat dengan `status: 'pending'`
- [ ] Section pending tampil jika ada `pendingRequests.length > 0`
- [ ] Tombol Setujui/Tolak hanya untuk role admin/staff
- [ ] Approve: update local orders state + remove dari pending
- [ ] Reject: remove dari pending, orders tidak berubah
- [ ] Checkpoint:human-verify dilalui dan user approve
- [ ] AC-1 sampai AC-5 semua satisfied
</verification>

<success_criteria>
- Semua tasks completed dan checkpoint approved
- `npm run lint` clean
- Alur pengajuan → approval berfungsi end-to-end
</success_criteria>

<output>
Setelah selesai dan checkpoint approved, buat `.paul/phases/34-cancellation-return-approval/34-02-SUMMARY.md`
</output>
