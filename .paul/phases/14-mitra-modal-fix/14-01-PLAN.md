---
phase: 14-mitra-modal-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Mitras.tsx
autonomous: true
---

<objective>
## Goal
Modal Detail Info Mitra di desktop tampil clean tanpa scrollbar vertikal yang toggle.
</objective>

<context>
@src/pages/Mitras.tsx
</context>

<acceptance_criteria>

## AC-1: Modal tidak menampilkan scrollbar vertikal yang toggle
```gherkin
Given pengguna membuka modal Detail Info Mitra di desktop
When modal ditampilkan
Then tidak ada scrollbar vertikal yang muncul/menghilang (no-scrollbar)
And semua konten modal terlihat clean tanpa visual scrollbar
```

</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Fix modal scroll — no-scrollbar + max-h 90vh</name>
  <files>src/pages/Mitras.tsx</files>
  <action>
    1. Pada motion.div modal: ganti `lg:max-h-[85vh]` → `lg:max-h-[90vh]`
    2. Pada div konten `flex-1 overflow-y-auto p-5`: tambahkan `no-scrollbar`
    3. Kompres spacing konten view mode: `space-y-5` → `space-y-3.5`, grid gap `gap-2.5` → `gap-2`, padding cards `p-3` → `p-2.5`
  </action>
  <verify>npm run lint</verify>
  <done>AC-1 satisfied: no-scrollbar class aktif, max-h lebih besar, spacing lebih kompak</done>
</task>
</tasks>

<output>
After completion, create `.paul/phases/14-mitra-modal-fix/14-01-SUMMARY.md`
</output>
