---
phase: 18-orders-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/orders/OrdersList.tsx
autonomous: false
---

<objective>
## Goal
Tingkatkan visual polish halaman Daftar Pesanan (mobile & desktop): status badge dark-theme dengan glow, filter status jadi horizontal scrollable chips, typography dikurangi uppercase-nya, mobile card hierarchy diperbaiki, search bar active state, dan pagination dipadatkan.

## Purpose
Halaman pesanan adalah halaman paling sering dibuka. Perbaikan visual ini menaikkan skor UI dari 8.2 → target 9+ tanpa menambah fitur baru — hanya memperhalus elemen yang sudah ada.

## Output
`src/pages/orders/OrdersList.tsx` diperbarui.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@src/pages/orders/OrdersList.tsx
</context>

<acceptance_criteria>

## AC-1: Status Badge Dark-Theme Glow
```gherkin
Given halaman OrdersList dirender
When melihat badge status pada card/tabel
Then badge menggunakan dark background (bukan light/white bg) dengan warna text+border yang matching, dan ada subtle glow shadow sesuai warna status — tanpa mengubah teks label status
```

## AC-2: Filter Status → Horizontal Scrollable Chips
```gherkin
Given halaman OrdersList dirender (bukan isDraftPage)
When melihat area filter status
Then dropdown <select> status diganti dengan row horizontal chips yang bisa di-scroll, setiap chip menampilkan label + count pesanan, chip aktif berwarna biru solid, chip tidak aktif berwarna transparan dengan border
```

## AC-3: Typography — Kurangi Uppercase Berlebihan
```gherkin
Given halaman OrdersList dirender
When melihat label-label di mobile card (Tipe, Total) dan header tabel desktop
Then label kecil di mobile card tidak lagi full uppercase+tracking-widest (gunakan title case atau sentence case yang lebih readable), header tabel desktop pertahankan uppercase tapi tanpa tracking terlalu renggang
```

## AC-4: Mobile Card — Customer Name Dominan
```gherkin
Given pengguna dengan role bukan mitra membuka OrdersList di mobile
When melihat card pesanan
Then nama mitra/customer tampil sebagai elemen paling dominan di area kiri-atas card (font lebih besar, warna lebih terang dari order number), order number tampil lebih kecil di bawahnya
```

## AC-5: Search Bar Active State
```gherkin
Given halaman OrdersList dirender
When pengguna focus atau mengetik di search bar
Then search bar memiliki filled surface yang lebih terang (bg-slate-800 saat focus), border berubah ke biru, ada clear button (×) yang muncul saat ada input
```

## AC-6: Pagination Kompak
```gherkin
Given halaman OrdersList dirender dengan data > pageSize
When melihat area pagination di bawah
Then pagination menggunakan satu baris yang lebih ringkas: info "X–Y dari Z" di kiri, tombol navigasi + nomor halaman di kanan, tanpa blok besar terpisah; "Tampilkan N" dropdown tetap ada tapi lebih kecil inline
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Redesign status badge ke dark-theme glow</name>
  <files>src/pages/orders/OrdersList.tsx</files>
  <action>
    Ubah fungsi `getStatusBadge()` — ganti semua `bg-*-50` / `text-*-700` / `border-*-200` ke dark-theme equivalents dengan glow shadow:

    ```ts
    const specs = {
      draft:               { bg: 'bg-slate-500/15',  text: 'text-slate-300',  border: 'border-slate-500/30',  glow: '',                          dot: 'bg-slate-400',   label: 'Draft' },
      waiting_confirmation:{ bg: 'bg-amber-500/15',  text: 'text-amber-300',  border: 'border-amber-500/30',  glow: 'shadow-amber-500/20',        dot: 'bg-amber-400',   label: 'Menunggu' },
      confirmed:           { bg: 'bg-blue-500/15',   text: 'text-blue-300',   border: 'border-blue-500/30',   glow: 'shadow-blue-500/20',         dot: 'bg-blue-400',    label: 'Dikonfirmasi' },
      processing:          { bg: 'bg-purple-500/15', text: 'text-purple-300', border: 'border-purple-500/30', glow: 'shadow-purple-500/20',       dot: 'bg-purple-400',  label: 'Diproses' },
      pressing:            { bg: 'bg-rose-500/15',   text: 'text-rose-300',   border: 'border-rose-500/30',   glow: 'shadow-rose-500/20',         dot: 'bg-rose-400',    label: 'Press Sablon' },
      packing:             { bg: 'bg-orange-500/15', text: 'text-orange-300', border: 'border-orange-500/30', glow: 'shadow-orange-500/20',       dot: 'bg-orange-400',  label: 'Packing' },
      shipped:             { bg: 'bg-emerald-500/15',text: 'text-emerald-300',border: 'border-emerald-500/30',glow: 'shadow-emerald-500/20',      dot: 'bg-emerald-400', label: 'Terkirim' },
      returned:            { bg: 'bg-red-500/15',    text: 'text-red-300',    border: 'border-red-500/30',    glow: 'shadow-red-500/20',          dot: 'bg-red-400',     label: 'Retur' },
      cancelled:           { bg: 'bg-slate-500/10',  text: 'text-slate-400',  border: 'border-slate-600/30',  glow: '',                          dot: 'bg-slate-500',   label: 'Batal' },
    };
    ```

    Update render badge menggunakan `shadow-sm ${s.glow}`:
    ```tsx
    <span className={`inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full border text-[10px] font-bold uppercase tracking-wide shadow-sm ${s.glow} ${s.bg} ${s.text} ${s.border}`}>
      <span className={`w-1.5 h-1.5 rounded-full ${s.dot}`} />
      {s.label}
    </span>
    ```

    Ganti `font-extrabold tracking-wider` → `font-bold tracking-wide` (sedikit kurangi aggressiveness uppercase).
  </action>
  <verify>Buka /orders — semua badge tampil dark background (tidak ada putih/terang), warna per-status berbeda, tidak ada visual regression</verify>
  <done>AC-1 satisfied: badge dark-theme glow konsisten di semua status</done>
</task>

<task type="auto">
  <name>Task 2: Ganti filter dropdown dengan horizontal scrollable chips</name>
  <files>src/pages/orders/OrdersList.tsx</files>
  <action>
    Hapus blok `<div className="flex items-center gap-2 bg-slate-900 ...">` yang berisi `<select>` status filter.

    Ganti dengan horizontal scrollable chips di bawah search bar (pindah ke baris terpisah, full width):

    ```tsx
    {!isDraftPage && (
      <div className="flex gap-2 overflow-x-auto pb-1 scrollbar-hide -mx-1 px-1">
        {[
          { value: 'all', label: 'Semua', count: totalStats.orders },
          { value: 'waiting_confirmation', label: 'Menunggu', count: statusStats['waiting_confirmation']?.orders || 0 },
          { value: 'confirmed', label: 'Dikonfirmasi', count: statusStats['confirmed']?.orders || 0 },
          { value: 'processing', label: 'Diproses', count: statusStats['processing']?.orders || 0 },
          { value: 'pressing', label: 'Press', count: statusStats['pressing']?.orders || 0 },
          { value: 'packing', label: 'Packing', count: statusStats['packing']?.orders || 0 },
          { value: 'shipped', label: 'Terkirim', count: statusStats['shipped']?.orders || 0 },
        ].map(chip => (
          <button
            key={chip.value}
            type="button"
            onClick={() => { setStatusFilter(chip.value); setCurrentPage(1); }}
            className={cn(
              'flex items-center gap-1.5 px-3 py-1.5 rounded-xl text-xs font-semibold whitespace-nowrap transition-all border shrink-0',
              statusFilter === chip.value
                ? 'bg-blue-600 text-white border-blue-500 shadow-lg shadow-blue-500/20'
                : 'bg-slate-900 text-slate-400 border-slate-800 hover:border-slate-600 hover:text-slate-200'
            )}
          >
            {chip.label}
            {chip.count > 0 && (
              <span className={cn(
                'text-[10px] font-bold px-1.5 py-0.5 rounded-md tabular-nums',
                statusFilter === chip.value ? 'bg-white/20 text-white' : 'bg-slate-800 text-slate-500'
              )}>
                {chip.count}
              </span>
            )}
          </button>
        ))}
      </div>
    )}
    ```

    Sesuaikan layout control bar: search bar full width di baris pertama, chips di baris kedua (hapus `lg:flex-row` jika tidak relevan lagi). Pertahankan bagian "Total: N Pesanan" tapi pindahkan inline dengan chips atau tata ulang agar tidak mengambil terlalu banyak ruang.

    Tambahkan `import` yang diperlukan jika ada, tidak menghapus yang masih dipakai. Hapus import `LayoutList` jika tidak lagi digunakan oleh elemen lain.
  </action>
  <verify>Buka /orders — tampil row chips horizontal yang bisa di-scroll, chip "Semua" aktif by default, klik chip lain filter berubah, chip aktif biru solid dengan count</verify>
  <done>AC-2 satisfied: filter chips horizontal berfungsi dan tergantikan dropdown</done>
</task>

<task type="auto">
  <name>Task 3: Typography, mobile card hierarchy, search clear button, pagination kompak</name>
  <files>src/pages/orders/OrdersList.tsx</files>
  <action>
    **3a. Mobile card — customer name dominan (role bukan mitra):**
    Ubah order informasi di card mobile: pindahkan `mitraName` ke posisi paling atas kiri dengan `font-bold text-white text-sm`, order number di bawahnya dengan `font-mono text-xs text-slate-500 tracking-tight` (tidak uppercase lagi atau uppercase tapi kecil):
    ```tsx
    {user.role !== 'mitra' && (
      <p className="font-bold text-white text-sm leading-tight">{mitraName}</p>
    )}
    <p className="font-mono text-xs text-slate-500 mt-0.5">{o.orderNumber}</p>
    ```

    **3b. Mobile card label kecil — kurangi uppercase agresif:**
    Ganti `text-[9px] text-slate-600 font-bold uppercase tracking-widest` pada label "Tipe" dan "Total" di footer card menjadi:
    `text-[10px] text-slate-500 font-medium` (title case, tanpa uppercase+tracking-widest)

    **3c. Search bar — clear button + filled surface on focus:**
    Tambahkan state `const [searchFocused, setSearchFocused] = useState(false)` (opsional, bisa pakai CSS `focus-within` saja).
    Tambahkan clear button (×) yang muncul saat `searchQuery` tidak kosong:
    ```tsx
    {searchQuery && (
      <button
        type="button"
        onClick={() => handleSearchChange('')}
        className="absolute right-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-500 hover:text-slate-300 transition-colors"
      >
        <X className="w-full h-full" />
      </button>
    )}
    ```
    Tambahkan `X` ke import lucide-react. Tambahkan `pr-8` ke className input saat `searchQuery` ada (atau selalu `pr-10`).

    **3d. Pagination kompak — single row:**
    Ganti blok pagination yang sekarang (`flex flex-col lg:flex-row ... p-4 bg-slate-900/50`) dengan layout single row yang lebih ringkas:
    ```tsx
    <div className="flex items-center justify-between gap-3 px-1 py-2">
      {/* Kiri: info + page size */}
      <div className="flex items-center gap-3">
        <select
          value={pageSize}
          onChange={e => { setPageSize(Number(e.target.value)); setCurrentPage(1); }}
          className="bg-slate-900 border border-slate-800 rounded-lg text-[11px] font-bold text-slate-400 outline-none px-2 py-1.5 cursor-pointer"
        >
          <option value={5}>5</option>
          <option value={10}>10</option>
          <option value={25}>25</option>
          <option value={50}>50</option>
          <option value={100}>100</option>
        </select>
        <span className="text-[11px] text-slate-500 tabular-nums hidden sm:block">
          <span className="text-slate-300 font-semibold">{startIndex + 1}–{Math.min(endIndex, totalItems)}</span>
          {' '}<span className="opacity-60">dari</span>{' '}
          <span className="text-slate-300 font-semibold">{totalItems}</span>
        </span>
      </div>
      {/* Kanan: prev + pages + next */}
      {totalPages > 1 && (
        <div className="flex items-center gap-1">
          <button onClick={() => setCurrentPage(p => Math.max(p-1,1))} disabled={currentPage===1}
            className="w-8 h-8 flex items-center justify-center rounded-lg border border-slate-800 bg-slate-900 text-slate-400 hover:text-white hover:bg-slate-800 transition disabled:opacity-30 disabled:cursor-not-allowed">
            <ChevronLeft className="w-4 h-4" />
          </button>
          <div className="flex items-center gap-1">
            {getPageNumbers().map((p, idx) => {
              if (p === 'ellipsis1' || p === 'ellipsis2') return (
                <span key={`e-${idx}`} className="w-7 text-center text-slate-700 text-xs">…</span>
              );
              const n = p as number;
              return (
                <button key={n} onClick={() => setCurrentPage(n)}
                  className={cn('w-8 h-8 rounded-lg text-[12px] font-bold transition-all tabular-nums',
                    n === currentPage
                      ? 'bg-blue-600 text-white shadow-md shadow-blue-500/20'
                      : 'text-slate-500 hover:text-slate-200 hover:bg-slate-800')}>
                  {n}
                </button>
              );
            })}
          </div>
          <button onClick={() => setCurrentPage(p => Math.min(p+1,totalPages))} disabled={currentPage===totalPages}
            className="w-8 h-8 flex items-center justify-center rounded-lg border border-slate-800 bg-slate-900 text-slate-400 hover:text-white hover:bg-slate-800 transition disabled:opacity-30 disabled:cursor-not-allowed">
            <ChevronRight className="w-4 h-4" />
          </button>
        </div>
      )}
    </div>
    ```
    Hapus wrapper `bg-slate-900/50 rounded-2xl shadow-xl border border-white/5 backdrop-blur-sm p-4` dari pagination — lebih lightweight tanpa container besar.
  </action>
  <verify>
    1. Mobile card (role admin/staff): nama customer paling atas, besar, terang
    2. Label "Tipe"/"Total" di footer card tidak lagi uppercase tracking-widest
    3. Search bar: ketik sesuatu → muncul tombol ×, klik × → input kosong
    4. Pagination: tampil satu baris ringkas tanpa container card besar
  </verify>
  <done>AC-3, AC-4, AC-5, AC-6 satisfied: typography, hierarchy, search clear, pagination kompak</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>OrdersList.tsx diupdate: status badge dark-glow, filter chips horizontal, typography lebih clean, mobile card hierarchy, search clear button, pagination kompak</what-built>
  <how-to-verify>
    1. Jalankan dev server: npm run dev (port 3000)
    2. Login dan buka http://localhost:3000/orders
    3. Cek di mode mobile (DevTools → mobile viewport):
       [ ] Badge status: dark background berwarna (bukan putih), ada glow subtle per warna
       [ ] Filter chips horizontal scrollable, chip "Semua" aktif default (biru)
       [ ] Klik chip "Menunggu" → hanya pesanan waiting_confirmation yang tampil
       [ ] Customer name lebih besar dan terang dari order number di card mobile
       [ ] Label "Tipe"/"Total" tidak uppercase tracking-widest lagi
       [ ] Search: ketik sesuatu → muncul tombol ×, klik × → bersih
       [ ] Pagination: satu baris ringkas, tidak ada container card besar
    4. Cek desktop (≥768px):
       [ ] Tabel masih tampil dan header tabel readable
       [ ] Badge konsisten dengan mobile
    5. Pastikan filter masih berfungsi (klik chips)
    6. Pastikan klik card masih navigate ke detail pesanan
  </how-to-verify>
  <resume-signal>Ketik "approved" untuk lanjut ke UNIFY, atau deskripsikan masalah</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logic filtering (role-based, draft page, status filter) — hanya styling
- `getPageNumbers()` function — algoritma pagination tidak diubah
- `handleLogin`, API calls, `useEffect` data fetching
- `OrderDetail.tsx`, `CreateOrder.tsx` — tidak disentuh
- Desktop tabel layout — pertahankan struktur kolom

## SCOPE LIMITS
- Tidak menambah fitur baru (summary cards, sorting, realtime — itu Plan B)
- Tidak mengubah routing atau navigation
- Tidak mengubah backend/API
- Hanya `src/pages/orders/OrdersList.tsx`

</boundaries>

<verification>
Sebelum plan dianggap complete:
- [ ] `npm run lint` — 0 TypeScript error
- [ ] Halaman /orders render tanpa console error
- [ ] Filter chips berfungsi (klik chip → orders filtered)
- [ ] Search + clear button berfungsi
- [ ] Navigasi ke order detail masih berfungsi
- [ ] Semua 6 AC terpenuhi
- [ ] Checkpoint visual verify pass
</verification>

<success_criteria>
- OrdersList.tsx diperbarui sesuai 6 AC
- Tidak ada regresi pada fungsionalitas filter, search, pagination, navigasi
- TypeScript clean
- Visual verify pass dari user
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/18-orders-visual-polish/18-01-SUMMARY.md`
</output>
