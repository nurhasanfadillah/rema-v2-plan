---
phase: 36-sidebar-user-footer
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Pindahkan info pengguna dan tombol logout ke footer sidebar dalam satu baris; klik avatar atau nama membuka modal ganti password inline (tanpa navigasi ke /change-password).
</objective>

<context>
@.paul/PROJECT.md
@src/components/Layout.tsx
@src/pages/ChangePassword.tsx
</context>

<acceptance_criteria>
## AC-1: Sidebar Footer User Row
```gherkin
Given pengguna login dan sidebar terbuka
When sidebar dirender
Then footer sidebar menampilkan satu baris berisi: avatar-initial (bulat kecil) + nama pengguna + tombol LogOut di ujung kanan
```

## AC-2: Modal Ganti Password
```gherkin
Given footer sidebar terlihat
When pengguna klik avatar atau nama pengguna di footer sidebar
Then muncul modal centered dengan form ganti kata sandi (field: kata sandi lama, baru, konfirmasi) — tanpa navigasi ke /change-password
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Sidebar footer + modal ganti password inline</name>
  <files>src/components/Layout.tsx</files>
  <action>
    1. Tambah state `changePassOpen` (boolean, default false) di Layout.
    
    2. **Ganti footer SidebarContent** (line 120-123 yang kosong) dengan satu baris:
       ```
       [avatar-initial circle] [nama + role badge] [LogOut icon button]
       ```
       - Avatar: div kecil ~w-8 h-8 rounded-full bg-blue-500/15 text-blue-400 font-bold text-xs
       - Nama + role: flex-1, nama truncated text-sm text-white, badge role text-[9px] text-blue-400
       - Entire left section (avatar + nama) dapat diklik → setChangePassOpen(true)
       - LogOut button di ujung kanan → logout() langsung
       - Styling: p-4 border-t border-slate-900 flex items-center gap-3 cursor-pointer hover:bg-slate-900/50

    3. **Tambah modal ganti password** baru (setelah profileOpen AnimatePresence block):
       - State: `changePassOpen`, `cpOldPw`, `cpNewPw`, `cpConfirm`, `cpLoading`
       - Backdrop + centered dialog (mirip gaya profileOpen tapi lebih kecil, centered)
       - Form dengan 3 field (kata sandi lama, baru, konfirmasi) — field lama tidak muncul jika user.mustChangePassword
       - Submit call: `api.users.changePassword(user.id, { oldPassword: cpOldPw, newPassword: cpNewPw })`
       - On success: `updateUser(updatedUser)`, toast.success, tutup modal, reset field
       - On error: toast.error
       - Saat modal ditutup (X atau backdrop): reset semua field cp*

    4. **Hapus/pertahankan** profileOpen block di header — pertahankan saja apa adanya (tidak perlu dihapus, biarkan ada di header juga).

    5. Import tambahan yang dibutuhkan: `updateUser` dari useAuth (cek apakah sudah ada).
       
    Hindari: mengubah route /change-password, mengubah ChangePassword.tsx, mengubah logic navLinks.
  </action>
  <verify>
    1. Buka app → sidebar terlihat → footer menampilkan avatar + nama + tombol keluar dalam satu baris
    2. Klik avatar/nama → modal ganti password muncul dengan 3 field
    3. Klik LogOut di footer → langsung logout
    4. Form submit berhasil → toast success + modal tutup
  </verify>
  <done>AC-1 dan AC-2 satisfied: footer sidebar satu baris + modal inline berfungsi</done>
</task>
</tasks>

<output>
Setelah selesai, buat `.paul/phases/36-sidebar-user-footer/36-01-SUMMARY.md`
</output>
