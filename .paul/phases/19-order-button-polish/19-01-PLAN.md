---
phase: 19-order-button-polish
plan: 01
type: execute
wave: 1
depends_on: ["18-01"]
files_modified:
  - src/pages/orders/OrderDetail.tsx
autonomous: true
---

<objective>
## Goal
Dua perubahan UI kecil di halaman Order Detail:
1. Warna tombol "Lanjutkan ke {status}" lebih mencolok — ganti dari blue-600 ke gradient/solid yang lebih vivid dengan shadow lebih kuat
2. Label tombol PDF ubah dari "Cetak SPK A6" → "Cetak SPK"

## Purpose
Tombol primary CTA status harus langsung menangkap perhatian operator di lapangan — saat ini terlalu menyatu dengan background gelap. Label "A6" tidak relevan bagi end-user (itu ukuran kertas internal).

## Output
- `src/pages/orders/OrderDetail.tsx` — 2 perubahan inline:
  - Tombol `canAdvanceNormally()` CTA: className lebih vivid
  - PDFDownloadLink label: "Cetak SPK"
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@src/pages/orders/OrderDetail.tsx
</context>

<acceptance_criteria>

## AC-1: Label SPK dipersingkat
```gherkin
Given halaman Order Detail terbuka
When user melihat section "Aksi & Kontrol"
Then tombol download PDF bertuliskan "Cetak SPK" (bukan "Cetak SPK A6")
```

## AC-2: Tombol status lebih mencolok
```gherkin
Given halaman Order Detail dengan status yang bisa dilanjutkan (mis. waiting_confirmation)
When user melihat tombol CTA "Lanjutkan ke..."
Then tombol tampak vivid — warna solid terang, shadow lebih kuat, kontras tinggi vs background gelap
```

## AC-3: Type check bersih
```gherkin
Given perubahan diterapkan
When `npm run lint` dijalankan
Then tidak ada TypeScript error
```

</acceptance_criteria>

<tasks>
## T1 — Ubah label "Cetak SPK A6" → "Cetak SPK"
- File: `src/pages/orders/OrderDetail.tsx`
- Lokasi: string literal `'Cetak SPK A6'` di dalam PDFDownloadLink render prop
- Aksi: ganti teks saja

## T2 — Perkuat warna tombol CTA status
- File: `src/pages/orders/OrderDetail.tsx`  
- Lokasi: tombol `canAdvanceNormally() && normalNext` (sudah ada `bg-blue-600`)
- Aksi: ubah ke warna lebih vivid — contoh `bg-blue-500` + shadow lebih kuat + ring lebih visible, atau pakai green/emerald untuk aksi "maju"
- Pertimbangan: jangan ubah semantik warna (masih boleh biru/hijau), cukup contrast lebih tinggi

## T3 — Type check
- Jalankan `npm run lint`
- Pastikan clean
</tasks>
