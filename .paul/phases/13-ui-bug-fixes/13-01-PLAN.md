---
phase: 13-ui-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/orders/OrderDetail.tsx
  - src/components/Layout.tsx
  - src/pages/Dashboard.tsx
  - src/pages/Login.tsx
  - src/pages/Mitras.tsx
  - src/pages/orders/CreateOrder.tsx
autonomous: true
---

<objective>
## Goal
Perbaiki 9 item: 3 bug (button mitra+draft, sidebar active, modal copy), 5 UI/copy (dashboard labels, about text, login alignment, login desktop layout, mitra modal desktop center), dan 1 feature (admin edit harga satuan di mode edit).

## Purpose
Meningkatkan konsistensi UX dan memperbaiki bug-bug yang ditemukan saat usage production — role mitra melihat button redundan, sidebar menampilkan 2 item aktif sekaligus, dan admin tidak bisa override harga satuan saat edit pesanan.

## Output
6 file dimodifikasi. Tidak ada file baru. Tidak ada perubahan API/schema.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@src/pages/orders/OrderDetail.tsx
@src/components/Layout.tsx
@src/pages/Dashboard.tsx
@src/pages/Login.tsx
@src/pages/Mitras.tsx
@src/pages/orders/CreateOrder.tsx
</context>

<acceptance_criteria>

## AC-1: Button "Lanjutkan" tidak muncul untuk mitra di pesanan draft
```gherkin
Given pengguna login sebagai mitra dan membuka detail pesanan berstatus draft
When halaman Order Detail ditampilkan
Then hanya tombol "Ajukan Sekarang" yang terlihat di section Aksi & Kontrol Pesanan
And tombol "Lanjutkan ke Menunggu Konfirmasi" tidak tampil
```

## AC-2: Copy modal konfirmasi Ajukan Draft sesuai konteks pengajuan
```gherkin
Given mitra menekan tombol "Ajukan Sekarang" di pesanan draft
When modal konfirmasi muncul
Then judul modal adalah "Ajukan Draft Pesanan"
And pesan modal menjelaskan bahwa pesanan akan dikirim ke admin untuk verifikasi
And bukan teks generik "mengubah status pesanan menjadi waiting_confirmation"
```

## AC-3: Sidebar aktif hanya satu item saat di halaman Draft Pesanan
```gherkin
Given pengguna mitra berada di halaman /orders/drafts
When sidebar dirender
Then hanya item "Draft Pesanan" yang aktif (highlight biru)
And item "Daftar Pesanan" tidak aktif
```

## AC-4: Label dashboard sesuai spesifikasi baru
```gherkin
Given pengguna berada di halaman Dashboard
When section grafik dan tabel pesanan ditampilkan
Then label grafik menampilkan "Monitor Pesanan Masuk"
And label tabel menampilkan "Pesanan Terbaru"
```

## AC-5: Deskripsi "Tentang Sistem" menjelaskan aplikasi REMA secara user-friendly
```gherkin
Given pengguna berada di halaman Dashboard
When section Tentang Sistem ditampilkan
Then teks menjelaskan tujuan aplikasi REMA untuk mitra dan admin
And tidak ada referensi teknis (Cloudflare R2, cloud storage, dll)
```

## AC-6: Modal Detail Info Mitra di desktop tampil centered tanpa overflow
```gherkin
Given pengguna berada di halaman Mitra pada layar desktop (lg+)
When modal Detail Info Mitra dibuka
Then modal tampil di tengah layar (horizontally dan vertically centered)
And tidak ada horizontal/vertical overflow sehingga tidak perlu scroll layar
And pada mobile tetap tampil sebagai drawer dari kanan
```

## AC-7: Login page — teks heading dan subheading centered
```gherkin
Given pengguna membuka halaman Login
When form login ditampilkan
Then teks "Selamat Datang Kembali" dan teksnya di bawahnya memiliki text-align center
```

## AC-8: Login page desktop — layout fit dalam viewport tanpa scroll
```gherkin
Given pengguna membuka halaman Login di desktop (lg+)
When halaman dirender
Then tidak perlu scroll untuk melihat seluruh form login
And halaman menggunakan h-screen bukan min-h-screen
```

## AC-9: Admin dapat mengedit harga satuan item saat mode edit pesanan
```gherkin
Given admin/staff membuka halaman edit pesanan (/orders/:id/edit)
When item produk ditampilkan
Then ada input field untuk mengubah harga satuan (priceSnapshot) per item
And mengubah harga satuan langsung mengupdate subtotal item
And mitra tidak melihat input harga satuan (hanya admin/staff)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Bug fixes — button mitra+draft, sidebar active state, modal copy</name>
  <files>src/pages/orders/OrderDetail.tsx, src/components/Layout.tsx</files>
  <action>
    **OrderDetail.tsx — Fix 1: Hide "Lanjutkan ke..." button untuk mitra+draft**
    Di `canAdvanceNormally()` (line ~244), baris:
    ```
    if (order.status === 'draft') return user.role === 'mitra';
    ```
    Ubah ke `return false` — karena mitra+draft sudah punya panel "Ajukan Sekarang" tersendiri (line 515-532). Button "Lanjutkan ke..." di section Aksi & Kontrol jadi tidak muncul untuk kasus ini.

    **OrderDetail.tsx — Fix 2: Copy modal untuk Ajukan Draft**
    Di `handleUpdateStatus()` (line ~117-122), sebelum `const isConfirmed = await confirm(...)`, tambahkan kondisi khusus untuk `newStatus === 'waiting_confirmation' && order.status === 'draft'`:
    ```typescript
    const isDraftSubmit = newStatus === 'waiting_confirmation' && order.status === 'draft';
    const isConfirmed = await confirm({
      title: isDraftSubmit ? 'Ajukan Draft Pesanan' : (isCancel ? 'Batalkan Pesanan' : (isReturn ? 'Retur Pesanan' : 'Update Status Pesanan')),
      message: isDraftSubmit
        ? 'Pesanan akan dikirimkan ke admin untuk diverifikasi. Pastikan semua detail item dan lampiran sudah benar sebelum mengajukan.'
        : `Apakah Anda yakin ingin ${isCancel ? 'membatalkan' : (isReturn ? 'meretur' : `mengubah status pesanan menjadi ${STATUS_LABELS[newStatus] || newStatus}`)} pesanan ini?`,
      confirmText: isDraftSubmit ? 'Ya, Ajukan' : (isCancel ? 'Ya, Batalkan' : (isReturn ? 'Ya, Retur' : 'Ya, Update')),
      type: (isCancel || isReturn) ? 'danger' : 'info'
    });
    ```

    **Layout.tsx — Fix 3: Sidebar active state**
    Di array `links` (line ~26-38), tambahkan property `excludePrefix?: string` untuk link `/orders`:
    ```typescript
    { to: '/orders', ..., excludePrefix: '/orders/drafts' }
    ```
    Pada render NavLink (line ~58-84), gunakan `useLocation()` (sudah diimport) untuk custom isActive:
    ```tsx
    <NavLink 
      key={l.to} 
      to={l.to}
      className={({ isActive }) => {
        const active = l.excludePrefix 
          ? location.pathname.startsWith(l.to) && !location.pathname.startsWith(l.excludePrefix)
          : isActive;
        return cn(
          "flex items-center ...",
          active ? "bg-gradient-to-r from-blue-600 to-blue-500 text-white shadow-lg shadow-blue-500/15"
                 : "text-slate-400 hover:bg-slate-900 hover:text-slate-100"
        );
      }}
    >
      {({ isActive }) => {
        const active = l.excludePrefix 
          ? location.pathname.startsWith(l.to) && !location.pathname.startsWith(l.excludePrefix)
          : isActive;
        return (
          <>
            <div className="flex items-center gap-3">
              {React.cloneElement(l.icon as React.ReactElement, { 
                className: cn("w-4.5 h-4.5 transition-colors duration-300", 
                active ? "text-white" : "text-slate-500 group-hover:text-slate-300") 
              })}
              <span>{l.label}</span>
            </div>
            {!active && <ChevronRight ... />}
          </>
        );
      }}
    </NavLink>
    ```
    Juga perlu update tipe link di array: `type Link = { to: string; icon: JSX.Element; label: string; roles: string[]; excludePrefix?: string }` (bisa inline, tidak perlu export).
  </action>
  <verify>
    1. Buka detail pesanan berstatus draft sebagai mitra → hanya "Ajukan Sekarang" terlihat, tidak ada "Lanjutkan ke Menunggu Konfirmasi"
    2. Klik "Ajukan Sekarang" → modal muncul dengan judul "Ajukan Draft Pesanan" dan teks pengajuan
    3. Navigasi ke /orders/drafts sebagai mitra → sidebar "Draft Pesanan" aktif, "Daftar Pesanan" tidak aktif
    4. Navigasi ke /orders → sidebar "Daftar Pesanan" aktif, "Draft Pesanan" tidak aktif
    5. `npm run lint` tanpa error TypeScript
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: UI & Copy — Dashboard labels, About text, Login alignment, Login desktop, Mitra modal</name>
  <files>src/pages/Dashboard.tsx, src/pages/Login.tsx, src/pages/Mitras.tsx</files>
  <action>
    **Dashboard.tsx — Label grafik dan tabel**
    - Line ~170: Ganti `Grafik Pesanan Masuk` → `Monitor Pesanan Masuk`
    - Line ~222: Ganti `10 Pesanan Dikonfirmasi Terakhir` → `Pesanan Terbaru`

    **Dashboard.tsx — Tentang Sistem**
    - Line ~268-271, ganti isi paragraf menjadi teks user-friendly:
    ```
    REMA adalah platform digital untuk mitra konveksi PT. Redone Berkah Mandiri Utama. 
    Melalui aplikasi ini, mitra dapat membuat dan memantau pesanan secara real-time, 
    sementara admin mengelola alur produksi, keuangan, dan aktivitas mitra dalam satu tempat.
    ```

    **Login.tsx — Text alignment center**
    - Line ~107: div `className="mb-10"` → tambahkan `text-center` sehingga heading dan subheading centered
    - Hasilnya: `<div className="mb-10 text-center">`

    **Login.tsx — Desktop fit screen**
    - Line ~34: Ganti `min-h-screen` → `h-screen` di class wrapper utama `<div className="min-h-screen w-full flex ...">` agar layout menggunakan height viewport penuh dan tidak scroll
    - Right panel (line ~86): ganti padding dari `p-6 sm:p-12 md:p-20` ke `p-6 sm:p-10 lg:p-12` dan pastikan `overflow-y-auto` ada agar konten tidak terpotong
    - Tambahkan `overflow-hidden` di wrapper utama untuk mencegah scroll

    **Mitras.tsx — Modal Detail Info Mitra desktop center**
    Modal container (line ~331): saat ini `fixed inset-0 z-50 flex justify-end` (drawer kanan).
    Ubah ke responsive:
    ```tsx
    <div className="fixed inset-0 z-50 flex lg:items-center lg:justify-center justify-end bg-slate-950/40 backdrop-blur-sm">
      <motion.div
        initial={{ x: '100vw' }}  // tetap untuk mobile
        animate={{ x: 0 }}
        ...
        className="bg-slate-950 w-full max-w-md lg:max-h-[85vh] lg:rounded-3xl h-full lg:h-auto shadow-2xl flex flex-col border border-white/5 lg:border-white/10 overflow-hidden"
      >
    ```
    Pastikan inner scrollable area (`flex-1 overflow-y-auto`) tetap ada agar konten panjang bisa di-scroll dalam modal — tapi layar tidak perlu di-scroll.
    Untuk animasi desktop yang lebih natural, gunakan:
    ```tsx
    initial={{ opacity: 0, scale: 0.95 }}  // untuk lg
    animate={{ opacity: 1, scale: 1 }}
    ```
    Implementasi: gunakan breakpoint CSS untuk animasi. Tapi karena Framer Motion tidak langsung support breakpoint, gunakan pendekatan sederhana: tetap gunakan animasi x untuk mobile (drawer) dan tambahkan `lg:` class untuk centering. Animasi visual yang minor tidak bermasalah karena fokus utama adalah posisi dan ukuran.
  </action>
  <verify>
    1. Buka Dashboard → label grafik "Monitor Pesanan Masuk", tabel "Pesanan Terbaru"
    2. Section "Tentang Sistem" menampilkan teks user-friendly tanpa referensi teknis
    3. Halaman Login → heading "Selamat Datang Kembali" centered
    4. Halaman Login di browser desktop (>= 1024px) → tidak perlu scroll untuk lihat seluruh form
    5. Halaman Mitra → buka modal Detail Info Mitra di desktop → modal tampil di center, tidak overflow ke luar layar
    6. `npm run lint` tanpa error TypeScript
  </verify>
  <done>AC-4, AC-5, AC-6, AC-7, AC-8 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Feature — Admin dapat edit harga satuan item di mode edit pesanan</name>
  <files>src/pages/orders/CreateOrder.tsx</files>
  <action>
    Di CreateOrder.tsx, dalam item row (line ~343-366), setelah qty input dan sebelum tombol hapus, tambahkan input harga satuan yang HANYA tampil untuk admin/staff DAN dalam mode edit (ketika `id` ada / `existingOrder` ada):

    ```tsx
    {(user.role === 'admin' || user.role === 'staff') && id && (
      <div className="flex items-center gap-2">
        <label className="text-[10px] font-bold text-slate-400 uppercase tracking-wider whitespace-nowrap">Harga/Unit</label>
        <input
          type="number"
          min="0"
          step="500"
          value={item.priceSnapshot || ''}
          onChange={e => handleUpdateItem(item.id, 'priceSnapshot', Math.max(0, parseInt(e.target.value) || 0))}
          className="w-28 px-3 py-1.5 border border-slate-200 rounded-xl outline-none focus:ring-4 focus:ring-blue-500/15 focus:border-blue-500 font-bold text-xs text-slate-800 bg-white [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none"
          placeholder="Harga"
        />
      </div>
    )}
    ```

    Letakkan di dalam div yang sama dengan qty input (line ~344) atau di row terpisah di bawahnya — pilih yang paling rapi secara visual. Disarankan di row yang sama dengan qty (flex-wrap agar tidak overflow mobile).

    Pastikan `handleUpdateItem(item.id, 'priceSnapshot', val)` sudah handle tipe number dengan benar — sudah ada `[field]: val` generic, jadi langsung berfungsi.

    Tidak perlu perubahan API/schema — `priceSnapshot` sudah ada di OrderItem type dan disimpan saat save.
  </action>
  <verify>
    1. Login sebagai admin → buka edit pesanan apapun
    2. Setiap item menampilkan input "Harga/Unit" di samping qty
    3. Mengubah nilai harga → Subtotal langsung update (reaktif via formatCurrency(item.qty * item.priceSnapshot))
    4. Login sebagai mitra → buka edit draft pesanan → input harga satuan tidak tampil
    5. Save pesanan → harga tersimpan ke API (priceSnapshot di payload)
    6. `npm run lint` tanpa error TypeScript
  </verify>
  <done>AC-9 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/db/schema.ts` — schema database tidak berubah
- `src/api/routes/` — tidak ada perubahan API
- `src/lib/api.ts` — tidak ada perubahan API client
- `src/types.ts` — tidak ada perubahan type (priceSnapshot sudah ada di OrderItem)
- PWA config, vite config, tailwind config

## SCOPE LIMITS
- Task ini hanya mengubah presentasi dan logika UI — tidak ada migrasi data
- Harga satuan editable hanya di mode edit (bukan create baru)
- Mitra modal hanya diubah posisinya pada desktop — tidak redesign kontennya
- Login page desktop layout — hanya penyesuaian height/padding, tidak redesign

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` — TypeScript type check pass tanpa error
- [ ] Buka halaman sebagai mitra: detail pesanan draft hanya punya 1 tombol pengajuan
- [ ] Buka modal Ajukan Draft → judul dan pesan sesuai AC-2
- [ ] Di /orders/drafts — sidebar hanya Draft Pesanan yang aktif
- [ ] Di /orders — sidebar hanya Daftar Pesanan yang aktif
- [ ] Dashboard labels: "Monitor Pesanan Masuk" dan "Pesanan Terbaru"
- [ ] Tentang Sistem: teks user-friendly tanpa referensi teknis
- [ ] Login: heading centered, desktop fit viewport tanpa scroll
- [ ] Mitra modal: centered di desktop, tidak overflow
- [ ] Admin edit pesanan: input harga satuan tampil dan reaktif
- [ ] Mitra edit pesanan: input harga satuan tidak tampil
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Semua 9 item dari permintaan user teraddress
- Tidak ada regresi di feature lain (orders, finance, auth)
- TypeScript clean (npm run lint tanpa error baru)
- Tidak ada perubahan API, schema, atau type definitions
</success_criteria>

<output>
After completion, create `.paul/phases/13-ui-bug-fixes/13-01-SUMMARY.md`
</output>
