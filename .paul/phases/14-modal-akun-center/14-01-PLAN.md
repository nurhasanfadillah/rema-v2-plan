---
phase: 14-modal-akun-center
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Ubah modal Detail Info Akun dari slide-in panel kanan menjadi centered overlay modal dengan animasi fade+scale.
</objective>

<context>
@.paul/PROJECT.md
@src/pages/Users.tsx
</context>

<acceptance_criteria>
## AC-1: Modal Centered
Given user membuka Detail Info Akun dari halaman Users
When modal muncul
Then modal tampil di tengah layar (horizontal + vertical center), bukan dari kanan
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Ubah layout dan animasi UserDetailPanel menjadi centered modal</name>
  <files>src/pages/Users.tsx</files>
  <action>
    Di komponen UserDetailPanel (baris 354–360), ubah:

    1. Overlay div: ganti `flex justify-end` → `flex items-center justify-center p-4`
    2. Motion div:
       - Hapus animasi x (slide): `initial={{ x: '100dvw' }}`, `animate={{ x: 0 }}`, `exit={{ x: '100dvw' }}`
       - Ganti dengan fade+scale: `initial={{ opacity: 0, scale: 0.95 }}`, `animate={{ opacity: 1, scale: 1 }}`, `exit={{ opacity: 0, scale: 0.95 }}`
       - Transition: `transition={{ type: 'tween', duration: 0.18 }}`
       - Ganti class `h-full` → `max-h-[90vh]`, tambah `rounded-2xl overflow-hidden`
       - Pertahankan `w-full max-w-md shadow-2xl flex flex-col`

    Hasil class motion div:
    `className="bg-white w-full max-w-md max-h-[90vh] rounded-2xl shadow-2xl flex flex-col overflow-hidden"`
  </action>
  <verify>Buka halaman Users → klik salah satu user → modal muncul di tengah layar, bukan slide dari kanan</verify>
  <done>AC-1 satisfied: modal terlihat di center overlay, bukan panel kanan</done>
</task>
</tasks>

<output>
After completion, create `.paul/phases/14-modal-akun-center/14-01-SUMMARY.md`
</output>
