---
phase: 11-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RunningOrders.tsx
  - src/pages/Dashboard.tsx
autonomous: true
---

<objective>
## Goal
Perbaiki 3 item UI: (1) RunningOrders hanya tampilkan order confirmed, (2) grafik dashboard exclude status draft/waiting_confirmation/cancelled/returned, (3) tambah tabel ringkas 10 order confirmed terakhir di dashboard.

## Purpose
Data yang ditampilkan di ticker dan grafik saat ini menyertakan order yang belum valid (draft, waiting) dan yang sudah selesai negatif (cancelled, returned), sehingga angka tidak merepresentasikan aktivitas produksi aktual.

## Output
- `src/components/RunningOrders.tsx` — filter confirmed only
- `src/pages/Dashboard.tsx` — filter chart + tambah tabel ringkas
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@src/components/RunningOrders.tsx
@src/pages/Dashboard.tsx
@src/types.ts
</context>

<acceptance_criteria>

## AC-1: RunningOrders hanya tampilkan status confirmed
```gherkin
Given ticker RunningOrders sedang berjalan
When data orders di-load dari API
Then hanya order dengan status 'confirmed' yang muncul di ticker (draft/waiting/processing/shipped/dll tidak muncul)
```

## AC-2: Grafik dashboard hanya hitungan order aktif
```gherkin
Given halaman dashboard terbuka
When chart "Grafik Pesanan Masuk" di-render
Then order dengan status draft, waiting_confirmation, cancelled, returned tidak dihitung dalam grafik
```

## AC-3: Tabel ringkas 10 order confirmed terakhir di dashboard
```gherkin
Given halaman dashboard terbuka (role admin)
When data orders sudah di-load
Then muncul section baru berisi tabel dengan kolom Nama Mitra | Qty | Jenis (Online/Offline), menampilkan maksimal 10 order confirmed terakhir, diurutkan by createdAt descending
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Filter RunningOrders hanya tampilkan status confirmed</name>
  <files>src/components/RunningOrders.tsx</files>
  <action>
    Di dalam `useMemo` yang menghasilkan `activeOrdersString`, tambahkan filter sebelum sort:
    ```
    const confirmed = orders.filter(o => o.status === 'confirmed');
    const latest = [...confirmed].sort(...).slice(0, 10);
    ```
    Label ticker tetap "Terbaru" — tidak perlu diganti.
    Jika tidak ada order confirmed, teks fallback: "Belum ada pesanan terkonfirmasi..."
  </action>
  <verify>Buka app, pastikan ticker hanya menampilkan order dengan status confirmed (cek di DB/API jika perlu)</verify>
  <done>AC-1 satisfied: ticker hanya order status confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Filter chartData dashboard — exclude draft/waiting_confirmation/cancelled/returned</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    Di dalam `useMemo` yang menghasilkan `chartData`, tambahkan filter pada `targetOrders` sebelum dipakai:
    ```
    const EXCLUDED_STATUSES = ['draft', 'waiting_confirmation', 'cancelled', 'returned'];
    const chartOrders = targetOrders.filter(o => !EXCLUDED_STATUSES.includes(o.status));
    ```
    Ganti semua referensi `targetOrders.forEach` di dalam useMemo menjadi `chartOrders.forEach`.
    Konstanta EXCLUDED_STATUSES didefinisikan di dalam useMemo (tidak perlu di luar).
  </action>
  <verify>Buka dashboard, filter ke Minggu Ini/Bulan Ini/Tahun Ini — pastikan order draft tidak terhitung di grafik</verify>
  <done>AC-2 satisfied: grafik exclude status yang tidak relevan</done>
</task>

<task type="auto">
  <name>Task 3: Tambah tabel ringkas 10 order confirmed terakhir di dashboard (admin only)</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    Tambah `useMemo` baru di bawah `chartData`:
    ```
    const recentConfirmedOrders = useMemo(() => {
      return [...orders]
        .filter(o => o.status === 'confirmed')
        .sort((a, b) => b.createdAt - a.createdAt)
        .slice(0, 10);
    }, [orders]);
    ```

    Tambah section baru di JSX, setelah "Orders Chart Section" dan sebelum "Main Info Box", hanya tampil jika `user.role !== 'mitra'`:
    ```jsx
    {user.role !== 'mitra' && (
      <motion.div variants={itemVariants} className="card">
        <div className="flex items-center gap-2.5 mb-5">
          <div className="w-1 h-5 bg-green-600 rounded-full" />
          <h2 className="section-title">10 Order Dikonfirmasi Terakhir</h2>
        </div>
        <div className="overflow-x-auto">
          <table className="w-full text-sm">
            <thead>
              <tr className="border-b border-slate-100 text-left">
                <th className="pb-3 text-xs font-semibold text-slate-500 uppercase tracking-wider">Nama Mitra</th>
                <th className="pb-3 text-xs font-semibold text-slate-500 uppercase tracking-wider text-center">Qty</th>
                <th className="pb-3 text-xs font-semibold text-slate-500 uppercase tracking-wider text-center">Jenis</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-slate-50">
              {recentConfirmedOrders.length === 0 ? (
                <tr><td colSpan={3} className="py-8 text-center text-slate-400 text-xs">Belum ada order dikonfirmasi</td></tr>
              ) : (
                recentConfirmedOrders.map(order => {
                  const mitra = mitras.find(m => m.id === order.mitraId);
                  return (
                    <tr key={order.id} className="hover:bg-slate-50/50 transition-colors">
                      <td className="py-3 text-slate-700 font-medium text-[13px]">{mitra?.name || 'Unknown'}</td>
                      <td className="py-3 text-slate-600 text-center tabular-nums font-semibold">{order.totalQty}</td>
                      <td className="py-3 text-center">
                        <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-[10px] font-bold uppercase tracking-wide ${order.type === 'online' ? 'bg-blue-50 text-blue-700' : 'bg-orange-50 text-orange-700'}`}>
                          {order.type === 'online' ? 'Online' : 'Offline'}
                        </span>
                      </td>
                    </tr>
                  );
                })
              )}
            </tbody>
          </table>
        </div>
      </motion.div>
    )}
    ```
  </action>
  <verify>Buka dashboard sebagai admin — pastikan tabel muncul dengan 3 kolom, berisi order confirmed terakhir, badge Online/Offline berwarna</verify>
  <done>AC-3 satisfied: tabel ringkas 10 order confirmed terakhir muncul di dashboard</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logika auth, routing, dan API calls di luar 2 file yang ditentukan
- `src/db/schema.ts` dan `src/api/` — tidak ada perubahan backend
- Styling class system (`@layer components`) di `index.css`

## SCOPE LIMITS
- Hanya modifikasi di `RunningOrders.tsx` dan `Dashboard.tsx`
- Tidak menambah dependency baru
- Tabel summary hanya untuk admin/staff — tidak tampil untuk role mitra
- Tidak mengubah logika filter `activeOrders` yang dipakai untuk mitra metrics (baris 28)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` — no TypeScript errors
- [ ] RunningOrders ticker hanya menampilkan order confirmed
- [ ] Grafik dashboard tidak menghitung draft/waiting_confirmation/cancelled/returned
- [ ] Tabel ringkas muncul di dashboard (admin) dengan 3 kolom: Nama Mitra, Qty, Jenis
- [ ] Badge Online/Offline terbaca jelas
- [ ] Tidak ada regresi di fitur dashboard lainnya (metrics mitra, omzet, pending confirmation)
</verification>

<success_criteria>
- Semua 3 acceptance criteria terpenuhi
- TypeScript tidak ada error baru
- UI konsisten dengan design system yang ada (class card, section-title, dll)
</success_criteria>

<output>
After completion, create `.paul/phases/11-ui-fixes/11-01-SUMMARY.md`
</output>
