---
phase: 27-ui-polish-batch-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Layout.tsx
  - src/pages/orders/OrderDetail.tsx
  - src/components/orders/OrderSPKPDF.tsx
  - src/components/MultiFileUpload.tsx
autonomous: true
---

<objective>
## Goal
5 perbaikan UI: hapus kode bell/search tersembunyi di header, hapus badge "Menunggu Pengajuan" di panel AJUKAN, ubah tombol "Ajukan Sekarang" jadi biru, relabel PDF SPK + integrasikan catatan desain ke baris item, dan buat upload thumbnail inline dengan kartu square.

## Purpose
Membersihkan dead code tersembunyi, menyeragamkan visual button, menyederhanakan output PDF SPK, dan meningkatkan UX form pesanan.

## Output
4 file dimodifikasi. Tidak ada file baru.
</objective>

<context>
@.paul/PROJECT.md
@src/components/Layout.tsx
@src/pages/orders/OrderDetail.tsx
@src/components/orders/OrderSPKPDF.tsx
@src/components/MultiFileUpload.tsx
@src/pages/orders/CreateOrder.tsx
</context>

<acceptance_criteria>

## AC-1: Header Bersih
```gherkin
Given pengguna membuka halaman mana pun
When topbar dirender
Then tidak ada elemen bell, search input, atau notif dropdown — baik di DOM maupun di source code Layout.tsx
```

## AC-2: Panel AJUKAN Sekarang — Badge Hilang, Button Biru
```gherkin
Given pesanan berstatus draft dibuka oleh role mitra
When panel "Ajukan Draft Pesanan" muncul
Then badge kecil "Menunggu Pengajuan" (orange inline) tidak ada
And tombol "Ajukan Sekarang" berwarna biru (bg-blue-600 hover:bg-blue-500), bukan oranye
```

## AC-3: PDF SPK — Label dan Deskripsi Item
```gherkin
Given admin mengklik "Cetak SPK" pada detail pesanan
When PDF dirender
Then kolom yang sebelumnya berlabel "Logistik" sekarang berlabel "Jenis Pesanan"
And setiap baris DESKRIPSI ITEM memiliki baris ke-2:
    - Jika item polos: teks "Polos"
    - Jika item custom: catatan desain (item.designNotes), atau "Custom Logo" jika kosong
And section "Global Design Notes" terpisah (kotak catatan di bawah tabel) sudah dihapus
```

## AC-4: Upload Thumbnail Inline dan Square
```gherkin
Given pengguna membuka form tambah/edit pesanan dengan item Custom Logo
When section upload "Unggah Visual Preview" atau "File Master Desain" dirender
Then kotak upload (dropzone) berada di baris yang sama dengan kartu thumbnail yang sudah ada
And setiap kartu thumbnail berbentuk square (rasio 1:1), bukan rectangle
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Hapus bell/search dari Layout.tsx + bersihkan OrderDetail</name>
  <files>src/components/Layout.tsx, src/pages/orders/OrderDetail.tsx</files>
  <action>
    **Layout.tsx:**
    1. Hapus seluruh block `{/* Search — hidden */}` (div className="hidden" yang berisi AnimatePresence + search input + search button).
    2. Hapus seluruh block `{/* Notification — hidden */}` (div className="hidden relative" yang berisi bell button + dropdown notif).
    3. Hapus state yang tidak lagi digunakan: `searchOpen`, `setSearchOpen`, `searchQuery`, `setSearchQuery`, `notifOpen`, `setNotifOpen`.
    4. Hapus import `Search` dan `Bell` dari lucide-react jika tidak digunakan di tempat lain.
    Jangan sentuh elemen header lainnya (logo, sidebar toggle, user avatar button).

    **OrderDetail.tsx:**
    5. Di dalam block `order.status === 'draft' && user.role === 'mitra'`, hapus `<div className="inline-flex ... bg-orange-500/10 ... text-orange-400 ..."><ShieldAlert ... /> Menunggu Pengajuan</div>` (badge kecil di atas teks "Ajukan Draft Pesanan"). Pertahankan card, teks deskripsi, dan tombolnya.
    6. Ubah tombol "Ajukan Sekarang": ganti `bg-orange-600 hover:bg-orange-500 shadow-orange-600/20` → `bg-blue-600 hover:bg-blue-500 shadow-blue-600/20`. Juga update warna border/glow card pembungkus dari `border-orange-500/30` → `border-blue-500/30` dan glow div dari `bg-orange-500/10` → `bg-blue-500/10`.
    7. Hapus import `Tag` dari lucide-react jika tidak digunakan di tempat lain (cek seluruh file).
    Jangan ubah logika `handleUpdateStatus`, validasi tagihan, atau bagian lain OrderDetail.
  </action>
  <verify>
    - `npm run lint` lulus tanpa error TypeScript
    - Grep `Bell|Search|notifOpen|searchOpen|searchQuery` di Layout.tsx → tidak ada match
    - Grep `Tag` di OrderDetail.tsx → hanya ada di import jika masih dipakai, atau tidak ada sama sekali
    - Grep `bg-orange-600` di OrderDetail.tsx → tidak ada di tombol Ajukan
  </verify>
  <done>AC-1 dan AC-2 terpenuhi</done>
</task>

<task type="auto">
  <name>Task 2: Relabel PDF SPK dan integrasikan catatan desain ke baris item</name>
  <files>src/components/orders/OrderSPKPDF.tsx</files>
  <action>
    1. Di section Info grid, ubah `<Text style={styles.label}>Logistik</Text>` → `<Text style={styles.label}>Jenis Pesanan</Text>`.
    2. Di dalam `order.items.map(...)` pada Manifest Produksi, di `<View style={styles.colDesc}>`, tambahkan baris ke-2 setelah baris nama produk:
       ```
       {item.isCustomLogo
         ? <Text style={{ fontSize: 6, color: '#475569', marginTop: 2 }}>{item.designNotes || 'Custom Logo'}</Text>
         : <Text style={{ fontSize: 6, color: '#94a3b8', marginTop: 2 }}>Polos</Text>
       }
       ```
    3. Hapus seluruh block `{/* Global Design Notes ... */}` (View dengan kondisi `order.items.some(i => i.designNotes)`) beserta styles `noteBox`, `noteLabel`, `noteText` dari objek `styles` jika tidak digunakan di tempat lain.
    Jangan ubah kolom QTY, CETAK, summary total, atau styling lainnya.
  </action>
  <verify>
    - `npm run lint` lulus
    - Buka halaman detail pesanan di dev server, klik "Cetak SPK" → cek PDF preview: kolom menampilkan "Jenis Pesanan", setiap item punya baris kedua (Polos atau catatan desain), tidak ada section notes terpisah di bawah tabel.
  </verify>
  <done>AC-3 terpenuhi</done>
</task>

<task type="auto">
  <name>Task 3: Upload thumbnail inline dan kartu square di MultiFileUpload</name>
  <files>src/components/MultiFileUpload.tsx</files>
  <action>
    Ubah layout MultiFileUpload agar upload dropzone berada dalam grid yang sama dengan thumbnail — bukan di bawahnya:

    1. Gabungkan thumbnail grid dan dropzone ke dalam satu container `<div className="grid grid-cols-3 sm:grid-cols-4 gap-3">` (atau sesuaikan column count).
    2. Setiap kartu thumbnail: hapus `h-24`, tambahkan `aspect-square` agar square. Pertahankan `w-full` dan `object-cover`.
    3. Upload dropzone dijadikan grid item terakhir dengan ukuran yang sama (aspect-square, `min-h-0`). Sederhanakan konten dropzone: cukup ikon Upload + teks pendek "Tambah" agar muat dalam kotak square kecil.
    4. Kondisi: jika `values.length === 0` tampilkan dropzone saja (full-width atau dalam grid). Jika `values.length > 0` tampilkan thumbnail cards + dropzone sebagai item terakhir.
    5. Jaga fungsionalitas drag-drop, click to upload, remove button, dan loading spinner tetap berjalan.
    Jangan ubah logika `processFiles`, `handleRemove`, atau API call.
  </action>
  <verify>
    - `npm run lint` lulus
    - Buka form tambah pesanan, tambah item Custom Logo, cek section "Unggah Visual Preview":
      - Saat belum ada upload: dropzone muncul normal
      - Setelah upload 1-2 gambar: thumbnail muncul square, dropzone ada di samping kanan thumbnail (sama baris)
      - Remove button pada thumbnail masih berfungsi
    - Hal yang sama berlaku untuk "File Master Desain"
  </verify>
  <done>AC-4 terpenuhi</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/pages/orders/OrderDetail.tsx — logika `handleUpdateStatus`, validasi credit limit, getStatusBadge function
- src/components/orders/OrderSPKPDF.tsx — kolom QTY, CETAK (DTF badge), summary total quantity, header PDF
- src/components/MultiFileUpload.tsx — `processFiles`, `handleRemove`, API call, validasi ukuran file
- src/components/Layout.tsx — sidebar, nav links, user avatar, header container, main content area

## SCOPE LIMITS
- Tidak ada perubahan backend/API
- Tidak ada perubahan di CreateOrder.tsx (MultiFileUpload sudah digunakan via props, tidak perlu ubah pemanggil)
- Tidak ada perubahan logika bisnis, hanya perubahan visual/layout
- Tidak menambah dependencies baru

</boundaries>

<verification>
- [ ] `npm run lint` lulus tanpa error TypeScript baru
- [ ] Buka http://localhost:3000 — header tidak menampilkan bell/search di DOM
- [ ] Buka detail pesanan status draft sebagai mitra — card AJUKAN biru, badge orange hilang
- [ ] Klik Cetak SPK pada pesanan — PDF menampilkan "Jenis Pesanan", baris deskripsi item ada "Polos"/catatan desain, tidak ada notes section terpisah
- [ ] Buka form pesanan, tambah item Custom Logo — thumbnail preview square, dropzone dalam baris yang sama
- [ ] Semua acceptance criteria terpenuhi
</verification>

<success_criteria>
- Semua 3 task selesai
- `npm run lint` lulus
- Semua 4 AC terverifikasi visual
- Tidak ada regresi pada fitur lain
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/27-ui-polish-batch-3/27-01-SUMMARY.md`
</output>
