---
phase: 21-cancellations-returns-polish
plan: 01
type: execute
wave: 1
depends_on: ["20-01"]
files_modified:
  - src/pages/CancellationsReturns.tsx
autonomous: true
---

<objective>
## Goal
Polish halaman Pembatalan & Retur: selaraskan header ke utility class system, lightkan main list container dari dark-slate ke white/card system, tambah colored left-border pada mobile cards by status, dan sederhanakan typography section header.

## Purpose
Halaman ini satu-satunya yang masih pakai custom `h1` raw dan main list container `bg-slate-950` — tidak konsisten dengan halaman lain (Finance, dll) yang sudah menggunakan `page-header`, `page-title`, dan white card system. Form area dark (danger context) tetap dipertahankan karena intentional.

## Output
- `src/pages/CancellationsReturns.tsx` — 3 area perubahan:
  1. Header section → page-header + page-title + subtitle
  2. Main list container + mobile cards → white/light + colored left-border
  3. Desktop table → thead/tbody lighter, typography simplified
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## CSS Utilities (sudah dikonfirmasi)
- `.page-header` → `flex items-center justify-between gap-4 flex-wrap`
- `.page-title` → `text-xl sm:text-2xl font-bold tracking-tight text-white font-display`

## Source Files
@src/pages/CancellationsReturns.tsx
</context>

<acceptance_criteria>

## AC-1: Header menggunakan utility class system
```gherkin
Given halaman Pembatalan & Retur terbuka
When user melihat bagian atas halaman
Then header menggunakan class page-header dan h1 menggunakan page-title
  Dan ada subtitle "Kelola pembatalan dan retur pesanan mitra."
  Dan AlertTriangle icon tetap ada di sebelah judul
```

## AC-2: Main list container — white/light
```gherkin
Given halaman Pembatalan & Retur terbuka
When user melihat tabel Riwayat Aktivitas
Then container memiliki background putih/light (bg-white, border-slate-200/60)
  bukan bg-slate-950
  Dan header row container menggunakan bg-slate-50/80 bukan bg-slate-900/30
```

## AC-3: Mobile cards — colored left-border by status
```gherkin
Given halaman terbuka di viewport mobile/tablet (< lg)
When user melihat daftar riwayat card
Then card dengan status "cancelled" memiliki border-l-4 border-l-red-400
  Dan card dengan status "returned" memiliki border-l-4 border-l-purple-400
  Dan warna teks card menyesuaikan background terang
```

## AC-4: Desktop table — lighter style
```gherkin
Given halaman terbuka di viewport desktop (>= lg)
When user melihat tabel riwayat
Then thead menggunakan bg-slate-50/80 dan text-slate-900 (bukan slate-800/30 + slate-500)
  Dan tbody rows menggunakan hover:bg-slate-50/70 (bukan hover:bg-slate-800/20)
  Dan divider antar row menggunakan divide-slate-100 (bukan divide-slate-800)
```

## AC-5: Type check bersih
```gherkin
Given semua perubahan diterapkan
When `npm run lint` dijalankan
Then tidak ada TypeScript error
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>T1 — Header section: page-header + page-title + subtitle</name>
  <files>src/pages/CancellationsReturns.tsx</files>
  <action>
    Lokasi: baris 174-203 (div wrapper header sampai closing div CTA buttons)

    1. Ganti outer div wrapper:
       Dari: `className="flex flex-col sm:flex-row sm:items-center justify-between gap-4 py-3 border-b border-white/10 mt-[-8px]"`
       Menjadi: `className="page-header"`

    2. Ganti h1:
       Dari: `className="text-xl font-black tracking-tight text-white flex items-center gap-2"`
       Menjadi: `className="page-title flex items-center gap-2"`

    3. Ganti subtitle p tag:
       Dari: `className="text-[11px] text-slate-400 font-medium tracking-wide"`
         teks: `"Proses pembatalan dan retur pesanan mitra."`
       Menjadi: `className="text-[12px] text-slate-500 mt-0.5 font-semibold"`
         teks: `"Kelola pembatalan dan retur pesanan mitra."`

    Jangan ubah CTA buttons (pill container dengan Pembatalan + Retur) — sudah visual dan berfungsi baik.
    Jangan ubah form area (AnimatePresence / motion.div dark card) — intentional dark.
  </action>
  <verify>Baca baris 174-185 — pastikan page-header, page-title, subtitle baru applied</verify>
  <done>AC-1 satisfied: header menggunakan utility class system</done>
</task>

<task type="auto">
  <name>T2 — Main list container + mobile cards</name>
  <files>src/pages/CancellationsReturns.tsx</files>
  <action>
    Lokasi: baris 319-379

    **A. Container utama (baris ~319):**
    Dari: `className="bg-slate-950 rounded-2xl border border-slate-900 overflow-hidden shadow-2xl"`
    Menjadi: `className="bg-white rounded-2xl border border-slate-200/60 overflow-hidden shadow-sm"`

    **B. Header row dalam container (baris ~320):**
    Dari: `className="px-5 py-4 border-b border-slate-900 bg-slate-900/30 flex items-center justify-between"`
    Menjadi: `className="px-5 py-4 border-b border-slate-100 bg-slate-50/80 flex items-center justify-between"`

    **C. "Riwayat Aktivitas" label (baris ~325):**
    Dari: `className="text-[11px] font-black text-slate-200 uppercase tracking-[0.15em]"`
    Menjadi: `className="text-[13px] font-extrabold text-slate-800"`

    **D. Record counter badge (baris ~327-329):**
    Dari: `className="flex items-center gap-1.5 px-2 py-0.5 bg-slate-900 rounded-full border border-slate-800"`
    Menjadi: `className="flex items-center gap-1.5 px-2 py-0.5 bg-slate-100 rounded-full border border-slate-200"`
    Dan inner span: `className="text-[9px] font-mono text-slate-400 font-bold tracking-tighter uppercase"` → `className="text-[9px] font-bold text-slate-500"`

    **E. Empty state (baris ~335):**
    Dari: `className="w-12 h-12 bg-slate-800 rounded-full ..."`
    Menjadi: `className="w-12 h-12 bg-slate-100 rounded-full ..."`
    Dan h3: `className="text-sm font-bold text-slate-400"` → `className="text-sm font-bold text-slate-600"`
    Dan p: `className="text-[11px] text-slate-600 mt-1"` → tetap atau `className="text-[11px] text-slate-400 mt-1"`

    **F. Mobile card wrapper (baris ~353):**
    Dari: `className="bg-slate-800/40 border border-slate-700/50 rounded-xl p-4 space-y-3 hover:border-slate-600 transition-colors cursor-pointer group"`
    Menjadi (conditional berdasarkan `o.status`):
    ```
    className={`bg-white border border-slate-200/60 rounded-xl p-4 space-y-3 hover:border-blue-300 transition-colors cursor-pointer group border-l-4 ${o.status === 'cancelled' ? 'border-l-red-400' : 'border-l-purple-400'}`}
    ```

    **G. Mobile card text colors:**
    - Order number `text-white` → `text-slate-900`
    - Label `text-slate-500` → tetap (sudah cocok untuk light bg)
    - Value `text-slate-200 font-bold` → `text-slate-800 font-bold`
    - Nominal `text-emerald-400` → `text-emerald-600`
    - Alasan italic `text-slate-400 italic ... border-t border-slate-700 pt-2` → `text-slate-500 italic ... border-t border-slate-100 pt-2`
  </action>
  <verify>Baca baris 319-379 — container white, header row slate-50, mobile cards border-l-4 conditional, text colors light-bg</verify>
  <done>AC-2 + AC-3 satisfied: container putih, mobile cards colored left-border</done>
</task>

<task type="auto">
  <name>T3 — Desktop table: lighter style</name>
  <files>src/pages/CancellationsReturns.tsx</files>
  <action>
    Lokasi: baris 382-437

    **A. Thead row (baris ~385):**
    Dari: `className="bg-slate-800/30 text-[10px] uppercase tracking-widest text-slate-500"`
    Menjadi: `className="bg-slate-50/80 border-b border-slate-200/80 text-slate-900 font-extrabold text-[10px] uppercase tracking-wider"`

    **B. Tbody (baris ~395):**
    Dari: `className="divide-y divide-slate-800"`
    Menjadi: `className="divide-y divide-slate-100 font-semibold text-slate-700"`

    **C. Row hover (baris ~403):**
    Dari: `className="hover:bg-slate-800/20 cursor-pointer transition-colors group"`
    Menjadi: `className="hover:bg-slate-50/70 cursor-pointer transition-colors duration-200 group"`

    **D. Order number cell (baris ~405-406):**
    Dari: `className="px-4 py-2.5 font-mono text-xs font-black text-slate-200"`
      inner span: `className="px-1.5 py-0.5 rounded bg-slate-800 text-slate-300 mr-2 group-hover:bg-slate-700"`
    Menjadi: `className="px-4 py-2.5 font-mono text-xs font-black text-slate-800"`
      inner span: `className="px-1.5 py-0.5 rounded bg-slate-100 text-slate-700 mr-2 group-hover:bg-slate-200"`

    **E. Partner name cell (baris ~417-419):**
    Dari: `className="px-4 py-2.5 text-[11px] font-extrabold text-slate-300"`
    Menjadi: `className="px-4 py-2.5 text-[11px] font-extrabold text-slate-900"`

    Jangan ubah kolom Tipe (sky/amber badge), Waktu, Alasan, Nominal, Status — sudah oke.
  </action>
  <verify>Baca baris 382-437 — thead bg-slate-50, tbody divide-slate-100, row hover bg-slate-50, teks dark-on-light</verify>
  <done>AC-4 satisfied: desktop table lighter style</done>
</task>

<task type="auto">
  <name>T4 — Type check</name>
  <files>-</files>
  <action>Jalankan `npm run lint`</action>
  <verify>Exit code 0, tidak ada TypeScript error</verify>
  <done>AC-5 satisfied: lint clean</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Form area (AnimatePresence dark card baris 206-316) — intentional dark untuk danger context
- CTA buttons container (pill dengan Pembatalan + Retur) — sudah bagus
- Logic: handleSubmit, handleOpenForm, getEligibleOrders — tidak ada perubahan behavior
- getStatusBadge() function — badge styling sudah tepat
- Desktop kolom: Tipe badge (sky/amber), Waktu, Alasan, Nominal, Status badge

## SCOPE LIMITS
- Tidak ada perubahan data/API
- Tidak ada fitur baru
- Tidak ada pagination (halaman ini tidak punya pagination — tidak perlu ditambahkan)

</boundaries>

<verification>
- [ ] `npm run lint` — TypeScript clean
- [ ] Header: page-header class, page-title class, subtitle baru
- [ ] Main container: bg-white, bukan bg-slate-950
- [ ] Mobile cards: border-l-4 merah (cancelled) / ungu (returned)
- [ ] Desktop table: thead putih, divider slate-100, row hover putih
- [ ] Form area dark: TIDAK berubah
</verification>

<success_criteria>
- Semua 4 tasks selesai
- Semua 5 AC pass
- Lint clean
- Form area dark tetap utuh
- Tidak ada regresi visual/fungsional
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/21-cancellations-returns-polish/21-01-SUMMARY.md`
</output>
