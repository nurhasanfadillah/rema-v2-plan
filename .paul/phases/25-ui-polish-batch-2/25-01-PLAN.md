---
phase: 25-ui-polish-batch-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/OrderPriorities.tsx
  - src/components/Layout.tsx
  - src/pages/orders/OrderDetail.tsx
  - src/pages/Finance.tsx
  - src/pages/CancellationsReturns.tsx
autonomous: true
---

<objective>
## Goal
Polish UI di 5 titik: Priority page mobile button alignment, sembunyikan icon lonceng+cari di header, OrderDetail badge tagih + label + button colors, Finance modal no-scrollbar, CancellationsReturns button di luar card.

## Purpose
Konsistensi visual dan UX yang lebih clean — menghapus elemen yang tidak perlu, memperkuat visual hierarchy button, dan menyamakan pola UI di seluruh halaman.

## Output
5 file dimodifikasi — semua perubahan UI-only, tanpa perubahan logic atau API.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@src/pages/OrderPriorities.tsx
@src/components/Layout.tsx
@src/pages/orders/OrderDetail.tsx
@src/pages/Finance.tsx
@src/pages/CancellationsReturns.tsx
</context>

<acceptance_criteria>

## AC-1: Priority Page — Mobile Button Kanan
```gherkin
Given halaman Prioritas Pesanan di viewport mobile
When user membuka halaman /priority
Then button "Tambah Prioritas" muncul di sisi kanan atas header (sejajar dengan judul, tidak wrap ke bawah)
```

## AC-2: Header — Bell dan Search Tersembunyi
```gherkin
Given topbar Layout di semua halaman
When user membuka halaman manapun
Then icon lonceng (Bell) dan icon pencarian (Search) tidak terlihat — hidden, bukan dihapus dari DOM
```

## AC-3: OrderDetail — Badge Tagih Dihapus
```gherkin
Given halaman Detail Pesanan
When user membuka pesanan manapun
Then tidak ada badge "Tertagih" / "Belum Tagih" di header maupun row "Status Tagihan" di card Rincian Tagihan
```

## AC-4: OrderDetail — Label dan Button Colors
```gherkin
Given halaman Detail Pesanan
When user melihat card informasi dan panel Aksi & Kontrol
Then:
  - Label "Metode Pengiriman" → "Jenis Pesanan"
  - Label "Basis Harga" di item → "Harga Satuan"
  - Button "Ajukan Sekarang" background orange (bg-orange-600)
  - Button "Sudah Cetak" background ungu (bg-purple-600) saat aktif
  - Button "Hapus" background merah solid (bg-red-600) dengan teks putih
  - Button "Cetak SPK" punya warna accent emerald
  - Button "Resi Offline" punya warna accent blue
  - Button "Edit Pesanan" punya warna accent amber
  - Button "Koreksi Status" tetap amber text
```

## AC-5: Finance — Modal Clean No-Scrollbar
```gherkin
Given modal detail transaksi, modal edit, dan modal tambah pembayaran di halaman Finance
When user membuka salah satu modal tersebut
Then scrollbar browser tidak terlihat (class no-scrollbar), tapi konten masih bisa discroll jika overflow
```

## AC-6: CancellationsReturns — Button Bebas Card
```gherkin
Given halaman Pembatalan & Retur
When user melihat header halaman
Then button "Pembatalan" dan "Retur" tampil langsung di header tanpa wrapper card/container berlatar slate-900, menggunakan style btn-primary / standalone button yang konsisten dengan halaman lain
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: OrderPriorities mobile button + Layout hide bell/search</name>
  <files>src/pages/OrderPriorities.tsx, src/components/Layout.tsx</files>
  <action>
    **OrderPriorities.tsx:**
    Di `page-header` div, `page-header` sudah `flex items-center justify-between gap-4 flex-wrap`.
    Problem: pada mobile (flex-wrap), jika konten kiri terlalu lebar, button wrap ke baris baru dan mulai dari kiri.
    Fix: bungkus seluruh isi header dalam `flex items-center justify-between w-full gap-3` — pastikan button selalu di kanan.
    Struktur saat ini:
    ```
    <div className="page-header">
      <div className="flex items-center gap-3"> ... </div>  ← kiri
      {isMitra && <button ...>Tambah Prioritas</button>}    ← kanan
    </div>
    ```
    Ubah `page-header` wrapper menjadi `flex items-center justify-between gap-3 mb-6` (ganti page-header dengan class inline agar tidak bergantung pada flex-wrap). Atau, tambahkan `flex-shrink-0` pada button dan pastikan div kiri punya `min-w-0`.

    **Layout.tsx:**
    Tambahkan `hidden` pada wrapper button Search dan wrapper button/div Bell agar tidak terlihat.
    Cari section Search (sekitar baris 242-289) dan Bell (sekitar baris 285-305).
    - Pada button search (key="search-btn") dan input search (key="search-input"): wrap keduanya dalam kondisi `hidden` atau tambahkan class `hidden` pada parent container search.
    - Pada button Bell dan dropdown notifnya: tambahkan `hidden` pada wrapper div Bell.
    Jangan hapus state searchOpen/searchQuery — hanya sembunyikan UI-nya.
  </action>
  <verify>Buka /priority di mobile viewport (375px). Button "Tambah Prioritas" harus ada di kanan judul. Di semua halaman, topbar tidak menampilkan icon lonceng maupun search.</verify>
  <done>AC-1 dan AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: OrderDetail — hapus badge tagih, ubah label, warnai buttons</name>
  <files>src/pages/orders/OrderDetail.tsx</files>
  <action>
    **1. Hapus badge "Tertagih/Belum Tagih" di header (sekitar baris 322–335):**
    Hapus seluruh block `{(user.role === 'admin' || user.role === 'mitra') && (order.isBilled ? ... : ...)}` di flex header badges.

    **2. Hapus row "Status Tagihan" di card Rincian Tagihan (sekitar baris 392–403):**
    Hapus seluruh `<div className="flex justify-between items-center px-1">` yang berisi label "Status Tagihan:" dan badge isBilled/belum ditagih.

    **3. Ubah label "Metode Pengiriman" → "Jenis Pesanan" (baris ~350):**
    `<span ...>Metode Pengiriman</span>` → `<span ...>Jenis Pesanan</span>`

    **4. Ubah label "Basis Harga" → "Harga Satuan" (baris ~448):**
    `Basis Harga: {formatCurrency(...)}` → `Harga Satuan: {formatCurrency(...)}`

    **5. Button "Ajukan Sekarang" → orange (baris ~541–546):**
    Ganti class dari `bg-blue-600 ... hover:bg-blue-500 ... shadow-blue-600/20` menjadi `bg-orange-600 hover:bg-orange-500 shadow-xl shadow-orange-600/20 text-white font-bold`.

    **6. Button "Sudah Cetak" → purple saat aktif (baris ~507–511):**
    Di kondisi `status === 'sudah_cetak'` yang aktif, ganti `bg-emerald-600 border-emerald-500 text-white shadow-emerald-600/20` → `bg-purple-600 border-purple-500 text-white shadow-purple-600/20`.

    **7. Button "Hapus" → red solid bg + white text (baris ~600–605):**
    Ganti className dari `text-red-500/70 hover:text-red-400 hover:bg-red-500/10 border-red-900/40 hover:border-red-500/30 bg-slate-900/80`
    menjadi: `bg-red-600 hover:bg-red-500 text-white border-red-600 hover:border-red-500 shadow-lg shadow-red-600/20 active:scale-95`.
    Pertahankan padding/rounding/font yang sama (flex-1 sm:flex-none dll).

    **8. Button "Cetak SPK" → emerald accent (baris ~562–573):**
    Ubah dari `text-slate-400 hover:text-emerald-400 hover:bg-emerald-500/10 border-slate-800 hover:border-emerald-500/30 bg-slate-900/80`
    menjadi: `text-emerald-400 bg-emerald-500/10 border-emerald-500/20 hover:bg-emerald-500/20 hover:border-emerald-500/40 bg-slate-900/80`.

    **9. Button "Resi Offline" → blue accent (baris ~575–588):**
    Ubah dari `text-slate-400 hover:text-blue-400 hover:bg-blue-500/10 border-slate-800 hover:border-blue-500/30 bg-slate-900/80`
    menjadi: `text-blue-400 bg-blue-500/10 border-blue-500/20 hover:bg-blue-500/20 hover:border-blue-500/40 bg-slate-900/80`.

    **10. Button "Edit Pesanan" → amber accent (baris ~590–597):**
    Ubah dari `text-slate-400 hover:text-blue-400 hover:bg-blue-500/10 border-slate-800 hover:border-blue-500/30 bg-slate-900/80`
    menjadi: `text-amber-400 bg-amber-500/10 border-amber-500/20 hover:bg-amber-500/20 hover:border-amber-500/40 bg-slate-900/80`.

    **Note:** Jangan ubah logic/kondisi tampil button — hanya className yang berubah.
  </action>
  <verify>Buka /orders/[any-id]. Tidak ada badge status tagih. Label "Jenis Pesanan" dan "Harga Satuan" terlihat. Pada draft+mitra: button Ajukan Sekarang berwarna orange. Button DTF Sudah Cetak berwarna ungu saat aktif. Button Hapus background merah solid teks putih. Cetak SPK, Resi Offline, Edit Pesanan punya accent color masing-masing.</verify>
  <done>AC-3 dan AC-4 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Finance modal no-scrollbar + CancellationsReturns button refactor</name>
  <files>src/pages/Finance.tsx, src/pages/CancellationsReturns.tsx</files>
  <action>
    **Finance.tsx — tambah no-scrollbar pada semua modal:**
    1. PaymentModal div (baris ~748): sudah ada `no-scrollbar` — skip.
    2. EditModal div (baris ~865): `max-h-[90vh] overflow-y-auto` → `max-h-[90vh] overflow-y-auto no-scrollbar`.
    3. TransactionDetailModal div (baris ~920): `max-h-[90vh] overflow-y-auto` → `max-h-[90vh] overflow-y-auto no-scrollbar`.
    4. Main table wrapper (baris ~453): `overflow-x-auto overflow-y-auto max-h-[600px]` → tambah `no-scrollbar`.
    Jangan hapus overflow-y-auto — hanya sembunyikan scrollbar visual.

    **CancellationsReturns.tsx — hapus wrapper card dari buttons:**
    Saat ini buttons Pembatalan & Retur dibungkus dalam:
    ```
    <div className="flex items-center gap-1.5 bg-slate-900/60 p-1.5 rounded-xl border border-slate-800 shadow-inner">
      <button ...Pembatalan...</button>
      <button ...Retur...</button>
    </div>
    ```
    Ubah: hapus wrapper div berlatar slate-900 tersebut.
    Ganti dua button menjadi button standalone dengan style yang konsisten dengan page-header di halaman lain:
    - Button "Pembatalan": `btn-secondary` class + ikon XOctagon + teks merah/danger. Atau inline: `flex items-center gap-2 px-4 py-2 bg-red-500/10 hover:bg-red-500/20 text-red-500 font-bold text-[11px] uppercase tracking-wide rounded-xl border border-red-500/20 transition-all cursor-pointer active:scale-95`.
    - Button "Retur": `flex items-center gap-2 px-4 py-2 bg-purple-500/10 hover:bg-purple-500/20 text-purple-400 font-bold text-[11px] uppercase tracking-wide rounded-xl border border-purple-500/20 transition-all cursor-pointer active:scale-95`.
    Kedua button sejajar langsung di `page-header` dalam `flex items-center gap-2`.
    Pertahankan onClick dan kondisi `user.role !== 'operational'`.
  </action>
  <verify>Di halaman Finance, buka modal detail transaksi dan modal edit — tidak ada scrollbar visible. Di halaman Pembatalan & Retur, button Pembatalan dan Retur terlihat langsung di header tanpa container card berlatar gelap.</verify>
  <done>AC-5 dan AC-6 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logic/kondisi tampil button di OrderDetail.tsx (isDeleteEligible, isEditEligible, canAdvanceNormally, dll)
- isBilled field dan logic di OrderDetail.tsx — hanya hilangkan badge UI-nya, field tetap digunakan untuk logic packing
- API calls, state management, dan business logic di semua file
- src/index.css — tidak perlu perubahan

## SCOPE LIMITS
- Tidak ada perubahan backend/API
- Tidak ada perubahan schema atau types
- Tidak menambah dependency baru
- Layout.tsx: hanya sembunyikan bell+search, jangan hapus state atau logic

</boundaries>

<verification>
- [ ] `npm run lint` — no TypeScript errors
- [ ] `/priority` mobile: button "Tambah Prioritas" di kanan
- [ ] Semua halaman: tidak ada icon Bell dan Search di topbar
- [ ] `/orders/[id]`: tidak ada badge tagih di header atau card rincian tagihan
- [ ] `/orders/[id]`: label "Jenis Pesanan" dan "Harga Satuan" muncul
- [ ] `/orders/[id]`: Ajukan Sekarang=orange, Sudah Cetak=purple, Hapus=red solid, Cetak SPK=emerald, Resi Offline=blue, Edit Pesanan=amber
- [ ] Finance: modal tanpa scrollbar visible, scroll masih berfungsi
- [ ] `/cancellations`: button Pembatalan & Retur tanpa wrapper card gelap
</verification>

<success_criteria>
- Semua task selesai tanpa error TypeScript
- Semua 6 AC satisfied
- Tidak ada regresi pada logic pesanan, keuangan, maupun navigasi
</success_criteria>

<output>
After completion, create `.paul/phases/25-ui-polish-batch-2/25-01-SUMMARY.md`
</output>
