---
phase: 08-ui-consistency
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - src/components/Layout.tsx
  - src/pages/Dashboard.tsx
  - src/pages/Login.tsx
  - src/pages/Users.tsx
autonomous: true
---

<objective>
## Goal
Apply utility classes dari Plan 08-01 ke Layout, Dashboard, Login, dan Users — mengganti hardcoded button styles, heading sizes, card wrappers, dan form label patterns dengan class yang sudah terdefinisi.

## Purpose
Setelah CSS Foundation siap, empat file ini adalah shell + halaman paling sering dilihat. Konsistensi di sini langsung terasa secara visual.

## Output
- Layout.tsx: logout button → `.btn-danger`, nav label → `.label-xs`
- Dashboard.tsx: chart card + info card → `.card`, section headings → `.section-title`
- Login.tsx: form labels → `.label-xs`, submit button → `.btn-primary`
- Users.tsx: page header → `.page-header` + `.page-title`, Add User → `.btn-primary`, form action buttons → `.btn-secondary` / `.btn-ghost`
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/08-ui-consistency/08-01-SUMMARY.md

## Source Files
@src/components/Layout.tsx
@src/pages/Dashboard.tsx
@src/pages/Login.tsx
@src/pages/Users.tsx
</context>

<acceptance_criteria>

## AC-1: Logout button di Layout menggunakan .btn-danger
```gherkin
Given Layout.tsx dirender
When user membuka profile dropdown
Then tombol "Keluar Sesi" tampil dengan style .btn-danger (merah, border merah, hover solid)
```

## AC-2: Dashboard cards menggunakan .card
```gherkin
Given Dashboard.tsx dirender
When user melihat halaman dashboard
Then chart card dan info card memiliki radius, shadow, dan padding konsisten dari .card
```

## AC-3: Form labels Login menggunakan .label-xs
```gherkin
Given Login.tsx dirender
When user melihat form login
Then label "Nomor Telepon" dan "Kata Sandi" tampil sebagai label-xs (10px, uppercase, tracking-widest)
```

## AC-4: Users page header menggunakan utility classes
```gherkin
Given Users.tsx dirender
When user membuka halaman Pengguna
Then judul "Otentikasi & Akun Pengguna" pakai .page-title, wrapper pakai .page-header, tombol "Add User" pakai .btn-primary
```

## AC-5: Build tetap clean
```gherkin
Given semua perubahan diterapkan
When npm run build dijalankan
Then build selesai tanpa error (exit 0)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update Layout.tsx</name>
  <files>src/components/Layout.tsx</files>
  <action>
    Dua perubahan di Layout.tsx:

    1. **Nav section label** (baris ~56):
       Ubah:
       ```
       <p className="px-3 text-[10px] font-bold text-slate-600 uppercase tracking-widest mb-2">Main Menu</p>
       ```
       Jadi:
       ```
       <p className="label-xs px-3 mb-2">Main Menu</p>
       ```

    2. **Logout button** (baris ~192-198):
       Ubah className panjang:
       ```
       className="w-full flex items-center justify-center gap-2.5 px-3.5 py-3 rounded-2xl bg-red-500/10 hover:bg-red-500 text-red-500 hover:text-white border border-red-500/20 transition-all duration-300 font-bold text-[13px] active:scale-[0.98] cursor-pointer"
       ```
       Jadi:
       ```
       className="btn-danger w-full"
       ```

    Jangan ubah NavLink styling, sidebar structure, header, atau animasi.
  </action>
  <verify>npm run build — build clean, tidak ada error</verify>
  <done>AC-1 satisfied: logout button pakai .btn-danger</done>
</task>

<task type="auto">
  <name>Task 2: Update Dashboard.tsx</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    Empat perubahan di Dashboard.tsx:

    1. **Chart card wrapper** (baris ~155):
       Ubah:
       ```
       className="bg-white rounded-3xl shadow-[0_2px_12px_-4px_rgba(0,0,0,0.04),0_8px_20px_-8px_rgba(0,0,0,0.02)] border border-slate-200/60 p-6 lg:p-7"
       ```
       Jadi:
       ```
       className="card"
       ```

    2. **Chart section heading h2** (baris ~160):
       Ubah:
       ```
       <h2 className="text-base sm:text-lg font-bold tracking-tight text-slate-800">
         Grafik Pesanan Masuk
       </h2>
       ```
       Jadi:
       ```
       <h2 className="section-title">
         Grafik Pesanan Masuk
       </h2>
       ```

    3. **Info box card wrapper** (baris ~211):
       Ubah:
       ```
       className="bg-white rounded-3xl shadow-[0_2px_12px_-4px_rgba(0,0,0,0.04),0_8px_20px_-8px_rgba(0,0,0,0.02)] border border-slate-200/60 p-6 lg:p-7"
       ```
       Jadi:
       ```
       className="card"
       ```

    4. **Info box heading h2** (baris ~215):
       Ubah:
       ```
       <h2 className="text-base sm:text-lg font-bold tracking-tight text-slate-900">Spesifikasi Sistem REMA v2.1</h2>
       ```
       Jadi:
       ```
       <h2 className="section-title">Spesifikasi Sistem REMA v2.1</h2>
       ```

    Jangan ubah: greeting banner (gradient hero), alert banner kredit, chart library props (Recharts), animasi framer motion, dan greeting h1.
  </action>
  <verify>npm run build — build clean</verify>
  <done>AC-2 satisfied: kedua card pakai .card, headings pakai .section-title</done>
</task>

<task type="auto">
  <name>Task 3: Update Login.tsx</name>
  <files>src/pages/Login.tsx</files>
  <action>
    Dua perubahan di Login.tsx:

    1. **Label "Nomor Telepon"** (baris ~114):
       Ubah:
       ```
       <label className="text-[10px] font-bold text-slate-500 uppercase tracking-widest ml-1">Nomor Telepon</label>
       ```
       Jadi:
       ```
       <label className="label-xs ml-1">Nomor Telepon</label>
       ```

    2. **Label "Kata Sandi"** (baris ~132):
       Ubah:
       ```
       <label className="text-[10px] font-bold text-slate-500 uppercase tracking-widest">Kata Sandi</label>
       ```
       Jadi:
       ```
       <label className="label-xs">Kata Sandi</label>
       ```

    3. **Submit button** (baris ~157-183):
       Ubah className button submit yang panjang:
       ```
       className="w-full bg-blue-600 hover:bg-blue-500 text-white font-bold py-4 rounded-2xl transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center gap-2 group shadow-xl shadow-blue-600/10 active:scale-[0.98] mt-8 text-[13px] border border-blue-400/20"
       ```
       Jadi:
       ```
       className="btn-primary w-full py-4 mt-8"
       ```
       Catatan: `py-4` override padding default `.btn-primary` (py-2.5) agar login button tetap lebih tinggi.

    Jangan ubah: hero panel kiri, animasi framer motion, input fields, "Lupa sandi?" link, footer legal.
  </action>
  <verify>npm run build — build clean</verify>
  <done>AC-3 satisfied: form labels pakai .label-xs, submit button pakai .btn-primary</done>
</task>

<task type="auto">
  <name>Task 4: Update Users.tsx</name>
  <files>src/pages/Users.tsx</files>
  <action>
    Lima perubahan di Users.tsx:

    1. **Page header wrapper** (baris ~147):
       Ubah:
       ```
       <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-3">
       ```
       Jadi:
       ```
       <div className="page-header">
       ```

    2. **Page title h1** (baris ~149):
       Ubah:
       ```
       <h1 className="text-xl font-bold text-white tracking-tight leading-none">Otentikasi & Akun Pengguna</h1>
       ```
       Jadi:
       ```
       <h1 className="page-title">Otentikasi & Akun Pengguna</h1>
       ```

    3. **Add User button** (baris ~153-156):
       Ubah className panjang:
       ```
       className="bg-blue-600 hover:bg-blue-500 text-white px-4 py-2 rounded-xl text-[10px] font-bold flex items-center justify-center gap-1.5 transition-all shadow-xl shadow-blue-600/10 active:scale-[0.98] cursor-pointer uppercase tracking-wider"
       ```
       Jadi:
       ```
       className="btn-primary text-[10px] uppercase tracking-wider"
       ```

    4. **UserDetailPanel form action buttons** (baris ~443-445):
       Ubah dua button di akhir form edit:
       ```
       <button type="button" onClick={() => setIsEditing(false)} className="px-4 py-2 bg-slate-100 hover:bg-slate-200 text-slate-700 rounded-xl font-bold transition-all cursor-pointer">Batal</button>
       <button type="submit" className="px-4 py-2 bg-slate-900 hover:bg-slate-800 text-white rounded-xl font-bold transition-all cursor-pointer">Simpan Perubahan</button>
       ```
       Jadi:
       ```
       <button type="button" onClick={() => setIsEditing(false)} className="btn-ghost">Batal</button>
       <button type="submit" className="btn-secondary">Simpan Perubahan</button>
       ```

    5. **AddUserModal form action buttons** (baris ~512-514):
       Ubah dua button yang sama polanya:
       ```
       <button type="button" onClick={onClose} className="px-4 py-2 bg-slate-100 hover:bg-slate-200 text-slate-700 rounded-xl font-bold transition-all cursor-pointer">Batal</button>
       <button type="submit" className="px-4 py-2 bg-slate-900 hover:bg-slate-800 text-white rounded-xl font-bold transition-all cursor-pointer active:scale-95">Simpan Data</button>
       ```
       Jadi:
       ```
       <button type="button" onClick={onClose} className="btn-ghost">Batal</button>
       <button type="submit" className="btn-secondary">Simpan Data</button>
       ```

    Jangan ubah: mobile card list, desktop table, UserDetailPanel action buttons (Edit/Unlock/Reset/Delete — mereka punya semantic warna masing-masing), empty state, animasi.
  </action>
  <verify>npm run build — build clean</verify>
  <done>AC-4 satisfied: page-header, page-title, btn-primary, btn-secondary/btn-ghost diterapkan</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Sidebar NavLink styling (gradient aktif, transition, ChevronRight)
- Animasi Framer Motion (variants, motion.div props)
- Recharts chart props dan konfigurasi
- Input field styling (sudah konsisten, dihandle global CSS)
- Mobile card list di Users (complex status/color logic)
- Desktop table structure di Users
- UserDetailPanel operational action buttons (Edit/Unlock/Reset/Disable/Delete — semantic colors)
- Greeting banner di Dashboard (gradient hero, custom radius)
- Login hero panel kiri
- src/index.css

## SCOPE LIMITS
- Hanya 4 file: Layout.tsx, Dashboard.tsx, Login.tsx, Users.tsx
- Hanya ganti className yang eksplisit disebutkan di tasks
- Tidak menambah fitur, logika, atau state baru
- Tidak menyentuh halaman lain (Mitras, Products, Finance, dll — itu Plan 08-03)

</boundaries>

<verification>
- [ ] npm run build clean (exit 0)
- [ ] Layout.tsx: logout button pakai btn-danger
- [ ] Dashboard.tsx: dua cards pakai .card, dua headings pakai .section-title
- [ ] Login.tsx: dua labels pakai .label-xs, submit pakai .btn-primary
- [ ] Users.tsx: page-header, page-title, btn-primary, btn-secondary, btn-ghost terapplied
- [ ] Tidak ada className yang sebelumnya ada sekarang hilang tanpa pengganti
</verification>

<success_criteria>
- Semua 5 AC terpenuhi
- Build clean
- Zero perubahan fungsional (hanya styling)
- 4 file terubah sesuai boundaries
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/08-ui-consistency/08-02-SUMMARY.md`
</output>
