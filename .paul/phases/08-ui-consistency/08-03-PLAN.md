---
phase: 08-ui-consistency
plan: 03
type: execute
wave: 1
depends_on: ["08-01", "08-02"]
files_modified:
  - src/pages/Mitras.tsx
  - src/pages/Products.tsx
  - src/pages/Finance.tsx
  - src/pages/AppQueue.tsx
  - src/pages/Orders/OrdersList.tsx
  - src/pages/AuditLogs.tsx
autonomous: true
---

<objective>
## Goal
Apply utility classes ke 6 halaman tersisa — Mitras, Products, Finance, AppQueue, OrdersList, AuditLogs — dengan pola yang sama dengan Plan 08-02.

## Purpose
Menyelesaikan konsistensi UI di seluruh aplikasi: semua page headers seragam, semua primary buttons dari satu sumber, metric cards di Finance konsisten.

## Output
- Semua 6 halaman memiliki `.page-header` + `.page-title` pada area judul
- btn-primary/success/secondary pada action buttons
- Finance metric cards → `.card-sm`
- AuditLogs table headers → `.label-xs`
- AppQueue h1 → `.page-title` + pertahankan underline custom
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/08-ui-consistency/08-02-SUMMARY.md

## Source Files
@src/pages/Mitras.tsx
@src/pages/Products.tsx
@src/pages/Finance.tsx
@src/pages/AppQueue.tsx
@src/pages/Orders/OrdersList.tsx
@src/pages/AuditLogs.tsx
</context>

<acceptance_criteria>

## AC-1: Semua 6 halaman punya .page-header + .page-title
```gherkin
Given semua halaman dirender
When user membuka Mitras, Products, Finance, AppQueue, Orders, AuditLogs
Then area judul setiap halaman menggunakan .page-header (wrapper) dan .page-title (h1)
```

## AC-2: Action buttons menggunakan utility classes
```gherkin
Given Products, Finance, dan OrdersList dirender
When user melihat header action buttons
Then: Add Item → .btn-primary, Input Bayar → .btn-success, Tagihan Manual → .btn-secondary, Buat Pesanan Baru → .btn-primary
```

## AC-3: Finance metric cards menggunakan .card-sm
```gherkin
Given Finance dirender
When user melihat area stat cards (Saldo Piutang, Tagihan Pending, Debit, Kredit)
Then semua metric cards menggunakan .card-sm dengan radius dan padding konsisten
```

## AC-4: AuditLogs table headers menggunakan .label-xs
```gherkin
Given AuditLogs dirender
When user melihat tabel log
Then header kolom (Waktu, Pengguna, Aksi, Detail) pakai .label-xs
```

## AC-5: Build clean
```gherkin
Given semua perubahan diterapkan
When npm run build dijalankan
Then build selesai tanpa error (exit 0)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update Mitras.tsx</name>
  <files>src/pages/Mitras.tsx</files>
  <action>
    Dua perubahan di Mitras.tsx:

    1. **Page header wrapper** (baris ~144):
       Ubah:
       ```
       <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-3">
       ```
       Jadi:
       ```
       <div className="page-header">
       ```

    2. **Page title h1** (baris ~146):
       Ubah:
       ```
       <h1 className="text-xl font-bold text-white tracking-tight leading-none">Manajemen Partner & Mitra</h1>
       ```
       Jadi:
       ```
       <h1 className="page-title">Manajemen Partner & Mitra</h1>
       ```

    Jangan ubah: mitra cards, detail panel, form labels, action buttons di detail panel.
  </action>
  <verify>npm run build — build clean</verify>
  <done>AC-1 satisfied (Mitras): page-header + page-title diterapkan</done>
</task>

<task type="auto">
  <name>Task 2: Update Products.tsx</name>
  <files>src/pages/Products.tsx</files>
  <action>
    Tiga perubahan di Products.tsx:

    1. **Page header wrapper** (baris ~119):
       Ubah:
       ```
       <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
       ```
       Jadi:
       ```
       <div className="page-header">
       ```

    2. **Page title h1** (baris ~121):
       Ubah:
       ```
       <h1 className="text-2xl font-bold text-white tracking-tighter leading-none">Katalog & Koleksi Produk</h1>
       ```
       Jadi:
       ```
       <h1 className="page-title">Katalog & Koleksi Produk</h1>
       ```

    3. **Add Item button** (baris ~127):
       Ubah:
       ```
       className="bg-blue-600 hover:bg-blue-500 text-white px-5 py-3 rounded-xl text-[11px] font-bold flex items-center justify-center gap-2 transition-all shadow-xl shadow-blue-600/10 active:scale-95 cursor-pointer uppercase tracking-widest"
       ```
       Jadi:
       ```
       className="btn-primary text-[11px] uppercase tracking-widest"
       ```

    Jangan ubah: product grid cards, detail panel, form fields, modal overlays.
  </action>
  <verify>npm run build — build clean</verify>
  <done>AC-1 (Products) + AC-2 (btn-primary) satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Update Finance.tsx</name>
  <files>src/pages/Finance.tsx</files>
  <action>
    Enam perubahan di Finance.tsx:

    1. **Page header wrapper** (baris ~155):
       Ubah:
       ```
       <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-3">
       ```
       Jadi:
       ```
       <div className="page-header">
       ```

    2. **Page title h1** (baris ~157):
       Ubah:
       ```
       <h1 className="text-lg sm:text-xl font-black tracking-tight text-slate-900 leading-tight">Keuangan & Tagihan Mitra</h1>
       ```
       Jadi:
       ```
       <h1 className="page-title">Keuangan & Tagihan Mitra</h1>
       ```

    3. **Input Bayar button** (baris ~165):
       Ubah:
       ```
       className="flex-1 sm:flex-none bg-emerald-600 hover:bg-emerald-700 text-white px-3.5 py-2 rounded-xl font-bold transition-all shadow-md shadow-emerald-500/10 flex justify-center items-center gap-1.5 active:scale-95 cursor-pointer text-[11px]"
       ```
       Jadi:
       ```
       className="btn-success flex-1 sm:flex-none text-[11px]"
       ```

    4. **Tagihan Manual button** (baris ~172):
       Ubah:
       ```
       className="flex-1 sm:flex-none bg-slate-900 hover:bg-slate-800 text-white px-3.5 py-2 rounded-xl font-bold transition-all shadow-md flex justify-center items-center gap-1.5 active:scale-95 cursor-pointer text-[11px]"
       ```
       Jadi:
       ```
       className="btn-secondary flex-1 sm:flex-none text-[11px]"
       ```

    5. **Metric card 1 — Saldo Piutang** (baris ~184):
       Ubah:
       ```
       className="bg-white p-3 sm:p-4 rounded-2xl border border-slate-200/60 shadow-[0_2px_8px_-4px_rgba(0,0,0,0.03),0_4px_12px_-8px_rgba(0,0,0,0.01)] flex flex-col justify-between"
       ```
       Jadi:
       ```
       className="card-sm flex flex-col justify-between"
       ```

    6. **Metric card 2 — Tagihan Pending** (baris ~199):
       Ubah className yang sama persis dengan card 1 (pattern identik):
       ```
       className="bg-white p-3 sm:p-4 rounded-2xl border border-slate-200/60 shadow-[0_2px_8px_-4px_rgba(0,0,0,0.03),0_4px_12px_-8px_rgba(0,0,0,0.01)] flex flex-col justify-between"
       ```
       Jadi:
       ```
       className="card-sm flex flex-col justify-between"
       ```

    7. **Metric card 3 — Debit (Tagihan)** (baris ~218):
       Ubah:
       ```
       className="bg-white p-3 sm:p-4 rounded-2xl border border-slate-200/60 shadow-sm flex flex-col justify-center"
       ```
       Jadi:
       ```
       className="card-sm flex flex-col justify-center"
       ```

    8. **Metric card 4 — Kredit (Bayar)** (baris ~228):
       Ubah className yang sama dengan card 3:
       ```
       className="bg-white p-3 sm:p-4 rounded-2xl border border-slate-200/60 shadow-sm flex flex-col justify-center"
       ```
       Jadi:
       ```
       className="card-sm flex flex-col justify-center"
       ```

    Jangan ubah: ledger table, modal forms (payment/charge), filter dropdown, date range picker.
  </action>
  <verify>npm run build — build clean</verify>
  <done>AC-1 (Finance) + AC-2 (btn-success/secondary) + AC-3 (card-sm) satisfied</done>
</task>

<task type="auto">
  <name>Task 4: Update AppQueue.tsx</name>
  <files>src/pages/AppQueue.tsx</files>
  <action>
    Dua perubahan di AppQueue.tsx:

    1. **Page header wrapper** (baris ~95):
       Ubah:
       ```
       <div className="flex flex-col sm:flex-row sm:items-end justify-between gap-4">
       ```
       Jadi:
       ```
       <div className="page-header items-end">
       ```
       (Pertahankan `items-end` karena AppQueue punya counter pills di kanan yang perlu align ke bawah)

    2. **Page title h1** (baris ~97):
       Ubah:
       ```
       <h1 className="text-xl font-black tracking-tight text-slate-900 leading-none underline decoration-indigo-500/30 decoration-4 underline-offset-8">Antrian Produksi</h1>
       ```
       Jadi:
       ```
       <h1 className="page-title underline decoration-indigo-500/30 decoration-4 underline-offset-8">Antrian Produksi</h1>
       ```
       (Pertahankan underline decoration custom — itu bagian dari estetika halaman ini)

    Jangan ubah: pipeline cards, status badges, order cards, counter pills.
  </action>
  <verify>npm run build — build clean</verify>
  <done>AC-1 (AppQueue) satisfied: page-header + page-title dengan underline preserved</done>
</task>

<task type="auto">
  <name>Task 5: Update OrdersList.tsx</name>
  <files>src/pages/Orders/OrdersList.tsx</files>
  <action>
    Tiga perubahan di OrdersList.tsx:

    1. **Page header wrapper** (baris ~190):
       Ubah:
       ```
       <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
       ```
       Jadi:
       ```
       <div className="page-header">
       ```

    2. **Page title h1** (baris ~192):
       Ubah:
       ```
       <h1 className="text-xl sm:text-2xl font-bold tracking-tighter text-white">{isDraftPage ? 'Draft Unit Pesanan' : 'Daftar Manajemen Pesanan'}</h1>
       ```
       Jadi:
       ```
       <h1 className="page-title">{isDraftPage ? 'Draft Unit Pesanan' : 'Daftar Manajemen Pesanan'}</h1>
       ```

    3. **Buat Pesanan Baru Link** (baris ~198 — ini adalah `<Link>` bukan `<button>`):
       Ubah:
       ```
       className="bg-slate-900 hover:bg-blue-600 text-white px-5 py-2.5 rounded-xl text-[13px] font-semibold flex items-center justify-center gap-2 transition-all border border-slate-800 hover:border-blue-500 shadow-xl shadow-black/20 active:scale-[0.98] w-full sm:w-auto text-center group"
       ```
       Jadi:
       ```
       className="btn-primary w-full sm:w-auto group"
       ```

    Jangan ubah: search bar, filter pills, mobile cards, desktop table, pagination.
  </action>
  <verify>npm run build — build clean</verify>
  <done>AC-1 (OrdersList) + AC-2 (btn-primary Link) satisfied</done>
</task>

<task type="auto">
  <name>Task 6: Update AuditLogs.tsx</name>
  <files>src/pages/AuditLogs.tsx</files>
  <action>
    Empat perubahan di AuditLogs.tsx:

    1. **Page header wrapper** (baris ~48):
       Ubah:
       ```
       <div className="flex flex-col md:flex-row md:items-center justify-between gap-4">
       ```
       Jadi:
       ```
       <div className="page-header">
       ```

    2. **Page title h1** (baris ~50):
       Ubah:
       ```
       <h1 className="text-3xl font-black tracking-tight text-white flex items-center gap-2">
       ```
       Jadi:
       ```
       <h1 className="page-title flex items-center gap-2">
       ```
       (Pertahankan `flex items-center gap-2` — dipakai untuk ikon di dalam h1)

    3. **Table header columns** (baris ~83-86) — 4 `<th>` elements:
       Ubah setiap:
       ```
       className="px-6 py-4 text-xs font-bold text-slate-500 uppercase tracking-widest"
       ```
       Jadi:
       ```
       className="px-6 py-4 label-xs"
       ```
       Ada 4 kolom: Waktu, Pengguna, Aksi, Detail — ubah semua dengan replace_all=true pada className tersebut.

    Jangan ubah: search input, filter display, log table rows, pagination, card container wrappers.
  </action>
  <verify>npm run build — build clean</verify>
  <done>AC-1 (AuditLogs) + AC-4 (label-xs headers) satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Mitra detail panel + form (complex layout)
- Products grid cards + product image handling
- Finance ledger table, modal forms (payment/charge), filter system
- AppQueue pipeline cards dan status logic
- OrdersList search/filter/pagination/table
- AuditLogs card container wrappers (complex flex layouts)
- src/index.css
- Semua halaman lain di luar 6 file ini

## SCOPE LIMITS
- Hanya 6 file di tasks
- Hanya className replacements — tidak ada logika baru
- `page-header` tidak override semua children — hanya wrapper div
- Tidak menyentuh modal content atau overlay components

</boundaries>

<verification>
- [ ] npm run build clean (exit 0)
- [ ] Mitras: page-header + page-title ✓
- [ ] Products: page-header + page-title + btn-primary ✓
- [ ] Finance: page-header + page-title + btn-success + btn-secondary + 4x card-sm ✓
- [ ] AppQueue: page-header (items-end) + page-title (underline preserved) ✓
- [ ] OrdersList: page-header + page-title + btn-primary Link ✓
- [ ] AuditLogs: page-header + page-title (flex preserved) + 4x label-xs th ✓
</verification>

<success_criteria>
- Semua 6 tasks selesai
- Build clean (exit 0)
- Zero perubahan fungsional — hanya className
- Phase 8 UI Consistency complete (semua 3 plans selesai)
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/08-ui-consistency/08-03-SUMMARY.md`
</output>
