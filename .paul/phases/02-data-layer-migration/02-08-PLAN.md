---
phase: 02-data-layer-migration
plan: 08
type: execute
wave: 1
depends_on: ["02-07"]
files_modified:
  - src/api/routes/orders.ts
  - src/lib/api.ts
  - src/pages/orders/OrdersList.tsx
  - src/pages/orders/CreateOrder.tsx
  - src/pages/orders/OrderDetail.tsx
autonomous: true
---

<objective>
## Goal
Migrasi orders/* (3 file, 1597L, 23 db.* calls) dari localStorage ke API — OrdersList.tsx, CreateOrder.tsx, dan OrderDetail.tsx bebas dari db.*.

## Purpose
orders/* adalah layer terakhir Phase 2. Setelah plan ini, seluruh data layer sudah via Neon DB dan Phase 2 selesai.

## Output
- `GET /api/orders/:id` — ambil satu order dengan items (dibutuhkan CreateOrder edit mode + OrderDetail)
- `DELETE /api/orders/:id` — hapus order + orderItems (dibutuhkan OrderDetail)
- `api.orders.get(id)` + `api.orders.remove(id)` di src/lib/api.ts
- `OrdersList.tsx` — useEffect load orders + mitras, zero db.*
- `CreateOrder.tsx` — async load products/existingOrder/myMitraRecord, save via api.orders.*
- `OrderDetail.tsx` — async load order/mitras/users, status transitions + ledger + delete via API
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/02-data-layer-migration/02-07-SUMMARY.md

## Source Files
@src/api/routes/orders.ts
@src/lib/api.ts
@src/pages/orders/OrdersList.tsx
@src/pages/orders/CreateOrder.tsx
@src/pages/orders/OrderDetail.tsx
</context>

<acceptance_criteria>

## AC-1: orders/* bebas dari db.*
```gherkin
Given semua perubahan diterapkan
When grep -rn "db\." src/pages/orders/ dijalankan
Then tidak ada output — zero db.* references di semua 3 file
```

## AC-2: GET + DELETE /api/orders/:id tersedia
```gherkin
Given order dengan id tertentu ada di DB
When GET /api/orders/:id dipanggil
Then order beserta items-nya dikembalikan sebagai JSON
And When DELETE /api/orders/:id dipanggil
Then order dan orderItems-nya terhapus dari DB
```

## AC-3: CreateOrder berfungsi via API
```gherkin
Given mitra membuka halaman buat pesanan baru atau edit draft
When mitra mengisi form dan klik Simpan Draft atau Ajukan
Then order tersimpan/terupdate via api.orders.create/update
And credit limit check dilakukan via api.ledgers.list(mitraId)
```

## AC-4: OrderDetail status transitions berfungsi via API
```gherkin
Given admin membuka detail pesanan
When admin mengubah status (misalnya confirmed → processing → packing)
Then api.orders.update() dipanggil dengan status baru
And saat packing: api.ledgers.create() mencatat billing debit
And saat cancelled/returned: api.ledgers.removeByOrder() rollback billing
```

## AC-5: TypeScript zero errors
```gherkin
Given semua perubahan diterapkan
When npx tsc --noEmit dijalankan
Then output bersih tanpa TypeScript error
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add GET/:id + DELETE/:id di orders.ts + api.ts + migrate OrdersList.tsx</name>
  <files>src/api/routes/orders.ts, src/lib/api.ts, src/pages/orders/OrdersList.tsx</files>
  <action>
    **A. Tambah GET /api/orders/:id di orders.ts (sebelum PUT /:id):**

    ```typescript
    router.get('/:id', async (req: Request, res: Response) => {
      try {
        const order = await db.select().from(orders).where(eq(orders.id, req.params.id)).limit(1);
        if (!order[0]) return res.status(404).json({ error: 'Order not found' });
        const items = await db.select().from(orderItems).where(eq(orderItems.orderId, req.params.id));
        return res.json({ ...order[0], items });
      } catch (err: any) {
        return res.status(500).json({ error: err.message });
      }
    });
    ```

    **B. Tambah DELETE /api/orders/:id di orders.ts (setelah PUT /:id):**

    Catatan: orderItems harus dihapus SEBELUM orders karena FK constraint (orderItems.orderId → orders.id).

    ```typescript
    router.delete('/:id', async (req: Request, res: Response) => {
      try {
        await db.delete(orderItems).where(eq(orderItems.orderId, req.params.id));
        await db.delete(orders).where(eq(orders.id, req.params.id));
        return res.json({ success: true });
      } catch (err: any) {
        return res.status(500).json({ error: err.message });
      }
    });
    ```

    **C. Update api.orders di src/lib/api.ts:**

    ```typescript
    orders: {
      list: () => request<Order[]>('GET', '/orders'),
      get: (id: string) => request<Order>('GET', `/orders/${id}`),
      create: (data: Omit<Order, 'id'>) => request<Order>('POST', '/orders', data),
      update: (id: string, data: Partial<Order>) => request<Order>('PUT', `/orders/${id}`, data),
      remove: (id: string) => request<void>('DELETE', `/orders/${id}`),
    },
    ```

    **D. Migrate OrdersList.tsx:**

    Ubah imports: hapus `import { db } from '../../lib/db'`, tambah `import { api } from '../../lib/api'`.
    Tambah `useEffect` ke React imports. Tambah import `Mitra` dari types.

    Ganti synchronous load dengan async state:

    ```typescript
    // SEBELUM
    const orders = db.getOrders();
    const mitras = db.getMitras();

    // SESUDAH
    const [orders, setOrders] = useState<Order[]>([]);
    const [mitras, setMitras] = useState<Mitra[]>([]);

    useEffect(() => {
      Promise.all([api.orders.list(), api.mitras.list()])
        .then(([o, m]) => { setOrders(o); setMitras(m); })
        .catch(() => toast.error('Gagal memuat data pesanan'));
    }, []);
    ```

    Tambah `import { toast } from 'react-hot-toast'` jika belum ada.
    Cek apakah ada `toast` yang perlu di-import (kemungkinan belum ada di OrdersList).
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero errors
    2. `grep -n "router.get\|router.delete\|router.put" src/api/routes/orders.ts` — ada GET /:id, DELETE /:id, PUT /:id
    3. `grep -n "get:\|remove:" src/lib/api.ts` — ada api.orders.get + api.orders.remove
    4. `grep -n "db\." src/pages/orders/OrdersList.tsx` — no output
  </verify>
  <done>AC-2 satisfied: GET + DELETE /api/orders/:id tersedia. AC-1 partial: OrdersList.tsx clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CreateOrder.tsx — async load + save via API</name>
  <files>src/pages/orders/CreateOrder.tsx</files>
  <action>
    Ubah imports: hapus `import { db } from '../../lib/db'`, tambah `import { api } from '../../lib/api'`.
    Tambah `useEffect` ke React imports. Tambah import `Mitra` dari types.

    **A. Ganti synchronous state ke async dengan loading state:**

    ```typescript
    // SEBELUM
    const products = db.getProducts().filter(p => !p.isArchived);
    const existingOrder = id ? db.getOrders().find(o => o.id === id) : undefined;
    const myMitraRecord = db.getMitras().find(m => m.userId === user?.id);

    // SESUDAH — tambah state
    const [products, setProducts] = useState<Product[]>([]);
    const [existingOrder, setExistingOrder] = useState<Order | undefined>(undefined);
    const [myMitraRecord, setMyMitraRecord] = useState<Mitra | undefined>(undefined);
    const [loading, setLoading] = useState(true);
    ```

    **B. Load data di useEffect (gabungkan dengan useEffect yang sudah ada untuk pre-fill form):**

    ```typescript
    useEffect(() => {
      const loadData = async () => {
        try {
          const [prods, mitras, ...(id ? [await api.orders.get(id)] : [])] = await Promise.all([
            api.products.list(),
            api.mitras.list(),
            ...(id ? [api.orders.get(id)] : []),
          ]);
          setProducts((prods as Product[]).filter(p => !p.isArchived));
          const myMitra = (mitras as Mitra[]).find(m => m.userId === user?.id);
          setMyMitraRecord(myMitra);
          if (id && prods !== undefined) {
            // ambil dari index 2 jika ada
          }
        } catch {
          toast.error('Gagal memuat data');
        }
      };
      loadData();
    }, [id]);
    ```

    Lebih mudah ditulis secara sequential:

    ```typescript
    useEffect(() => {
      Promise.all([
        api.products.list(),
        api.mitras.list(),
        id ? api.orders.get(id) : Promise.resolve(undefined),
      ]).then(([prods, mitras, order]) => {
        setProducts((prods as Product[]).filter(p => !p.isArchived));
        const myMitra = (mitras as Mitra[]).find(m => m.userId === user?.id);
        setMyMitraRecord(myMitra);
        if (order) {
          const o = order as Order;
          setExistingOrder(o);
          // Pre-fill form fields
          setType(o.type);
          setResiUrl(o.resiUrl || '');
          setRecipientName(o.recipientName || '');
          setRecipientPhone(o.recipientPhone || '');
          setRecipientAddress(o.recipientAddress || '');
          setItems(o.items || []);
        }
        setLoading(false);
      }).catch(() => {
        toast.error('Gagal memuat data pesanan');
        setLoading(false);
      });
    }, [id]);
    ```

    Hapus useEffect yang lama (untuk pre-fill form fields dari existingOrder):
    ```typescript
    // HAPUS ini:
    React.useEffect(() => {
      if (existingOrder) {
        setType(existingOrder.type);
        // ...
      }
    }, [id]);
    ```

    **C. Tambah loading guard di awal return:**

    ```typescript
    if (!user) return null;
    if (loading) return <div className="p-8 text-center text-slate-400 font-semibold">Memuat data...</div>;
    ```

    Pindahkan loading check SEBELUM authorization checks yang bergantung pada data.

    **D. Migrate handleSave — credit check + save via API:**

    ```typescript
    const handleSave = async (isSubmit: boolean) => {
      // ... validasi yang sama ...

      // Credit check (async sekarang)
      if (isSubmit && user.role === 'mitra' && myMitraRecord) {
        if (myMitraRecord.creditLimit) {
          try {
            const ledgers = await api.ledgers.list(myMitraRecord.id);
            const saldo = ledgers.reduce((acc, curr) => acc + (curr.direction === 'debit' ? curr.nominal : -curr.nominal), 0);
            if (saldo > myMitraRecord.creditLimit) {
              toast.error(`Tagihan berjalan Anda saat ini: ${formatCurrency(saldo)}. Limit tagihan Anda: ${formatCurrency(myMitraRecord.creditLimit)}. Mohon lakukan pembayaran saldo terlebih dahulu.`);
              return;
            }
          } catch {
            toast.error('Gagal memeriksa limit kredit');
            return;
          }
        }
      }

      // Build order object (sama seperti sebelumnya)
      const order: Order = { ... };

      try {
        if (existingOrder) {
          await api.orders.update(existingOrder.id, order);
          await api.auditLogs.create({ userId: user.id, action: 'ORDER_UPDATED', details: `Mengubah detail pesanan #${order.orderNumber}` });
          toast.success('Pesanan berhasil diperbarui');
        } else {
          if (isSubmit) {
            const isConfirmed = await confirm({ ... }); // sama
            if (!isConfirmed) return;
          }
          await api.orders.create(order);
          await api.auditLogs.create({ userId: user.id, action: isSubmit ? 'ORDER_SUBMITTED' : 'ORDER_DRAFTED', details: `Order ${order.orderNumber}` });
          toast.success(isSubmit ? 'Pesanan berhasil diajukan untuk verifikasi' : 'Draft pesanan disimpan');
        }
        navigate('/orders');
      } catch (err: any) {
        toast.error(err.message || 'Gagal menyimpan pesanan');
      }
    };
    ```

    Note: `order` object yang dibuat perlu mempertahankan `id` dari existingOrder jika edit, dan generate baru jika create. Gunakan pola yang sama dengan kode lama:
    ```typescript
    const order: Order = {
      ...(existingOrder || {
        id: crypto.randomUUID(),
        orderNumber: generateOrderNumber(),
        mitraId: myMitraRecord?.id || '',
        createdAt: Date.now(),
        isBilled: false,
        creatorId: user.id,
      }),
      type, resiUrl, recipientName, recipientPhone, recipientAddress,
      items, status: ..., updatedAt: Date.now(), hasCustomLogo, totalAmount, totalQty,
    };
    ```

    **E. Hapus import { db } yang sudah tidak terpakai.**
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero errors
    2. `grep -n "db\." src/pages/orders/CreateOrder.tsx` — no output
    3. `grep -n "import.*db" src/pages/orders/CreateOrder.tsx` — no output
  </verify>
  <done>AC-1 partial: CreateOrder.tsx clean. AC-3 satisfied: CreateOrder berfungsi via API.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate OrderDetail.tsx — async load + status transitions + ledger + delete via API</name>
  <files>src/pages/orders/OrderDetail.tsx</files>
  <action>
    Ubah imports: hapus `import { db } from '../../lib/db'`, tambah `import { api } from '../../lib/api'`.
    Tambah `useEffect` ke React imports. Tambah import `Mitra, User` dari types (jika belum ada).

    **A. Ganti synchronous state ke async:**

    ```typescript
    // SEBELUM
    const [orders, setOrders] = useState<Order[]>(db.getOrders());
    const mitrasList = db.getMitras();
    const usersList = db.getUsers();

    // SESUDAH
    const [order, setOrder] = useState<Order | null>(null);
    const [mitras, setMitras] = useState<Mitra[]>([]);
    const [users, setUsers] = useState<User[]>([]);
    const [loading, setLoading] = useState(true);
    ```

    **B. Load data via useEffect:**

    ```typescript
    useEffect(() => {
      if (!id) return;
      Promise.all([
        api.orders.get(id),
        api.mitras.list(),
        api.users.list(),
      ]).then(([o, m, u]) => {
        setOrder(o);
        setMitras(m);
        setUsers(u);
        setLoading(false);
      }).catch(() => {
        toast.error('Gagal memuat data pesanan');
        setLoading(false);
      });
    }, [id]);
    ```

    **C. Update computed values — ganti mitrasList/usersList ke mitras/users:**

    ```typescript
    // SEBELUM
    const activeMitra = mitrasList.find(m => m.userId === user.id);
    const orderMitra = mitrasList.find(m => m.id === order.mitraId);
    const mitraUser = usersList.find(u => u.id === orderMitra?.userId);

    // SESUDAH (hitung dari state)
    const activeMitra = mitras.find(m => m.userId === user?.id);
    const orderMitra = order ? mitras.find(m => m.id === order.mitraId) : undefined;
    const mitraUser = users.find(u => u.id === orderMitra?.userId);
    ```

    **D. Tambah loading guard dan null check untuk order:**

    ```typescript
    if (!user) return null;
    if (loading) return <div className="p-8 text-center text-slate-400 font-semibold">Memuat data...</div>;
    if (!order) return <div className="p-8 text-center text-slate-500 font-bold">Pesanan tidak ditemukan...</div>;
    ```

    Hapus baris lama: `const order = orders.find(o => o.id === id);` (karena sekarang `order` adalah state langsung).

    **E. Migrate handleUpdateStatus:**

    Credit check (waiting_confirmation):
    ```typescript
    if (newStatus === 'waiting_confirmation' && order.status === 'draft') {
      const myMitraRecord = mitras.find(m => m.userId === user.id);
      if (myMitraRecord?.creditLimit) {
        try {
          const ledgers = await api.ledgers.list(myMitraRecord.id);
          const saldo = ledgers.reduce((acc, curr) => acc + (curr.direction === 'debit' ? curr.nominal : -curr.nominal), 0);
          if (saldo > myMitraRecord.creditLimit) {
            toast.error(`Tagihan Anda saat ini: ${formatCurrency(saldo)}. Limit tagihan Anda: ${formatCurrency(myMitraRecord.creditLimit)}. Silakan lakukan pelunasan terlebih dahulu.`);
            return;
          }
        } catch {
          toast.error('Gagal memeriksa limit kredit');
          return;
        }
      }
    }
    ```

    Billing (packing):
    ```typescript
    if (newStatus === 'packing' && !order.isBilled) {
      await api.ledgers.create({
        mitraId: order.mitraId,
        source: 'order',
        direction: 'debit',
        nominal: order.totalAmount,
        description: `Pesanan ${order.orderNumber} - ${order.totalQty} pcs`,
        createdAt: Date.now(),
        referenceId: order.id,
      });
    }
    ```

    Rollback (cancelled/returned):
    ```typescript
    if ((newStatus === 'cancelled' || newStatus === 'returned') && order.isBilled) {
      await api.ledgers.removeByOrder(order.id);
    }
    ```

    Save + audit:
    ```typescript
    const updated = {
      ...order,
      status: newStatus,
      isBilled: newStatus === 'packing' ? true : ((newStatus === 'cancelled' || newStatus === 'returned') ? false : order.isBilled)
    };
    try {
      await api.orders.update(order.id, updated);
      await api.auditLogs.create({ userId: user.id, action: 'STATUS_CHANGE', details: `${order.status} -> ${newStatus} on ${order.orderNumber}` });
      setOrder(updated);
      toast.success('Status pesanan berhasil diperbarui');
    } catch (err: any) {
      toast.error(err.message || 'Gagal update status');
    }
    ```

    Wrap seluruh handleUpdateStatus dalam try/catch yang menangkap semua error.

    **F. Migrate handleUpdateDTFStatus:**

    ```typescript
    const handleUpdateDTFStatus = async (itemId: string, newDTFStatus: 'belum_cetak' | 'sudah_cetak') => {
      // ... validasi sama ...
      const updatedItems = order.items.map(item =>
        item.id === itemId ? { ...item, dtfStatus: newDTFStatus } : item
      );
      const updatedOrder = { ...order, items: updatedItems };
      try {
        await api.orders.update(order.id, updatedOrder);
        await api.auditLogs.create({ userId: user.id, action: 'DTF_STATUS_CHANGE', details: `Status DTF item diubah ke ${newDTFStatus} pada pesanan ${order.orderNumber}` });
        setOrder(updatedOrder);
        toast.success('Status Cetak DTF berhasil diubah');
      } catch (err: any) {
        toast.error(err.message || 'Gagal update status DTF');
      }
    };
    ```

    Perlu diubah dari sync ke async function.

    **G. Migrate handleDeleteOrder:**

    ```typescript
    const handleDeleteOrder = async () => {
      // ... confirm sama ...
      try {
        await api.orders.remove(order.id);
        await api.auditLogs.create({ userId: user.id, action: 'DELETE_ORDER', details: `Menghapus pesanan #${order.orderNumber}` });
        toast.success('Pesanan berhasil dihapus');
        navigate(order.status === 'draft' ? '/orders/drafts' : '/orders');
      } catch (err: any) {
        toast.error(err.message || 'Gagal menghapus pesanan');
      }
    };
    ```

    **H. Update reference di JSX — ganti `mitrasList` → `mitras`, `usersList` → `users` di seluruh render.**

    Cari semua referensi `mitrasList` dan `usersList` di JSX dan ganti dengan `mitras` dan `users`.

    Juga update draft access check:
    ```typescript
    // SEBELUM
    const creatorUserId = order.creatorId || mitrasList.find(m => m.id === order.mitraId)?.userId;

    // SESUDAH
    const creatorUserId = order.creatorId || mitras.find(m => m.id === order.mitraId)?.userId;
    ```
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero errors
    2. `grep -n "db\." src/pages/orders/OrderDetail.tsx` — no output
    3. `grep -rn "db\." src/pages/orders/` — no output (semua 3 file bersih)
  </verify>
  <done>AC-1 satisfied: semua 3 file orders/* bebas db.*. AC-4 satisfied: status transitions via API. AC-5 satisfied: TypeScript clean.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/lib/db.ts` — legacy localStorage wrapper, jangan dimodifikasi
- `src/db/schema.ts` — schema sudah final
- `src/types.ts` — jangan ubah type definitions
- `src/api/routes/ledgers.ts` — sudah selesai di Plan 02-07
- `src/api/routes/mitras.ts` — sudah selesai di Plan 02-06

## SCOPE LIMITS
- Jangan ubah visual/styling komponen
- FileUpload.tsx + MultiFileUpload.tsx tetap seperti sekarang (R2 adalah Phase 3)
- Jangan tambah fitur baru (server-side pagination, filter, dll)
- Jangan migrate file selain orders/* (sudah selesai)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -rn "db\." src/pages/orders/` — no output
- [ ] `npx tsc --noEmit` — clean
- [ ] GET /api/orders/:id route ada di orders.ts
- [ ] DELETE /api/orders/:id route ada (setelah PUT /:id, delete orderItems dulu)
- [ ] api.orders.get() + api.orders.remove() ada di api.ts
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- OrdersList.tsx: zero db.* references, data via api.orders.list() + api.mitras.list()
- CreateOrder.tsx: async load products/existingOrder/myMitraRecord, save via api.orders.*
- OrderDetail.tsx: async load, status transitions + ledger + delete via API
- GET + DELETE /api/orders/:id berfungsi
- TypeScript clean
- Phase 2 complete — semua localStorage calls di-migrasi ke API
</success_criteria>

<output>
After completion, create `.paul/phases/02-data-layer-migration/02-08-SUMMARY.md`
</output>
