---
phase: 02-data-layer-migration
plan: 05
type: execute
wave: 1
depends_on: ["02-04"]
files_modified:
  - src/pages/Users.tsx
  - src/api/routes/users.ts
  - src/lib/api.ts
autonomous: true
---

<objective>
## Goal
Migrasi Users.tsx dari localStorage db.* ke async api.* — termasuk menambah DELETE /api/users/:id endpoint dengan full server-side validation (audit trail, cascade mitra, self-delete guard).

## Purpose
Users.tsx adalah halaman paling kompleks yang tersisa: 568 baris, 5 handler mutasi, cross-entity (users + mitras + orders + ledgers + requests). Menyelesaikan plan ini berarti manajemen user sepenuhnya berjalan via Neon DB.

## Output
- DELETE /api/users/:id endpoint dengan 4 validasi server-side
- api.users.remove() di src/lib/api.ts
- Users.tsx bebas db.* — semua operasi via api.*, SafeUser types throughout
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/02-data-layer-migration/02-04-SUMMARY.md

## Source Files
@src/pages/Users.tsx
@src/api/routes/users.ts
@src/lib/api.ts
@src/db/schema.ts
</context>

<acceptance_criteria>

## AC-1: Users.tsx tidak ada db.*
```gherkin
Given Users.tsx sudah dimigrasikan
When file di-grep untuk "db\."
Then tidak ada match — semua operasi via api.users.* dan api.mitras.*
```

## AC-2: DELETE endpoint users berfungsi dengan semua validasi
```gherkin
Given admin mencoba menghapus akunnya sendiri
When DELETE /api/users/:id dengan id = req.user.sub
Then API mengembalikan 403

Given user yang memiliki riwayat audit log
When DELETE /api/users/:id dipanggil
Then API mengembalikan 409 "memiliki riwayat aktivitas"

Given mitra user yang sudah memiliki orders/ledgers/requests
When DELETE /api/users/:id dipanggil
Then API mengembalikan 409 "entitas mitranya sudah memiliki pesanan..."

Given user tanpa audit trail dan tanpa mitra dependencies
When DELETE /api/users/:id dipanggil
Then user terhapus dari database (beserta linked mitra jika ada)
```

## AC-3: Reset password via API menggunakan bcrypt (bukan plaintext)
```gherkin
Given admin melakukan reset password pada user lain
When handleResetPassword dipanggil
Then api.users.update(id, {newPassword: 'rema1234', mustChangePassword: true}) dipanggil
And server menyimpan bcrypt hash, bukan plaintext 'rema1234'
```

## AC-4: TypeScript compile zero errors
```gherkin
Given semua perubahan sudah diterapkan
When npx tsc --noEmit dijalankan
Then exit code 0, tidak ada TypeScript error
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Tambah DELETE /api/users/:id endpoint dan api.users.remove()</name>
  <files>src/api/routes/users.ts, src/lib/api.ts</files>
  <action>
    **Step A — src/api/routes/users.ts: tambah DELETE /:id**

    Tambah imports berikut di atas file (setelah import yang sudah ada):
    ```
    import { and, eq } from 'drizzle-orm';
    import { users, mitras, auditLogs, orders, ledgers, actionRequests } from '../../db/schema.ts';
    ```
    (ganti import `eq` yang sudah ada dengan `{ and, eq }`)

    Tambah endpoint DELETE di bawah router.patch (sebelum export default):
    ```
    router.delete('/:id', async (req: Request, res: Response) => {
      try {
        // Guard: tidak bisa hapus diri sendiri
        if (req.params.id === (req as any).user.sub) {
          return res.status(403).json({ error: 'Tidak dapat menghapus akun Anda sendiri.' });
        }

        // Cek audit trail
        const audit = await db.select().from(auditLogs)
          .where(eq(auditLogs.userId, req.params.id)).limit(1);
        if (audit.length > 0) {
          return res.status(409).json({ error: 'Tidak dapat menghapus pengguna ini karena memiliki riwayat aktivitas di sistem. Silakan nonaktifkan saja.' });
        }

        // Ambil user untuk cek role
        const [targetUser] = await db.select().from(users)
          .where(eq(users.id, req.params.id)).limit(1);
        if (!targetUser) return res.status(404).json({ error: 'User not found' });

        // Jika mitra: cek dependencies sebelum hapus
        if (targetUser.role === 'mitra') {
          const [linkedMitra] = await db.select().from(mitras)
            .where(eq(mitras.userId, req.params.id)).limit(1);
          if (linkedMitra) {
            const [usedOrder] = await db.select().from(orders)
              .where(eq(orders.mitraId, linkedMitra.id)).limit(1);
            const [usedLedger] = await db.select().from(ledgers)
              .where(eq(ledgers.mitraId, linkedMitra.id)).limit(1);
            const [usedRequest] = await db.select().from(actionRequests)
              .where(eq(actionRequests.mitraId, linkedMitra.id)).limit(1);
            if (usedOrder || usedLedger || usedRequest) {
              return res.status(409).json({ error: 'Tidak dapat menghapus user ini karena entitas mitranya sudah memiliki pesanan, riwayat saldo, atau pengajuan (request).' });
            }
            // Hapus linked mitra (hard delete)
            await db.delete(mitras).where(eq(mitras.id, linkedMitra.id));
          }
        }

        // Hapus user
        await db.delete(users).where(eq(users.id, req.params.id));
        return res.json({ ok: true });
      } catch (err: any) {
        return res.status(500).json({ error: err.message });
      }
    });
    ```

    PENTING: `req.user` diset oleh middleware auth sebagai `{ sub, role, phone }`. Akses via `(req as any).user.sub`.

    **Step B — src/lib/api.ts: tambah api.users.remove()**

    Di objek `users` dalam api, tambah setelah `changePassword`:
    ```
    remove: (id: string) => request<void>('DELETE', `/users/${id}`),
    ```
  </action>
  <verify>
    1. grep -n "router.delete" src/api/routes/users.ts — harus ada match
    2. grep -n "remove:" src/lib/api.ts — harus ada match di users section
    3. npx tsc --noEmit — exit 0
  </verify>
  <done>AC-2 satisfied: DELETE endpoint ada dengan 4 validasi; api.users.remove() tersedia</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Users.tsx ke async API pattern</name>
  <files>src/pages/Users.tsx</files>
  <action>
    Migrasi Users.tsx dari synchronous db.* ke async api.*. Perubahan per bagian:

    **1. Imports**
    - Hapus: `import { db } from '../lib/db'`
    - Hapus: `import { User, Role } from '../types'`
    - Tambah: `import { useState, useEffect } from 'react'`
    - Tambah: `import { api } from '../lib/api'`
    - Tambah: `import { User, Mitra, Role } from '../types'`

    **2. State dan type di komponen Users()**
    - Definisikan: `type SafeUser = Omit<User, 'passwordHash'>`
    - Ubah: `useState<User[]>(db.getUsers())` → `useState<SafeUser[]>([])`
    - Tambah: `const [mitras, setMitras] = useState<Mitra[]>([])`
    - Tambah useEffect SEBELUM `if (user?.role !== 'admin') return ...`:
      ```
      useEffect(() => {
        api.users.list().then(setUsers).catch(console.error);
        api.mitras.list().then(setMitras).catch(console.error);
      }, []);
      ```

    **3. handleToggleActive** — ubah jadi async:
    ```
    const updated = await api.users.update(targetId, { isActive: !currentStatus });
    await api.auditLogs.create({ userId: user.id, action: currentStatus ? 'USER_DISABLED' : 'USER_ENABLED', details: `Target: ${targetId}` });
    setUsers(prev => prev.map(u => u.id === targetId ? updated : u));
    setSelectedUser(updated);
    toast.success(...);
    ```
    Wrap dalam try/catch, toast.error(err.message) jika gagal.

    **4. handleUnlock** — ubah jadi async:
    ```
    const updated = await api.users.update(targetId, { lockedUntil: null, failedLoginAttempts: 0 });
    await api.auditLogs.create({ userId: user.id, action: 'USER_UNLOCKED', details: `Target: ${targetId}` });
    setUsers(prev => prev.map(u => u.id === targetId ? updated : u));
    setSelectedUser(updated);
    ```

    **5. handleResetPassword** — ubah jadi async (PENTING: pakai newPassword, bukan passwordHash):
    ```
    const updated = await api.users.update(targetId, { newPassword: 'rema1234', mustChangePassword: true });
    await api.auditLogs.create({ userId: user.id, action: 'PASSWORD_RESET_ADMIN', details: `Target: ${targetId}` });
    setUsers(prev => prev.map(u => u.id === targetId ? updated : u));
    setSelectedUser(updated);
    ```
    Server akan bcrypt-hash 'rema1234' — tidak ada plaintext di DB.

    **6. handleEditSave** — ubah signature dan implementasi:
    ```
    const handleEditSave = async (updatedUser: SafeUser) => {
      const oldUser = users.find(u => u.id === updatedUser.id);
      try {
        // Auto-create mitra jika role berubah ke mitra
        if (oldUser && oldUser.role !== 'mitra' && updatedUser.role === 'mitra') {
          const existingMitra = mitras.find(m => m.userId === updatedUser.id);
          if (!existingMitra) {
            const newMitra = { id: crypto.randomUUID(), userId: updatedUser.id, name: updatedUser.name, creditLimit: undefined, isArchived: false };
            const created = await api.mitras.create(newMitra as Omit<Mitra, 'id'>);
            setMitras(prev => [created, ...prev]);
          }
        }
        const updated = await api.users.update(updatedUser.id, updatedUser);
        await api.auditLogs.create({ userId: user.id, action: 'USER_UPDATED', details: `Target: ${updatedUser.id}` });
        setUsers(prev => prev.map(u => u.id === updatedUser.id ? updated : u));
        setSelectedUser(updated);
        toast.success('Data pengguna berhasil diperbarui');
      } catch (err: any) {
        toast.error(err.message || 'Gagal memperbarui pengguna');
      }
    };
    ```

    **7. handleDelete** — ubah jadi async (validasi sekarang server-side):
    ```
    const handleDelete = async (targetId: string) => {
      if (targetId === user.id) {
        toast.error("Anda tidak dapat menghapus akun Anda sendiri.");
        return;
      }
      const isConfirmed = await confirm({ ... });
      if (!isConfirmed) return;
      try {
        await api.users.remove(targetId);
        await api.auditLogs.create({ userId: user.id, action: 'USER_DELETED', details: `User ${targetId}` });
        setUsers(prev => prev.filter(u => u.id !== targetId));
        setMitras(prev => prev.filter(m => m.userId !== targetId));
        setSelectedUser(null);
        toast.success('Pengguna berhasil dihapus');
      } catch (err: any) {
        toast.error(err.message || 'Gagal menghapus pengguna');
      }
    };
    ```
    Hapus semua pre-flight db.getAuditLogs/getMitras/getOrders/getLedgers/getRequests — server yang validasi dan return 409 dengan pesan yang tepat.

    **8. AddUserModal** — update onAdd callback dan implementasi:
    - Ubah prop: `onAdd: (user: SafeUser) => void`
    - Ubah parent's onAdd invocation: `<AddUserModal onClose={...} onAdd={(newUser) => { setUsers(prev => [newUser, ...prev]); }} />`
    - Di dalam AddUserModal.handleSubmit:
      - Hapus `db.getUsers()`, `db.saveUsers()`, `db.saveMitras()`, `db.addAuditLog()`
      - Tambah async ke handleSubmit
      - Hapus phone uniqueness check client-side (server return 409 jika duplicate via DB constraint)
      - `const created = await api.users.create({ id: crypto.randomUUID(), name, phone: p, role, password: 'rema1234', isActive: true, mustChangePassword: true, failedLoginAttempts: 0 })`
      - Jika role === 'mitra': `await api.mitras.create({ id: crypto.randomUUID(), userId: created.id, name, creditLimit: undefined, isArchived: false } as Omit<Mitra, 'id'>)`
      - `await api.auditLogs.create({ userId: 'system', action: 'USER_CREATED', details: \`User ${name} created\` })`
      - `onAdd(created); onClose(); toast.success(...)`
      - Wrap dalam try/catch, toast.error(err.message)

    **9. UserDetailPanel** — update types dan phone check:
    - Ubah `userTarget: User` → `userTarget: SafeUser`
    - Tambah prop: `allUsers: SafeUser[]`
    - Di handleSubmit, phone uniqueness check: ganti `db.getUsers().find(...)` dengan `allUsers.find(u => u.phone === p && u.id !== userTarget.id)`
    - Ubah `onSave: (u: User) => void` → `onSave: (u: SafeUser) => void`
    - Di parent render, tambah `allUsers={users}` pada UserDetailPanel

    **10. Semua lokal `User[]` state type** → `SafeUser[]`

    Avoid: Mengubah UI, styling, atau logika tampilan apapun. Hanya ganti db.* dengan api.*.
  </action>
  <verify>
    1. grep -n "db\." src/pages/Users.tsx — harus tidak ada output
    2. npx tsc --noEmit — exit 0
  </verify>
  <done>AC-1, AC-3, AC-4 satisfied: Users.tsx bebas db.*, reset password via API dengan bcrypt, TypeScript clean</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/pages/Mitras.tsx, Finance.tsx, CancellationsReturns.tsx — plan berikutnya
- src/pages/orders/* — plan berikutnya
- src/lib/db.ts — tetap ada sebagai fallback
- src/db/schema.ts — schema tidak berubah
- UI/styling/UX di Users.tsx — hanya data layer yang berubah
- Logika display (role badges, lock indicators, dll) — tidak berubah

## SCOPE LIMITS
- Hanya Users.tsx yang dimigrasikan di plan ini
- DELETE endpoint hanya untuk /users/:id — bukan resource lain
- Tidak ada perubahan pada auth flow (Login, ChangePassword)

</boundaries>

<verification>
Sebelum deklarasi plan selesai:
- [ ] grep -n "db\." src/pages/Users.tsx — zero output
- [ ] grep -n "router.delete" src/api/routes/users.ts — ada match
- [ ] grep -n "remove:" src/lib/api.ts — ada match di users section
- [ ] npx tsc --noEmit — exit 0
</verification>

<success_criteria>
- Users.tsx bebas dari import db dan semua db.* calls
- DELETE /api/users/:id ada dengan 4 validasi (self-guard, audit trail, mitra dependencies, cascade delete)
- api.users.remove() tersedia di src/lib/api.ts
- handleResetPassword menggunakan api.users.update dengan newPassword (tidak plaintext)
- npx tsc --noEmit clean
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/02-data-layer-migration/02-05-SUMMARY.md`
</output>
