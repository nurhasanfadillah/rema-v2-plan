---
phase: 02-data-layer-migration
plan: 07
type: execute
wave: 1
depends_on: ["02-06"]
files_modified:
  - src/pages/Finance.tsx
  - src/api/routes/ledgers.ts
  - src/lib/api.ts
autonomous: true
---

<objective>
## Goal
Migrasi Finance.tsx (825L) dari localStorage ke API — hapus semua `db.*` calls di komponen utama dan tiga sub-komponen (PaymentModal, ChargeModal, TransactionDetailModal).

## Purpose
Finance.tsx adalah halaman keuangan — input pembayaran, manual charge, riwayat jurnal. Setelah plan ini, hanya orders/* (3 file) yang tersisa di Phase 2.

## Output
- `Finance.tsx` — full async: useEffect load mitras/ledgers/orders, semua mutasi via api.ledgers.*
- `PUT /api/ledgers/:id` — update ledger entry (dibutuhkan PaymentModal + ChargeModal edit mode)
- `DELETE /api/ledgers/:id` — hapus satu ledger entry (dibutuhkan handleDeleteLedger)
- `api.ledgers.update()` + `api.ledgers.remove()` di src/lib/api.ts
- `mitras` prop diteruskan ke PaymentModal, ChargeModal, TransactionDetailModal (bukan db.getMitras() lokal)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/02-data-layer-migration/02-06-SUMMARY.md

## Source Files
@src/pages/Finance.tsx
@src/api/routes/ledgers.ts
@src/lib/api.ts
</context>

<acceptance_criteria>

## AC-1: Finance.tsx bebas dari db.*
```gherkin
Given Finance.tsx dibuka di browser
When admin melihat jurnal keuangan, tambah pembayaran, tambah manual charge, atau hapus transaksi
Then semua data ter-load via API, semua mutasi dikirim ke API, tidak ada referensi ke import { db } dari lib/db
```

## AC-2: PUT /api/ledgers/:id dan DELETE /api/ledgers/:id tersedia
```gherkin
Given ledger entry sudah ada di DB
When PUT /api/ledgers/:id dipanggil dengan data baru
Then entry terupdate dan dikembalikan
And When DELETE /api/ledgers/:id dipanggil
Then entry terhapus dari DB
```

## AC-3: PaymentModal dan ChargeModal berfungsi via API
```gherkin
Given admin membuka modal Input Bayar atau Tagihan Manual
When admin mengisi form dan submit (create atau edit mode)
Then ledger entry tersimpan/terupdate via api.ledgers.create/update
And parent state diupdate tanpa full page reload
```

## AC-4: TypeScript zero errors
```gherkin
Given semua perubahan diterapkan
When npx tsc --noEmit dijalankan
Then output bersih tanpa TypeScript error
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add PUT /:id + DELETE /:id di ledgers.ts + update api.ts</name>
  <files>src/api/routes/ledgers.ts, src/lib/api.ts</files>
  <action>
    **A. Tambah PUT /api/ledgers/:id di ledgers.ts:**

    ```typescript
    router.put('/:id', async (req: Request, res: Response) => {
      try {
        const [updated] = await db.update(ledgers)
          .set(req.body)
          .where(eq(ledgers.id, req.params.id))
          .returning();
        if (!updated) return res.status(404).json({ error: 'Ledger not found' });
        return res.json(updated);
      } catch (err: any) {
        return res.status(500).json({ error: err.message });
      }
    });
    ```

    **B. Tambah DELETE /api/ledgers/:id di ledgers.ts:**

    Perhatikan: DELETE /order/:orderId sudah ada. Route DELETE /:id harus ditempatkan SETELAH /order/:orderId agar tidak di-intercept. Urutan route Express penting.

    ```typescript
    router.delete('/:id', async (req: Request, res: Response) => {
      try {
        await db.delete(ledgers).where(eq(ledgers.id, req.params.id));
        return res.json({ success: true });
      } catch (err: any) {
        return res.status(500).json({ error: err.message });
      }
    });
    ```

    **C. Tambah api.ledgers.update() + api.ledgers.remove() di api.ts:**

    ```typescript
    ledgers: {
      list: (mitraId?: string) =>
        request<LedgerEntry[]>('GET', mitraId ? `/ledgers?mitraId=${mitraId}` : '/ledgers'),
      create: (data: Omit<LedgerEntry, 'id'>) => request<LedgerEntry>('POST', '/ledgers', data),
      update: (id: string, data: Partial<LedgerEntry>) => request<LedgerEntry>('PUT', `/ledgers/${id}`, data),
      remove: (id: string) => request<void>('DELETE', `/ledgers/${id}`),
      removeByOrder: (orderId: string) => request<void>('DELETE', `/ledgers/order/${orderId}`),
    },
    ```
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero errors
    2. `grep -n "router.put\|router.delete" src/api/routes/ledgers.ts` — ada PUT /:id dan DELETE /:id
    3. `grep -n "update:\|remove:" src/lib/api.ts` — ada api.ledgers.update dan api.ledgers.remove
  </verify>
  <done>AC-2 satisfied: PUT /api/ledgers/:id dan DELETE /api/ledgers/:id tersedia</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Finance.tsx (main + PaymentModal + ChargeModal + TransactionDetailModal)</name>
  <files>src/pages/Finance.tsx</files>
  <action>
    Ubah imports: hapus `import { db } from '../lib/db'`, tambah `import { api } from '../lib/api'`.
    Tambah import `useEffect` dari React, tambah import `Mitra, Order` dari types (jika belum ada).

    **A. Main component — ubah state loading:**

    ```typescript
    // SEBELUM
    const mitras = db.getMitras();
    const [ledgers, setLedgers] = useState(db.getLedgers());
    const orders = db.getOrders();

    // SESUDAH
    const [mitras, setMitras] = useState<Mitra[]>([]);
    const [ledgers, setLedgers] = useState<LedgerEntry[]>([]);
    const [orders, setOrders] = useState<Order[]>([]);

    useEffect(() => {
      Promise.all([
        api.mitras.list(),
        api.ledgers.list(),
        api.orders.list(),
      ]).then(([m, l, o]) => {
        setMitras(m);
        setLedgers(l);
        setOrders(o);
      }).catch(() => toast.error('Gagal memuat data keuangan'));
    }, []);
    ```

    Catatan: selectedMitraId initial state bergantung pada `activeMitra`. Karena mitras di-load async, initial value tidak bisa langsung pakai mitras. Pindahkan `activeMitra` dan `selectedMitraId` initialization ke useEffect:

    ```typescript
    const [selectedMitraId, setSelectedMitraId] = useState<string>('all');

    useEffect(() => {
      Promise.all([
        api.mitras.list(),
        api.ledgers.list(),
        api.orders.list(),
      ]).then(([m, l, o]) => {
        setMitras(m);
        setLedgers(l);
        setOrders(o);
        // Set selectedMitraId berdasarkan role setelah mitras loaded
        if (user?.role === 'mitra') {
          const active = m.find(mt => mt.userId === user.id);
          if (active) setSelectedMitraId(active.id);
        }
      }).catch(() => toast.error('Gagal memuat data keuangan'));
    }, [user]);
    ```

    **B. handleDeleteLedger:**

    ```typescript
    const handleDeleteLedger = async (id: string) => {
      const isConfirmed = await confirm({ ... }); // sama
      if (!isConfirmed) return;
      try {
        await api.ledgers.remove(id);
        setLedgers(prev => prev.filter(l => l.id !== id));
        toast.success('Transaksi berhasil dihapus');
      } catch (err: any) {
        toast.error(err.message || 'Gagal menghapus transaksi');
      }
    };
    ```

    **C. handleRefreshLedgers → handleLedgerSaved:**

    Ubah nama dan signature — modals akan pass ledger yang baru/updated, bukan trigger full reload:

    ```typescript
    const handleLedgerSaved = (entry: LedgerEntry, isEdit: boolean) => {
      if (isEdit) {
        setLedgers(prev => prev.map(l => l.id === entry.id ? entry : l));
      } else {
        setLedgers(prev => [entry, ...prev]);
      }
    };
    ```

    Update semua pemanggil `handleRefreshLedgers` → `handleLedgerSaved` di JSX:
    - `{isPaymentOpen && <PaymentModal onClose={...} onSave={handleLedgerSaved} mitras={mitras} ... />}`
    - `{isChargeOpen && <ChargeModal onClose={...} onSave={handleLedgerSaved} mitras={mitras} ... />}`
    - `{editingLedger && <PaymentModal editData={editingLedger} onClose={...} onSave={handleLedgerSaved} mitras={mitras} ... />}`
    - `{editingLedger && <ChargeModal editData={editingLedger} onClose={...} onSave={handleLedgerSaved} mitras={mitras} ... />}`

    Juga pass `mitras` ke TransactionDetailModal:
    - `{selectedLedger && <TransactionDetailModal ledger={selectedLedger} mitras={mitras} ... />}`

    **D. Migrate PaymentModal:**

    Ubah props signature — tambah `mitras: Mitra[]`, ubah `onSave: (entry: LedgerEntry, isEdit: boolean) => void`:

    ```typescript
    function PaymentModal({
      onClose, onSave, formattedNominal, editData, mitras
    }: {
      onClose: () => void,
      onSave: (entry: LedgerEntry, isEdit: boolean) => void,
      formattedNominal: (v: string) => string,
      editData?: LedgerEntry,
      mitras: Mitra[]
    }) {
      const [mitra, setMitra] = useState(editData?.mitraId || mitras[0]?.id || '');
      // ... state sama

      const submit = async (e: React.FormEvent) => {
        e.preventDefault();
        const cleanNum = parseInt(nominalDisplay.replace(/\D/g, ''));
        if (isNaN(cleanNum) || cleanNum <= 0) { toast.error('...'); return; }

        const isConfirmed = await confirm({ ... }); // sama
        if (!isConfirmed) return;

        try {
          if (editData) {
            const updated = await api.ledgers.update(editData.id, {
              mitraId: mitra,
              nominal: cleanNum,
              description: `Pembayaran ${method.toUpperCase()} - Ref: ${ref}`,
              paymentMethod: method,
              referenceNumber: ref,
              attachmentUrl: attachment,
            });
            await api.auditLogs.create({ userId: 'admin', action: 'PAYMENT_EDITED', details: `ID: ${editData.id}, Nominal: ${cleanNum}` });
            toast.success('Pembayaran berhasil diperbarui!');
            onSave(updated, true);
          } else {
            const entry = await api.ledgers.create({
              mitraId: mitra,
              source: 'payment',
              direction: 'credit',
              nominal: cleanNum,
              description: `Pembayaran ${method.toUpperCase()} - Ref: ${ref}`,
              createdAt: Date.now(),
              paymentMethod: method,
              referenceNumber: ref,
              attachmentUrl: attachment,
            });
            await api.auditLogs.create({ userId: 'admin', action: 'PAYMENT_ADDED', details: `Nominal: ${entry.nominal}` });
            toast.success('Pembayaran berhasil ditambahkan!');
            onSave(entry, false);
          }
          onClose();
        } catch (err: any) {
          toast.error(err.message || 'Gagal menyimpan pembayaran');
        }
      };

      // Di JSX: ganti {db.getMitras().map(...)} dengan {mitras.map(...)}
    }
    ```

    **E. Migrate ChargeModal:**

    Sama seperti PaymentModal — tambah `mitras: Mitra[]` prop, ubah `onSave` signature, ganti db.* dengan api.*:

    ```typescript
    function ChargeModal({
      onClose, onSave, formattedNominal, editData, mitras
    }: {
      onClose: () => void,
      onSave: (entry: LedgerEntry, isEdit: boolean) => void,
      formattedNominal: (v: string) => string,
      editData?: LedgerEntry,
      mitras: Mitra[]
    }) {
      const [mitra, setMitra] = useState(editData?.mitraId || mitras[0]?.id || '');

      const submit = async (e: React.FormEvent) => {
        // ...
        try {
          if (editData) {
            const updated = await api.ledgers.update(editData.id, {
              mitraId: mitra,
              nominal: cleanNum,
              description: desc,
            });
            await api.auditLogs.create({ userId: 'admin', action: 'MANUAL_CHARGE_EDITED', details: `ID: ${editData.id}, Nominal: ${cleanNum}` });
            toast.success('Manual charge berhasil diperbarui!');
            onSave(updated, true);
          } else {
            const entry = await api.ledgers.create({
              mitraId: mitra,
              source: 'manual',
              direction: 'debit',
              nominal: cleanNum,
              description: desc,
              createdAt: Date.now(),
            });
            await api.auditLogs.create({ userId: 'admin', action: 'MANUAL_CHARGE_ADDED', details: `Nominal: ${entry.nominal}` });
            toast.success('Manual charge (debit) berhasil ditambahkan!');
            onSave(entry, false);
          }
          onClose();
        } catch (err: any) {
          toast.error(err.message || 'Gagal menyimpan tagihan');
        }
      };

      // Di JSX: ganti {db.getMitras().map(...)} dengan {mitras.map(...)}
    }
    ```

    **F. Migrate TransactionDetailModal:**

    Tambah `mitras: Mitra[]` prop, hapus `db.getMitras()`:

    ```typescript
    function TransactionDetailModal({
      ledger, onClose, onEdit, onDelete, isAdmin, mitras
    }: {
      ledger: LedgerEntry,
      onClose: () => void,
      onEdit: (l: LedgerEntry) => void,
      onDelete: (id: string) => void,
      isAdmin: boolean,
      mitras: Mitra[]
    }) {
      // Ganti: const mitras = db.getMitras();
      const mName = mitras.find(m => m.id === ledger.mitraId)?.name || 'Unknown';
      // sisa sama
    }
    ```

    **G. Hapus `activeMitra` di luar useEffect:**

    Setelah pindah initializer ke useEffect, pastikan `activeMitra` yang dipakai di render (`mitras.find(m => m.userId === user?.id)`) tetap ada untuk display — ini tetap bisa dihitung dari state:

    ```typescript
    const activeMitra = mitras.find(m => m.userId === user?.id);
    ```

    Ini bisa tetap di luar useEffect sebagai computed value dari state.
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero errors
    2. `grep -n "db\." src/pages/Finance.tsx` — no output
    3. `grep -n "import.*db" src/pages/Finance.tsx` — no output
    4. Jalankan dev server, buka `/finance` — daftar jurnal muncul
    5. Test filter by mitra → jurnal ter-filter
  </verify>
  <done>AC-1 satisfied: Finance.tsx bebas db.*, AC-3 satisfied: PaymentModal + ChargeModal via API, AC-4 satisfied: TypeScript clean</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/lib/db.ts` — legacy localStorage wrapper, jangan dimodifikasi
- `src/db/schema.ts` — schema sudah final
- `src/types.ts` — jangan ubah type definitions
- `src/api/routes/orders.ts` — out of scope, Plan 02-08
- `src/api/routes/mitras.ts` — sudah selesai di Plan 02-06

## SCOPE LIMITS
- Jangan migrate orders/* — itu Plan 02-08
- Jangan ubah visual/styling komponen
- FileUpload.tsx tetap seperti sekarang (R2 adalah Phase 3)
- Jangan tambah fitur baru (pagination server-side, dll)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -rn "db\." src/pages/Finance.tsx` — no output
- [ ] `npx tsc --noEmit` — clean
- [ ] PUT /api/ledgers/:id route ada di ledgers.ts
- [ ] DELETE /api/ledgers/:id route ada di ledgers.ts (setelah DELETE /order/:orderId)
- [ ] api.ledgers.update() dan api.ledgers.remove() ada di api.ts
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Finance.tsx: zero db.* references, semua data via API
- PaymentModal + ChargeModal: create/update via api.ledgers.*
- TransactionDetailModal: mitras dari props, bukan db.getMitras()
- PUT + DELETE /api/ledgers/:id berfungsi
- TypeScript clean
- Sisa: orders/OrdersList.tsx + orders/CreateOrder.tsx + orders/OrderDetail.tsx (Plan 02-08)
</success_criteria>

<output>
After completion, create `.paul/phases/02-data-layer-migration/02-07-SUMMARY.md`
</output>
