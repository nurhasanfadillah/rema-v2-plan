---
phase: 23-finance-desktop-enterprise-polish
plan: 03
type: execute
autonomous: true
---

<objective>
## Goal
Hilangkan scrollbar vertikal pada modal Catat Pembayaran (PaymentModal) — modal tampil clean tanpa scroll indicator.
</objective>

<context>
@.paul/PROJECT.md
@src/pages/Finance.tsx
</context>

<acceptance_criteria>
## AC-1: Modal tanpa scrollbar
Given user membuka modal Catat Pembayaran
When modal tampil
Then tidak ada scrollbar vertikal yang terlihat
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Remove overflow-y-auto from PaymentModal container</name>
  <files>src/pages/Finance.tsx</files>
  <action>
    Pada PaymentModal, line 748:
    Ubah `max-h-[95vh] overflow-y-auto` → `max-h-[95vh]` 
    
    Hapus `overflow-y-auto` dari container modal PaymentModal. 
    Form hanya memiliki 5 field (Mitra, Nominal, Metode, Referensi, Upload) + 2 button — konten muat dalam viewport.
    Jika tetap ingin fallback safety di layar sangat kecil, gunakan class `no-scrollbar` yang sudah didefinisikan di index.css.
    
    Avoid: Jangan hapus `max-h-[95vh]` — itu tetap diperlukan untuk mencegah modal overflow di layar kecil.
  </action>
  <verify>npm run lint passes; modal Catat Pembayaran tampil tanpa scrollbar</verify>
  <done>AC-1 satisfied: PaymentModal tanpa overflow-y-auto, scrollbar hilang</done>
</task>
</tasks>

<output>
After completion, create compressed SUMMARY.md
</output>
