---
phase: 16-produk-form-scroll-fix
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Perbaiki modal form Tambah/Edit Produk yang overflow screen tanpa bisa di-scroll — tambahkan max-h-[90vh] overflow-y-auto no-scrollbar agar konten bisa di-scroll saat viewport kecil, tanpa scrollbar terlihat secara visual.
</objective>

<context>
@.paul/PROJECT.md
@src/pages/Products.tsx
</context>

<acceptance_criteria>
## AC-1: Form bisa scroll tanpa scrollbar terlihat
Given user membuka form Tambah Produk di layar kecil (atau dengan keyboard mobile muncul)
When konten form melebihi tinggi viewport
Then form bisa di-scroll ke bawah tanpa ada visual scrollbar
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Tambahkan max-h + overflow-y-auto + no-scrollbar ke modal ProductFormModal</name>
  <files>src/pages/Products.tsx</files>
  <action>
    Di komponen ProductFormModal (baris 361), ubah wrapper modal div:
    - Dari: `className="bg-white rounded-3xl shadow-xl w-full max-w-md p-5 animate-in slide-in-from-bottom-8"`
    - Ke: `className="bg-white rounded-3xl shadow-xl w-full max-w-md max-h-[90vh] overflow-y-auto no-scrollbar p-5 animate-in slide-in-from-bottom-8"`

    Penambahan: `max-h-[90vh] overflow-y-auto no-scrollbar`
    — max-h membatasi tinggi maksimal modal
    — overflow-y-auto mengaktifkan scroll saat konten melebihi tinggi
    — no-scrollbar menyembunyikan scrollbar secara visual (class sudah ada di index.css)
  </action>
  <verify>Buka halaman Products → klik "Tambah Produk" → perkecil jendela browser atau buka di mobile → modal bisa di-scroll tanpa scrollbar terlihat</verify>
  <done>AC-1 satisfied: form scroll tersedia tanpa visual scrollbar</done>
</task>
</tasks>

<output>
After completion, create `.paul/phases/16-produk-form-scroll-fix/16-01-SUMMARY.md`
</output>
