---
phase: 28-pwa-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vite-env.d.ts
  - src/components/PWAUpdateBanner.tsx
  - src/components/Layout.tsx
autonomous: true
---

<objective>
## Goal
Tambah dua fitur PWA: (1) banner notifikasi saat versi baru tersedia + tombol "Perbarui Sekarang", dan (2) tombol install PWA di header saat browser mendukung install prompt.

## Purpose
Saat ini `registerType: 'autoUpdate'` aktif tapi silent — user tidak tahu ada update dan halaman bisa reload tiba-tiba. Install prompt hanya mengandalkan browser default yang timing-nya tidak terkontrol.

## Output
- File baru: `src/vite-env.d.ts` (type reference), `src/components/PWAUpdateBanner.tsx`
- File dimodifikasi: `src/components/Layout.tsx`
</objective>

<context>
@.paul/PROJECT.md
@src/components/Layout.tsx
@.paul/phases/28-pwa-audit/DISCOVERY.md
</context>

<acceptance_criteria>

## AC-1: Update Banner Muncul Saat Ada Versi Baru
```gherkin
Given app di-deploy ke production dengan versi baru
When service worker mendeteksi update tersedia
Then muncul banner fixed di bagian bawah layar dengan pesan "Versi baru tersedia" dan tombol "Perbarui Sekarang"
And mengklik tombol "Perbarui Sekarang" mereload halaman ke versi terbaru
And banner tidak muncul dalam kondisi normal (tidak ada update)
```

## AC-2: Tombol Install PWA di Header
```gherkin
Given pengguna membuka app di browser yang mendukung PWA install (Chrome/Edge desktop atau Android)
And app belum terinstall
When browser memicu beforeinstallprompt event
Then muncul tombol install (ikon download) di area header sebelah kiri avatar
And mengklik tombol memunculkan native install dialog browser
And setelah install selesai, tombol hilang otomatis
```

## AC-3: TypeScript Tidak Error
```gherkin
Given semua file dimodifikasi
When npm run lint dijalankan
Then tidak ada TypeScript error baru
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Type reference + PWAUpdateBanner component</name>
  <files>src/vite-env.d.ts, src/components/PWAUpdateBanner.tsx</files>
  <action>
    **1. Buat `src/vite-env.d.ts`:**
    ```typescript
    /// <reference types="vite/client" />
    /// <reference types="vite-plugin-pwa/client" />
    ```
    File ini diperlukan agar TypeScript mengenali `virtual:pwa-register/react`.

    **2. Buat `src/components/PWAUpdateBanner.tsx`:**
    ```tsx
    import { useRegisterSW } from 'virtual:pwa-register/react';

    export function PWAUpdateBanner() {
      const { needRefresh: [needRefresh], updateServiceWorker } = useRegisterSW();

      if (!needRefresh) return null;

      return (
        <div className="fixed bottom-0 left-0 right-0 z-50 bg-slate-900 border-t border-blue-500/20 px-4 py-3 flex items-center justify-between gap-4 shadow-2xl">
          <div className="min-w-0">
            <p className="text-[13px] font-bold text-white leading-tight">Versi baru tersedia</p>
            <p className="text-[11px] text-slate-400 font-medium mt-0.5">Perbarui untuk mendapatkan fitur terbaru.</p>
          </div>
          <button
            onClick={() => updateServiceWorker(true)}
            className="flex-shrink-0 px-4 py-2 bg-blue-600 hover:bg-blue-500 text-white text-[11px] font-bold rounded-xl transition-all active:scale-95 cursor-pointer uppercase tracking-wide shadow-lg shadow-blue-600/20"
          >
            Perbarui Sekarang
          </button>
        </div>
      );
    }
    ```
    Jangan tambahkan state lokal — `useRegisterSW` sudah mengelola state sendiri.
  </action>
  <verify>
    - `npm run lint` lulus tanpa error
    - File `src/vite-env.d.ts` ada dan berisi kedua reference
    - File `src/components/PWAUpdateBanner.tsx` ada dan menggunakan `useRegisterSW`
  </verify>
  <done>Komponen siap di-mount; AC-3 terpenuhi sebagian</done>
</task>

<task type="auto">
  <name>Task 2: Mount PWAUpdateBanner + install prompt di Layout.tsx</name>
  <files>src/components/Layout.tsx</files>
  <action>
    **Di `src/components/Layout.tsx`:**

    **1. Tambah import:**
    ```tsx
    import { useState, useEffect } from 'react'; // useEffect sudah ada, pastikan useState juga
    import { Download } from 'lucide-react';
    import { PWAUpdateBanner } from './PWAUpdateBanner';
    ```

    **2. Tambah type declaration (di atas fungsi Layout):**
    ```tsx
    interface BeforeInstallPromptEvent extends Event {
      prompt(): Promise<void>;
      userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>;
    }
    ```

    **3. Tambah state + effect di dalam fungsi `Layout()` (setelah state yang sudah ada):**
    ```tsx
    const [installPrompt, setInstallPrompt] = useState<BeforeInstallPromptEvent | null>(null);

    useEffect(() => {
      const handler = (e: Event) => {
        e.preventDefault();
        setInstallPrompt(e as BeforeInstallPromptEvent);
      };
      const installed = () => setInstallPrompt(null);
      window.addEventListener('beforeinstallprompt', handler);
      window.addEventListener('appinstalled', installed);
      return () => {
        window.removeEventListener('beforeinstallprompt', handler);
        window.removeEventListener('appinstalled', installed);
      };
    }, []);
    ```

    **4. Tambah tombol install di header** — di dalam `<div className="flex items-center gap-2 sm:gap-3">`, tepat sebelum div avatar (yang onClick setProfileOpen):
    ```tsx
    {installPrompt && (
      <button
        onClick={() => installPrompt.prompt()}
        className="p-2 rounded-xl text-slate-400 hover:text-blue-400 hover:bg-slate-800 transition-all active:scale-95 cursor-pointer"
        title="Install Aplikasi"
      >
        <Download className="w-4.5 h-4.5" />
      </button>
    )}
    ```

    **5. Mount `<PWAUpdateBanner />` di dalam return, di dalam div wrapper utama** (`<div className="flex h-screen w-full...">`), tepat sebelum tag penutup `</div>` terakhir:
    ```tsx
    <PWAUpdateBanner />
    ```

    Jangan ubah sidebar, nav links, header structure, atau logika auth.
    Pastikan `useState` sudah ada di import React (sudah pasti ada karena dipakai di mobileOpen dll).
  </action>
  <verify>
    - `npm run lint` lulus tanpa error baru
    - Grep `PWAUpdateBanner` di Layout.tsx → ada di import dan render
    - Grep `beforeinstallprompt` di Layout.tsx → ada di useEffect
    - Grep `Download` di import lucide-react Layout.tsx → ada
  </verify>
  <done>AC-1, AC-2, AC-3 terpenuhi</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `vite.config.ts` — registerType: 'autoUpdate' sudah benar, jangan diubah
- `src/App.tsx` — tidak perlu modifikasi, PWA dihandle di Layout
- Logika sidebar, nav links, auth, atau halaman lain

## SCOPE LIMITS
- Tidak ada perubahan backend/API
- Tidak ada push notification (beda phase)
- Tidak ada perubahan manifest atau service worker config
- Tidak menambah dependencies baru (useRegisterSW sudah termasuk di vite-plugin-pwa)

</boundaries>

<verification>
- [ ] `npm run lint` lulus tanpa error TypeScript
- [ ] `src/vite-env.d.ts` ada dengan dua reference lines
- [ ] `src/components/PWAUpdateBanner.tsx` ada, menggunakan `useRegisterSW`
- [ ] Layout.tsx berisi: import PWAUpdateBanner, import Download, BeforeInstallPromptEvent type, installPrompt state + useEffect, tombol install, `<PWAUpdateBanner />`
- [ ] Tidak ada regresi di komponen lain
</verification>

<success_criteria>
- Semua 2 task selesai
- `npm run lint` lulus
- Semua 3 AC terverifikasi
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/28-pwa-audit/28-01-SUMMARY.md`
</output>
