---
phase: 30-priority-header-mobile-fix
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Pindahkan button "Tambah Prioritas" ke baris kedua pada mobile — tidak sejajar dengan judul halaman.
</objective>

<context>
@.paul/PROJECT.md
@src/pages/OrderPriorities.tsx
</context>

<acceptance_criteria>
## AC-1: Button di Baris Kedua pada Mobile
```gherkin
Given halaman Prioritas Pesanan dibuka di layar mobile (< md breakpoint)
When user melihat header halaman
Then button "Tambah Prioritas" muncul di baris kedua, rata kanan, di bawah judul "Prioritas Pesanan"
```

## AC-2: Desktop Tidak Berubah
```gherkin
Given halaman Prioritas Pesanan dibuka di layar desktop (≥ md breakpoint)
When user melihat header halaman
Then judul dan button tetap sejajar dalam satu baris seperti sekarang
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Fix header layout — mobile stacked, desktop inline</name>
  <files>src/pages/OrderPriorities.tsx</files>
  <action>
    Di baris 115, ubah className wrapper container dari:
    `flex items-center justify-between gap-3 mb-6`
    menjadi:
    `flex flex-col gap-3 mb-6 md:flex-row md:items-center md:justify-between`

    Di baris 128, tambah `self-end md:self-auto` pada className button agar rata kanan di mobile:
    `btn-primary flex-shrink-0 self-end md:self-auto`
  </action>
  <verify>npm run lint — exit 0, tidak ada TypeScript error</verify>
  <done>AC-1 dan AC-2 satisfied: mobile stacked, desktop inline</done>
</task>
</tasks>

<output>
Setelah selesai, buat `.paul/phases/30-priority-header-mobile-fix/30-01-SUMMARY.md`
</output>
