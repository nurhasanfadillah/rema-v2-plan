---
phase: 22-reports-queue-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Reports.tsx
  - src/pages/AppQueue.tsx
autonomous: true
---

<objective>
## Goal
Polish halaman Laporan (`Reports.tsx`) dan Antrian Produksi (`AppQueue.tsx`) agar konsisten dengan pola visual yang sudah diterapkan di phase 18–21.

## Purpose
Dua halaman ini masih menggunakan layout header custom dan mobile cards tanpa colored left-border, sementara semua halaman lain sudah menggunakan `page-header`/`page-title` utility class dan status-colored border treatment yang konsisten.

## Output
- `Reports.tsx`: header menggunakan `page-header`/`page-title`, tombol Cetak PDF menggunakan `btn-primary`, tab pill alignment diperbaiki
- `AppQueue.tsx`: mobile cards mendapat colored left-border sesuai status produksi (blue/purple/rose/orange)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@src/pages/Reports.tsx
@src/pages/AppQueue.tsx
@src/index.css
</context>

<acceptance_criteria>

## AC-1: Reports Header Konsisten
```gherkin
Given user membuka halaman Laporan
When halaman di-render
Then header menampilkan `page-title` (font putih, tracking-tight) dengan `page-header` layout,
     dan tombol "Cetak PDF" menggunakan `btn-primary` class
```

## AC-2: AppQueue Mobile Cards Colored Left-Border
```gherkin
Given user membuka halaman Antrian Produksi di viewport mobile (< md)
When ada pesanan dengan status confirmed/processing/pressing/packing
Then setiap card menampilkan left-border berwarna sesuai status:
     confirmed=blue-400, processing=purple-400, pressing=rose-400, packing=orange-400
```

## AC-3: Tidak Ada Regresi Visual
```gherkin
Given semua perubahan sudah diaplikasikan
When halaman Laporan dan Antrian dibuka di mobile dan desktop
Then semua elemen existing (filter, chart, tabel, pipeline stats) tetap berfungsi normal
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Reports.tsx — header utility class + button class</name>
  <files>src/pages/Reports.tsx</files>
  <action>
    Ubah bagian header Reports (baris ~169–177):
    
    1. Ganti wrapper `<div className="flex flex-col md:flex-row justify-between ...">` dengan `<div className="page-header items-end">`
    
    2. Ganti `<h1 className="text-xl font-black tracking-tight text-slate-900 ...">` menjadi `<h1 className="page-title">` dengan teks tetap "Laporan & Analitik"
    
    3. Ganti subtitle `<p className="text-[12px] text-slate-500 ...">` menjadi `<p className="text-xs text-slate-400 mt-0.5 font-medium">` (lebih subtle, konsisten dengan halaman lain)
    
    4. Ubah tombol "Cetak PDF" dari class inline menjadi `btn-primary` — pertahankan icon `<Download>` dan onClick handler, hanya ganti className menjadi `"btn-primary"` (tidak perlu tambah class lain)
    
    Avoid: jangan ubah logic tab, filter, chart, atau export handler.
  </action>
  <verify>
    Jalankan `npm run lint` (type-check) — harus pass tanpa error baru.
    Secara visual: header putih/besar, tombol Cetak PDF style konsisten dengan btn-primary di halaman lain.
  </verify>
  <done>AC-1 satisfied: header page-header + page-title + btn-primary terpasang</done>
</task>

<task type="auto">
  <name>Task 2: AppQueue.tsx — mobile cards colored left-border</name>
  <files>src/pages/AppQueue.tsx</files>
  <action>
    Di bagian mobile view (sekitar baris 141–185), pada setiap `motion.div` kartu pesanan:

    1. Tambahkan mapping status → border color (sama persis dengan desktop table di baris 209–214):
    ```
    confirmed  → border-l-blue-400
    processing → border-l-purple-400
    pressing   → border-l-rose-400
    packing    → border-l-orange-400
    ```

    2. Tambahkan `border-l-4` ke className card (setelah `rounded-xl`) dan append `${statusLeftBorder}`:
    ```tsx
    const statusLeftBorder = {
      confirmed: 'border-l-blue-400',
      processing: 'border-l-purple-400',
      pressing: 'border-l-rose-400',
      packing: 'border-l-orange-400'
    }[o.status as string] || 'border-l-slate-200';
    ```

    Gunakan variabel `statusLeftBorder` di className mobile card. Card yang `isMine` mendapat border berwarna; yang bukan tetap `border-l-slate-200`.

    Avoid: jangan ubah desktop table, pipeline stats, atau logic `isMine`.
  </action>
  <verify>
    `npm run lint` pass.
    Di mobile viewport: kartu confirmed punya left-border biru, processing ungu, pressing merah muda, packing oranye.
  </verify>
  <done>AC-2 satisfied: mobile cards menampilkan colored left-border sesuai status</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logic filter, chart, PDF export di Reports.tsx
- Pipeline stats cards (dark bg-slate-950) di AppQueue.tsx — sudah bagus
- Desktop table di AppQueue.tsx — sudah ada colored left-border
- src/index.css — tidak perlu class baru, pakai yang sudah ada

## SCOPE LIMITS
- Tidak mengubah data fetching atau business logic
- Tidak mengubah halaman lain selain Reports.tsx dan AppQueue.tsx
- Tidak mengubah ConfirmContext.tsx atau Login.tsx (uncommitted changes tersebut terpisah)

</boundaries>

<verification>
Sebelum mendeklarasikan plan selesai:
- [ ] `npm run lint` pass (no TypeScript errors)
- [ ] Reports header menggunakan page-header + page-title (text-white)
- [ ] Reports "Cetak PDF" button menggunakan btn-primary
- [ ] AppQueue mobile cards memiliki border-l-4 + status color
- [ ] Tidak ada regresi: filter, chart, tabel, pipeline stats tetap berfungsi
</verification>

<success_criteria>
- Semua task selesai
- Verification checklist pass
- Visual konsisten dengan halaman 18–21 yang sudah di-polish
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/22-reports-queue-polish/22-01-SUMMARY.md`
</output>
