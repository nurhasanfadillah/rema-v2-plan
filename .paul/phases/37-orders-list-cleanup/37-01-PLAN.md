---
phase: 37-orders-list-cleanup
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Hapus kolom "Tipe" dari tabel desktop dan mobile card, lalu tampilkan tipe pesanan + catatan desain per item di renderItemsSummary.
</objective>

<context>
@.paul/PROJECT.md
@src/pages/orders/OrdersList.tsx
</context>

<acceptance_criteria>
## AC-1: Kolom Tipe dihapus
Given daftar pesanan ditampilkan
When user membuka halaman /orders (desktop maupun mobile)
Then kolom "Tipe" tidak muncul di desktop table dan mobile card footer tidak menampilkan label "Tipe"

## AC-2: Item pesanan menampilkan tipe + catatan desain
Given order memiliki items dengan designNotes
When renderItemsSummary dirender
Then setiap item tampil: nama produk × qty, diikuti tipe pesanan (badge kecil) dan designNotes yang ditruncate ≤30 karakter dengan akhiran "..."
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Hapus kolom Tipe dari desktop table dan mobile card</name>
  <files>src/pages/orders/OrdersList.tsx</files>
  <action>
    Desktop table:
    - Hapus `<th className="px-6 py-4">Tipe</th>` (baris header)
    - Hapus blok `<td>` yang merender badge tipe pesanan (online/offline) — baris 467–476
    - Ubah `colSpan={7}` di empty state row menjadi `colSpan={6}`

    Mobile card:
    - Hapus div "Tipe" di footer card (label "Tipe" + nilai `o.type`) — baris 411–414
  </action>
  <verify>Halaman /orders dirender tanpa kolom Tipe di desktop dan tanpa label Tipe di mobile card</verify>
  <done>AC-1 satisfied: kolom Tipe tidak tampil di kedua view</done>
</task>

<task type="auto">
  <name>Update renderItemsSummary: tambah tipe pesanan + designNotes</name>
  <files>src/pages/orders/OrdersList.tsx</files>
  <action>
    Ubah signature: `renderItemsSummary(items: OrderItem[], orderType: string, maxItems = 3)`

    Dalam loop setiap item, setelah baris `productName × qty`, tambahkan:
    - Badge tipe pesanan (order-level, tampil sekali di baris pertama saja — `idx === 0`): badge kecil inline `bg-sky-500/10 text-sky-400` (online) atau `bg-amber-500/10 text-amber-400` (offline), text-[9px] uppercase
    - designNotes jika ada: truncate ke maks 30 karakter dengan "..." — `text-[10px] text-slate-500 italic`

    Helper truncate: `const truncate = (s: string, max = 30) => s.length > max ? s.slice(0, max) + '...' : s`

    Update semua pemanggil renderItemsSummary:
    - Mobile view: `renderItemsSummary(o.items, o.type)`
    - Desktop table: `renderItemsSummary(o.items, o.type)`
  </action>
  <verify>Order dengan designNotes dan tipe menampilkan badge tipe + notes truncated di item summary, order tanpa designNotes tidak menampilkan baris kosong</verify>
  <done>AC-2 satisfied: tipe + designNotes tampil inline di item pesanan</done>
</task>
</tasks>

<output>
Setelah selesai, buat `.paul/phases/37-orders-list-cleanup/37-01-SUMMARY.md`
</output>
