---
phase: 05-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RunningOrders.tsx
  - src/components/Layout.tsx
autonomous: true
---

<objective>
## Goal
Percepat animasi running text pesanan terbaru, dan hapus fitur Edit Profil yang mock (tidak persist ke API).

## Purpose
Dua penyesuaian kecil post-production: UX running text terasa lambat, dan tombol Edit Profil menyesatkan karena perubahan hilang saat refresh.

## Output
- `src/components/RunningOrders.tsx` — duration animasi lebih cepat
- `src/components/Layout.tsx` — state, form, dan tombol Edit Profil dihapus
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@src/components/RunningOrders.tsx
@src/components/Layout.tsx
</context>

<acceptance_criteria>

## AC-1: Running text lebih cepat
```gherkin
Given halaman dengan RunningOrders tampil
When admin melihat ticker pesanan terbaru
Then teks bergerak lebih cepat dari sebelumnya (duration 20, bukan 40)
```

## AC-2: Fitur Edit Profil tidak ada
```gherkin
Given admin membuka profile drawer (klik avatar di header)
When drawer terbuka
Then tidak ada tombol "Edit Profil" maupun form edit nama/telepon
And hanya ada tombol "Ganti Password" dan "Keluar Sesi"
```

## AC-3: Tidak ada regresi
```gherkin
Given kedua file dimodifikasi
When npm run build dijalankan
Then build sukses tanpa TypeScript error
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Percepat animasi RunningOrders</name>
  <files>src/components/RunningOrders.tsx</files>
  <action>
    Di blok `transition`, ubah `duration: 40` menjadi `duration: 20`.
    Tidak ada perubahan lain di file ini.
  </action>
  <verify>grep -n "duration" src/components/RunningOrders.tsx — output: duration: 20</verify>
  <done>AC-1 satisfied: animasi 2x lebih cepat</done>
</task>

<task type="auto">
  <name>Task 2: Hapus fitur Edit Profil dari Layout.tsx</name>
  <files>src/components/Layout.tsx</files>
  <action>
    Hapus semua yang berkaitan dengan Edit Profil:

    1. Hapus 3 state variables di bagian atas fungsi Layout:
       - `const [isEditing, setIsEditing] = useState(false);`
       - `const [editName, setEditName] = useState('');`
       - `const [editPhone, setEditPhone] = useState('');`

    2. Hapus useEffect yang mereset state editing saat profileOpen:
       ```
       useEffect(() => {
         if (profileOpen && user) {
           setEditName(user.name);
           setEditPhone(user.phone || '');
           setIsEditing(false);
         }
       }, [profileOpen, user]);
       ```

    3. Hapus fungsi `handleUpdateProfile`.

    4. Di bagian JSX profile drawer, ganti blok `{isEditing ? (...form...) : (...buttons...)}` 
       dengan hanya konten tombol (versi non-editing):
       - Hapus tombol "Edit Profil" beserta ikonnya
       - Pertahankan tombol "Ganti Password"
       - Pertahankan tombol "Keluar Sesi"

    5. Hapus import `updateUser` dari destructuring useAuth jika tidak dipakai lagi:
       Ubah: `const { user, logout, updateUser } = useAuth();`
       Menjadi: `const { user, logout } = useAuth();`

    6. Periksa juga: import `UserSquare2` dari lucide-react — masih dipakai di nav links,
       jangan dihapus dari import list.

    Avoid: Menghapus tombol Ganti Password atau Keluar Sesi.
  </action>
  <verify>
    1. grep -n "isEditing\|editName\|editPhone\|handleUpdateProfile\|Edit Profil" src/components/Layout.tsx — output: no matches
    2. grep -n "Ganti Password\|Keluar Sesi" src/components/Layout.tsx — output: ada (tidak terhapus)
    3. npm run build — exit 0
  </verify>
  <done>AC-2 + AC-3 satisfied: form dan tombol Edit Profil hilang, build sukses</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/context/AuthContext.tsx` — tidak perlu disentuh
- `src/api/routes/` — semua routes tidak berubah
- Tombol "Ganti Password" dan "Keluar Sesi" di profile drawer

## SCOPE LIMITS
- Jangan tambah fitur edit profil yang real (API call) — out of scope plan ini
- Jangan ubah styling atau layout profile drawer selain menghapus elemen edit
- Jangan ubah kecepatan animasi selain `duration` value

</boundaries>

<verification>
Before declaring plan complete:
- [ ] grep duration RunningOrders.tsx → 20
- [ ] grep isEditing Layout.tsx → no match
- [ ] grep "Edit Profil" Layout.tsx → no match
- [ ] grep "Ganti Password" Layout.tsx → ada
- [ ] npm run build → exit 0
</verification>

<success_criteria>
- duration animasi: 20
- state isEditing/editName/editPhone: dihapus
- handleUpdateProfile: dihapus
- tombol Edit Profil: dihapus
- build sukses tanpa error
</success_criteria>

<output>
After completion, create `.paul/phases/05-ui-fixes/05-01-SUMMARY.md`
</output>
